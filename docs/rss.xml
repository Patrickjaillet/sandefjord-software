<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
<channel>
  <title>Sandefjord Software — What's New</title>
  <link>https://patrickjaillet.github.io/sandefjord-software/whats-new.html</link>
  <description>Every release from every Sandefjord Software application, newest first.</description>
  <language>en</language>
  <item>
    <title>Videotoy v2.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=videotoy</link>
    <guid isPermaLink="false">videotoy-2.0.0</guid>
    <pubDate>Wed, 02 Sep 2026 00:00:00 GMT</pubDate>
    <description>Consolidation release. No functional changes of its own beyond the fixes below — this is a synthesis of everything shipped since v1.0.0, across eight feature phases: v1.1.0 — Hardware encoders (NVENC/Quick Sync/AMF), extended encoding options, automatic export retry, and an export history panel. v1.2.0 — WebM (VP9/Opus) and MOV/ProRes container/codec support. v1.3.0 — Animated image export: GIF (with palette/dithering options) and animated WebP. v1.4.0 — Video files as iChannel inputs, decoded deterministically by timestamp; fixed a long-standing bug where image/audio iChannel bindings and multi-pass Buffer A-D inputs never actually reached the GPU during export. v1.5.0 — Render queue: batch export multiple shaders sequentially, with drag-reorder, thumbnails, per-item progress, error isolation, and persistence across restarts. v1.6.0 — Undo/redo on render/export settings and custom-uniform sliders, with cascade-aware snapshots and gesture coalescing. v1.7.0 — Automatic shader-language detection and support for WGSL and native HLSL sources alongside GLSL, with a status-bar override. v1.8.0 — UI/UX refresh: thematic collapsible settings sections, keyboard accessibility, a first-launch onboarding overlay, Shader Issues filters, a quick-action toolbar, and an animation-performance pass. ### Fixed The application crashed on every launch, on every machine, regardless of GPU — found once the non-regression test campaign could finally run on real hardware: `MainWindow` read `CollectionViewSource.View` before its `Source` binding could ever resolve (a `Window.Resources` binding has no inherited `DataContext` in WPF), always null. `Theme.xaml` and `Icons.xaml` had a `ResourceDictionary` merge-order cycle, each depending on a resource only the other one defined. ### Known limitation The render queue&apos;s persisted/in-memory item list has no automatic pruning of completed or failed entries (product decision on retention deferred to a future phase). No installer binary is attached to this release yet — it will be uploaded separately once compiled.</description>
  </item>
  <item>
    <title>Videotoy v1.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=videotoy</link>
    <guid isPermaLink="false">videotoy-1.0.0</guid>
    <pubDate>Tue, 01 Sep 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to this project are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/). ## [v1.0.0] ### Fixed `Directory.Build.props` / `Videotoy.Core.Version` were still stamped `0.8.0` even though Phase v1.0.0 (installer &amp; release) is now fully complete in `ROADMAP.md`. Bumped `VersionPrefix`/`FileVersion`/ `AssemblyVersion`/`InformationalVersion` in `Directory.Build.props` and `Major`/`Minor`/`Patch` in `Videotoy.Core.Version` to `1.0.0`, so the installer&apos;s `AppVersion`/`OutputBaseFilename` and the in-app &quot;À propos&quot; version both reflect the actual release `docs/index.html`: hero eyebrow badge still read &quot;v0.3.0&quot; instead of the current release version `GlslToHlslTranspiler.fs`: minified/&quot;code-golf&quot; style Shadertoy shaders commonly declare a scalar/vector local without initializing it and rely on it being used before assignment (e.g. `float a,c,h,j; for(u*=a; ...)`), a behavior most GLSL/WebGL drivers tolerate implicitly but that isn&apos;t guaranteed by the GLSL spec. HLSL/FXC rejects this outright (`X4000: variable used without having been completely initialized`), breaking otherwise-valid Shadertoy shaders on load. Added a transpiler pass (`initializeUnassignedLocals`) that scans multi-identifier local declarations of scalar/vector HLSL types (`float`/`int`/`uint`/`bool` and their `2`/`3`/`4`-component forms) and appends an explicit zero initializer (`= 0.`, `= float3(0, 0, 0)`, etc.) to every identifier in the list that doesn&apos;t already have one, leaving already-initialized identifiers and single-identifier declarations untouched. Runs after type replacement so it matches HLSL type names directly `GlslToHlslTranspiler.fs`: `fragCoord` was assigned directly from `SV_Position.xy`, whose origin is top-left with Y increasing downward (Direct3D screen-space convention). Shadertoy&apos;s `fragCoord` origin is bottom-left with Y increasing upward (OpenGL convention), so every transpiled shader rendered vertically flipped versus its Shadertoy preview. `fragCoord.y` is now computed as `iResolution.y - __svPosition.y` in the generated `PSMain` prologue so the Y axis matches Shadertoy&apos;s convention `Directory.Build.props` / `Videotoy.Core.Version` were still stamped `0.3.0`, three completed roadmap phases (v0.5.0–v0.8.0) behind the actually shipped feature set (the last fully-`[x]` phase in `ROADMAP.md`), silently violating this project&apos;s own &quot;automatic SemVer serialization&quot; convention: the installer&apos;s `AppVersion` (read from `Videotoy.exe`&apos;s version resource) and the in-app &quot;À propos&quot; version were both wrong. Bumped `VersionPrefix`/`FileVersion`/`AssemblyVersion`/ `InformationalVersion` in `Directory.Build.props` and `Major`/`Minor` in `Videotoy.Core.Version` to `0.8.0` ### Added `docs/index.html`: added a direct download button for the current release asset (`Videotoy-Setup-1.0.0.exe`), alongside the existing &quot;Voir toutes les releases&quot; link — &quot;Mise à jour finale du site GitHub Pages avec le lien de téléchargement de la release&quot; roadmap item, completing Phase v1.0.0. The download panel&apos;s requirement grid also now shows the current version number SemVer validation and display in the installer — &quot;Signature/vérification de version affichée dans l&apos;installateur (SemVer)&quot; roadmap item: - `installer/Videotoy.iss`: version is now read via the preprocessor&apos;s `GetVersionComponents` (integer Major/Minor/Revision/Build fields) instead of the previous `GetVersionNumbersString` + string-split, so an unreadable version resource is caught explicitly (`&quot;&quot;` return) rather than silently producing a malformed `AppVersion`. Added a build-time guard (`#error`) rejecting an unstamped `0.0.0.x` resource, which would otherwise let a build with a forgotten `Directory.Build.props` bump ship under a fake version. `MyAppVersion` is rebuilt as a strict three-part `X.Y.Z` string (the resource&apos;s fourth, Windows-only `Build` field is dropped, matching this project&apos;s three-part SemVer, not Windows&apos; four-part file version) - `[Code]` `InitializeWizard`: appends the verified `Version : X.Y.Z` line to the Welcome page body (`WelcomeLabel2`), so the SemVer is shown as plain, readable text before any file is installed — not only inside the wizard&apos;s window title / &quot;Ready to Install&quot; summary, which `AppVerName={#MyAppName} {#MyAppVersion}` already drove beforehand and still does `tools/ffmpeg/generate-hash.ps1`: this script was referenced by `.gitignore` (`!/tools/ffmpeg/generate-hash.ps1`), `COMPILATION.md`, and earlier `CHANGELOG.md` entries as already existing, but was missing from the repository — regenerates `tools/ffmpeg/ffmpeg.exe.sha256` (the hash `FfmpegIntegrityVerifier` checks at every startup) from the embedded `ffmpeg.exe`, in the `sha256sum`-compatible format the verifier already parses `tools/ffmpeg/README.md`: local setup notes for embedding `ffmpeg.exe` — where to obtain a Windows build, where to place it, and how `Videotoy.App.csproj` (copy to build output), `installer/Videotoy.iss` (embed in the installer, with a build-time guard if missing), and `FfmpegIntegrityVerifier` (startup SHA-256 check) each depend on it; tracked in Git via a `.gitignore` exception alongside the other `tools/ffmpeg` scaffolding files, closing out the v1.0.0 &quot;Embarquement de `ffmpeg.exe` dans le package d&apos;installation&quot; roadmap item — the packaging path itself (`.csproj` conditional copy + `.iss` `[Files]` recursive bundling + `#error` guard) was already in place since v0.5.0/v0.9.0, the only missing piece was this documented local setup step and the `generate-hash.ps1` script it depends on ### Fixed `Videotoy.App` failed to build (`CS9135`, `CS1061` — F#/C# discriminated union interop): two spots in `MainWindowViewModel.cs` treated F# union cases the way a C# `enum` would, which doesn&apos;t compile — each union case (with or without data) is a distinct nested type in the C#-visible API, not a constant: - `DescribeFirstValidationIssue`&apos;s `switch` on `Videotoy.Core.ExportSettingsValidator.ExportSettingsIssue` used its no-data cases (`InvalidResolution`, `InvalidFrameRate`, ...) as bare constant patterns (`ExportSettingsIssue.InvalidResolution =&gt;`, `CS9135: a constant value is expected`); switched to type patterns with a discard (`ExportSettingsIssue.InvalidResolution _ =&gt;`), the correct C# idiom for matching a data-less F# union case. The data-carrying cases (`OutOfRangeConstantRateFactor of value: int`, `OutOfRangeTargetBitrate of value: int`) also read the captured instance&apos;s field as `.Item` (`CS1061`, no such member); an F# union case with a *named* field exposes it under that field&apos;s own (capitalized) name, not `.Item` — fixed to `.Value`, matching the `value:` field name declared in `ExportSettingsValidator.fs` - `ApplyLoopPeriodDetection` read `detection.SuggestedCandidate is { } candidate` and then accessed `candidate.PeriodSeconds` / `.SourceExpression` / `.PassName` directly (`CS1061`, no such members): `SuggestedCandidate` is a `LoopPeriodCandidate option`, i.e. a C#-side `FSharpOption&lt;LoopPeriodCandidate&gt;` — `is { }` only proved the *option* itself non-null, not that it held a value, so `candidate` was bound to the option wrapper rather than the `LoopPeriodCandidate` inside it. Fixed to the nested property pattern `is { Value: { } candidate }`, which both checks for `Some` and unwraps `.Value` into `candidate` in one step — the same manual `is null` + `.Value` idiom already used correctly elsewhere in this file for `Videotoy.Core.ShaderModel.firstAudioChannelPath`&apos;s `string option` `Videotoy.App` failed to build (`MC1000`/`MC3024`, &quot;the property &apos;Button.Style&apos; has already been set and can only be set once&quot;): `MainWindow.xaml`&apos;s `TogglePanelButton` set `Style` twice — once as a plain attribute (`Style=&quot;{StaticResource IconButtonStyle}&quot;`) and again as a `&lt;Button.Style&gt;` element (added by the v0.8.0 tooltip pass, an inline style with a `DataTrigger` switching the tooltip text and already `BasedOn=&quot;{StaticResource IconButtonStyle}&quot;`) — the attribute form was never removed when the element form was introduced. Removed the now-redundant `Style=` attribute; the button keeps its icon-button appearance unchanged through the remaining `&lt;Button.Style&gt;`&apos;s `BasedOn` `Videotoy.Ffmpeg` failed to build (`CS0101`, `CS0111`, `CS8863`, `CS0708`): two pairs of types were each declared twice across different files, left over from an earlier refactor that was never fully cleaned up: - `FfmpegIntegrityException`: a second, narrower definition (single constructor) had been accidentally left at the top of `FfmpegIntegrityVerifier.cs`, duplicating the real one in `FfmpegIntegrityException.cs` (two constructors, including the `innerException` overload). Removed the duplicate from `FfmpegIntegrityVerifier.cs` - `FfmpegErrorCategory` / `FfmpegStderrDiagnosis` / `FfmpegStderrParser`: `FfmpegStderrDiagnosis.cs` held an entire unused, incompatible second implementation (an instance-based parser with `AppendLine`/`Diagnose()` and a list-valued `RawTailLines`), conflicting with the actual implementation in `FfmpegStderrParser.cs` (a static parser taking the stderr tail directly, `Diagnose(IReadOnlyList&lt;string&gt;)`, string-valued `RawTail`) that `FfmpegService.FinishAsync` and `EffectiveMaxTailLines` actually call. Deleted the unused `FfmpegStderrDiagnosis.cs` file entirely; `FfmpegStderrParser.cs` remains the single source of truth for FFmpeg stderr diagnosis ### Added Inno Setup 7 installer script finalized with custom icons and shortcuts — first item of the v1.0.0 &quot;Installateur &amp; release&quot; roadmap: - `installer/Videotoy.iss`: `AppVersion` is now read directly from the compiled `Videotoy.exe`&apos;s version resource via the ISPP `GetVersionNumbersString` function, instead of a hardcoded string that could silently drift from the actual build (`OutputBaseFilename` and the wizard&apos;s displayed `AppVerName` follow automatically) - `[Icons]`: added a Start Menu uninstall shortcut (`{group}\Uninstall Videotoy`, `IconFilename={uninstallexe}` so it automatically reuses `installer.ico` — the icon Inno Setup already applies to the generated uninstaller — reading as distinct from the app shortcut) alongside the existing Start Menu and Desktop application shortcuts (both already using the installed `Videotoy.exe`&apos;s embedded `app.ico`); the Desktop shortcut is now gated behind a new, unchecked-by-default `desktopicon` `[Tasks]` entry (standard Inno Setup &quot;Additional icons&quot; wizard page) instead of always being created - Compile-time guards (`#error`) added for a missing `app.ico` or `installer.ico`, alongside the pre-existing checks for the Release build output and the embedded `ffmpeg.exe`, so a broken icon reference fails the installer build immediately instead of silently shipping without one - `[Setup]`: added `AppVerName`, `AppSupportURL`, `AppUpdatesURL`, `UninstallDisplayName`, and `MinVersion=10.0.22000` (Windows 11 only, per this project&apos;s strict Windows-11-only support policy)</description>
  </item>
  <item>
    <title>Videotoy v0.8.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=videotoy</link>
    <guid isPermaLink="false">videotoy-0.8.0</guid>
    <pubDate>Mon, 31 Aug 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to this project are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/). ## [Unreleased] ### Fixed `Directory.Build.props` / `Videotoy.Core.Version` were still stamped `0.3.0`, three completed roadmap phases (v0.5.0–v0.8.0) behind the actually shipped feature set (the last fully-`[x]` phase in `ROADMAP.md`), silently violating this project&apos;s own &quot;automatic SemVer serialization&quot; convention: the installer&apos;s `AppVersion` (read from `Videotoy.exe`&apos;s version resource) and the in-app &quot;À propos&quot; version were both wrong. Bumped `VersionPrefix`/`FileVersion`/`AssemblyVersion`/ `InformationalVersion` in `Directory.Build.props` and `Major`/`Minor` in `Videotoy.Core.Version` to `0.8.0` ### Added SemVer validation and display in the installer — &quot;Signature/vérification de version affichée dans l&apos;installateur (SemVer)&quot; roadmap item: - `installer/Videotoy.iss`: version is now read via the preprocessor&apos;s `GetVersionComponents` (integer Major/Minor/Revision/Build fields) instead of the previous `GetVersionNumbersString` + string-split, so an unreadable version resource is caught explicitly (`&quot;&quot;` return) rather than silently producing a malformed `AppVersion`. Added a build-time guard (`#error`) rejecting an unstamped `0.0.0.x` resource, which would otherwise let a build with a forgotten `Directory.Build.props` bump ship under a fake version. `MyAppVersion` is rebuilt as a strict three-part `X.Y.Z` string (the resource&apos;s fourth, Windows-only `Build` field is dropped, matching this project&apos;s three-part SemVer, not Windows&apos; four-part file version) - `[Code]` `InitializeWizard`: appends the verified `Version : X.Y.Z` line to the Welcome page body (`WelcomeLabel2`), so the SemVer is shown as plain, readable text before any file is installed — not only inside the wizard&apos;s window title / &quot;Ready to Install&quot; summary, which `AppVerName={#MyAppName} {#MyAppVersion}` already drove beforehand and still does `tools/ffmpeg/generate-hash.ps1`: this script was referenced by `.gitignore` (`!/tools/ffmpeg/generate-hash.ps1`), `COMPILATION.md`, and earlier `CHANGELOG.md` entries as already existing, but was missing from the repository — regenerates `tools/ffmpeg/ffmpeg.exe.sha256` (the hash `FfmpegIntegrityVerifier` checks at every startup) from the embedded `ffmpeg.exe`, in the `sha256sum`-compatible format the verifier already parses `tools/ffmpeg/README.md`: local setup notes for embedding `ffmpeg.exe` — where to obtain a Windows build, where to place it, and how `Videotoy.App.csproj` (copy to build output), `installer/Videotoy.iss` (embed in the installer, with a build-time guard if missing), and `FfmpegIntegrityVerifier` (startup SHA-256 check) each depend on it; tracked in Git via a `.gitignore` exception alongside the other `tools/ffmpeg` scaffolding files, closing out the v1.0.0 &quot;Embarquement de `ffmpeg.exe` dans le package d&apos;installation&quot; roadmap item — the packaging path itself (`.csproj` conditional copy + `.iss` `[Files]` recursive bundling + `#error` guard) was already in place since v0.5.0/v0.9.0, the only missing piece was this documented local setup step and the `generate-hash.ps1` script it depends on ### Fixed `Videotoy.App` failed to build (`CS9135`, `CS1061` — F#/C# discriminated union interop): two spots in `MainWindowViewModel.cs` treated F# union cases the way a C# `enum` would, which doesn&apos;t compile — each union case (with or without data) is a distinct nested type in the C#-visible API, not a constant: - `DescribeFirstValidationIssue`&apos;s `switch` on `Videotoy.Core.ExportSettingsValidator.ExportSettingsIssue` used its no-data cases (`InvalidResolution`, `InvalidFrameRate`, ...) as bare constant patterns (`ExportSettingsIssue.InvalidResolution =&gt;`, `CS9135: a constant value is expected`); switched to type patterns with a discard (`ExportSettingsIssue.InvalidResolution _ =&gt;`), the correct C# idiom for matching a data-less F# union case. The data-carrying cases (`OutOfRangeConstantRateFactor of value: int`, `OutOfRangeTargetBitrate of value: int`) also read the captured instance&apos;s field as `.Item` (`CS1061`, no such member); an F# union case with a *named* field exposes it under that field&apos;s own (capitalized) name, not `.Item` — fixed to `.Value`, matching the `value:` field name declared in `ExportSettingsValidator.fs` - `ApplyLoopPeriodDetection` read `detection.SuggestedCandidate is { } candidate` and then accessed `candidate.PeriodSeconds` / `.SourceExpression` / `.PassName` directly (`CS1061`, no such members): `SuggestedCandidate` is a `LoopPeriodCandidate option`, i.e. a C#-side `FSharpOption&lt;LoopPeriodCandidate&gt;` — `is { }` only proved the *option* itself non-null, not that it held a value, so `candidate` was bound to the option wrapper rather than the `LoopPeriodCandidate` inside it. Fixed to the nested property pattern `is { Value: { } candidate }`, which both checks for `Some` and unwraps `.Value` into `candidate` in one step — the same manual `is null` + `.Value` idiom already used correctly elsewhere in this file for `Videotoy.Core.ShaderModel.firstAudioChannelPath`&apos;s `string option` `Videotoy.App` failed to build (`MC1000`/`MC3024`, &quot;the property &apos;Button.Style&apos; has already been set and can only be set once&quot;): `MainWindow.xaml`&apos;s `TogglePanelButton` set `Style` twice — once as a plain attribute (`Style=&quot;{StaticResource IconButtonStyle}&quot;`) and again as a `&lt;Button.Style&gt;` element (added by the v0.8.0 tooltip pass, an inline style with a `DataTrigger` switching the tooltip text and already `BasedOn=&quot;{StaticResource IconButtonStyle}&quot;`) — the attribute form was never removed when the element form was introduced. Removed the now-redundant `Style=` attribute; the button keeps its icon-button appearance unchanged through the remaining `&lt;Button.Style&gt;`&apos;s `BasedOn` `Videotoy.Ffmpeg` failed to build (`CS0101`, `CS0111`, `CS8863`, `CS0708`): two pairs of types were each declared twice across different files, left over from an earlier refactor that was never fully cleaned up: - `FfmpegIntegrityException`: a second, narrower definition (single constructor) had been accidentally left at the top of `FfmpegIntegrityVerifier.cs`, duplicating the real one in `FfmpegIntegrityException.cs` (two constructors, including the `innerException` overload). Removed the duplicate from `FfmpegIntegrityVerifier.cs` - `FfmpegErrorCategory` / `FfmpegStderrDiagnosis` / `FfmpegStderrParser`: `FfmpegStderrDiagnosis.cs` held an entire unused, incompatible second implementation (an instance-based parser with `AppendLine`/`Diagnose()` and a list-valued `RawTailLines`), conflicting with the actual implementation in `FfmpegStderrParser.cs` (a static parser taking the stderr tail directly, `Diagnose(IReadOnlyList&lt;string&gt;)`, string-valued `RawTail`) that `FfmpegService.FinishAsync` and `EffectiveMaxTailLines` actually call. Deleted the unused `FfmpegStderrDiagnosis.cs` file entirely; `FfmpegStderrParser.cs` remains the single source of truth for FFmpeg stderr diagnosis ### Added Inno Setup 7 installer script finalized with custom icons and shortcuts — first item of the v1.0.0 &quot;Installateur &amp; release&quot; roadmap: - `installer/Videotoy.iss`: `AppVersion` is now read directly from the compiled `Videotoy.exe`&apos;s version resource via the ISPP `GetVersionNumbersString` function, instead of a hardcoded string that could silently drift from the actual build (`OutputBaseFilename` and the wizard&apos;s displayed `AppVerName` follow automatically) - `[Icons]`: added a Start Menu uninstall shortcut (`{group}\Uninstall Videotoy`, `IconFilename={uninstallexe}` so it automatically reuses `installer.ico` — the icon Inno Setup already applies to the generated uninstaller — reading as distinct from the app shortcut) alongside the existing Start Menu and Desktop application shortcuts (both already using the installed `Videotoy.exe`&apos;s embedded `app.ico`); the Desktop shortcut is now gated behind a new, unchecked-by-default `desktopicon` `[Tasks]` entry (standard Inno Setup &quot;Additional icons&quot; wizard page) instead of always being created - Compile-time guards (`#error`) added for a missing `app.ico` or `installer.ico`, alongside the pre-existing checks for the Release build output and the embedded `ffmpeg.exe`, so a broken icon reference fails the installer build immediately instead of silently shipping without one - `[Setup]`: added `AppVerName`, `AppSupportURL`, `AppUpdatesURL`, `UninstallDisplayName`, and `MinVersion=10.0.22000` (Windows 11 only, per this project&apos;s strict Windows-11-only support policy) Software icon and dedicated installer icon — fourth and final item of the v0.8.0 &quot;Polish UI/FX&quot; roadmap: - `Assets/Icons/app.ico` (replaced): regenerated as a proper multi-resolution Windows icon (16/20/24/32/40/48/64/128/256px, PNG- compressed frames, transparent background) matching the app&apos;s actual logo mark — the `IconLoop` glyph already used in the title bar — on a rounded-square accent-blue tile, instead of the previous unrelated &quot;play button&quot; glyph on an opaque black square. Small sizes (≤48px) use a thicker stroke weight than the 64-256px frames so the loop shape stays legible at taskbar/Explorer scale. Referenced unchanged by `Videotoy.App.csproj`&apos;s existing `ApplicationIcon` - `installer/installer.ico` (new): same multi-resolution set, reusing the app&apos;s logo mark with a small green &quot;download into tray&quot; badge in the bottom-right corner so the installer&apos;s icon reads as distinct from the running application&apos;s in the taskbar/Explorer while still being immediately recognizable as Videotoy&apos;s installer - `installer/Videotoy.iss`: `[Setup]` section now sets `SetupIconFile=installer.ico` (the Inno Setup 7 wizard/executable icon) and `UninstallDisplayIcon={app}\{#MyAppExeName}` (Add/Remove Programs entry icon); `[Icons]` entries (Start Menu + Desktop shortcuts) now specify `IconFilename` explicitly for clarity, pointing at the installed `Videotoy.exe` Custom scrollbar, stylized tooltips, and contextual cursors — third item of the v0.8.0 &quot;Polish UI/FX&quot; roadmap: - `Theme.xaml`: new `CustomScrollBarStyle` (+ `CustomScrollBarThumbStyle`, `CustomScrollBarPageButtonStyle`) — a slim (10px), borderless, rounded thumb replacing the native Windows scrollbar chrome, tinted with `BorderBrush` at rest and `AccentBrush`/`AccentPressedBrush` on hover/drag. Applied globally via an implicit `Style TargetType=&quot;ScrollBar&quot;` so every `ScrollViewer` in the app (issues panel, render settings panel, combo box popups) picks it up automatically, no per-usage change required - `Theme.xaml`: new `StyledToolTipStyle` — a small elevated dark card (rounded corners, `PopupShadow`, fade + slide-down entrance animation) replacing the flat system tooltip, applied globally via an implicit `Style TargetType=&quot;ToolTip&quot;` - `MainWindow.xaml`: added tooltips to previously unlabeled icon-only buttons (title bar minimize/maximize/close, issues panel close button, render settings panel collapse/expand toggle — the latter switching text via a `DataTrigger` on `IsSettingsPanelOpen`) and to the preview viewport&apos;s drop zone; new `window.minimize`, `window.maximize`, `window.close`, `panel.toggle.collapse`, `panel.toggle.expand` resource keys in `en.json` / `fr.json` (also wired the previously unused `issues.panel.close` key as that button&apos;s tooltip) - `FieldTextBoxStyle` (`Theme.xaml`): `Cursor=&quot;IBeam&quot;` while enabled, reverting to the default arrow when disabled (e.g. output file name during export) so the cursor never implies text entry is possible - Preview viewport drop zone: `Cursor=&quot;Hand&quot;` while no shader is loaded, signaling the area accepts a dropped/clicked file Animated progress ring and continuous pulse shown while a video export is rendering — first item of the v0.8.0 &quot;Polish UI/FX&quot; roadmap: - `Converters/ProgressRingArcConverter.cs` (new): converts `ExportProgressPercent` (0-100) into a `PathGeometry` circular arc, sweeping clockwise from the top and reaching a full circle at 100% - `MainWindow.xaml`: the export-mode viewport overlay now shows a 36px progress ring (dim static track + animated indicator arc bound to `ExportProgressPercent` via `ProgressRingArcConverter`) with the `IconExport` glyph centered inside it, replacing the previously static icon-only overlay - `Theme.xaml`: new `RenderPulseStoryboard` — a slow, continuous breathing opacity/scale animation (`SineEase`, 1.1s, auto-reversing) applied to a halo ring behind the progress ring, so the overlay reads as &quot;actively working&quot; even during long gaps between progress callbacks on a low-spec render; `ExportProgressBarStyle` (side panel progress bar) gained the same breathing pulse on its filled indicator for consistency between the two progress displays Non-intrusive toast notifications for export outcomes — second item of the v0.8.0 &quot;Polish UI/FX&quot; roadmap: - `ViewModels/ToastNotificationViewModel.cs` (new): immutable `ToastSeverity` (Success/Error) + toast record (Id, Severity, Title, Message) - `MainWindowViewModel`: new `Toasts` observable collection, `ShowToast(severity, title, message)` (adds a toast and schedules its removal after 5 seconds via `DispatcherTimer`), `DismissToastCommand` for manual early dismissal. Wired into `ExportVideoAsync`: a success toast on completion, an error toast on pre-export settings-validation failure, `FfmpegEncodingException`, and any other exception. Cancellation remains status-bar-only (no toast), consistent with it being a user-initiated, non-error outcome - `Converters/ToastSeverityBrushConverter.cs`, `Converters/ToastSeverityIconConverter.cs` (new): map `ToastSeverity` to a color (green/red) and a small check/cross glyph respectively - `MainWindow.xaml`: new bottom-right floating `ItemsControl` stack bound to `Toasts`, each rendered via the new `ToastCardStyle` (`Theme.xaml`: slide-in-from-right + fade entrance animation, `PopupShadow`, rounded card) so notifications never block interaction with the rest of the window - `en.json` / `fr.json`: new `toast.export.success.title`, `toast.export.error.title`, `toast.dismiss` resource keys Animated transition between the &quot;Edit&quot; mode (interactive shader preview, all render settings editable) and the &quot;Export&quot; mode (settings locked, deterministic frame-by-frame encoding in progress) — second item of the v0.8.0 &quot;Polish UI/FX&quot; roadmap: - `MainWindow.xaml`: new `ExportModeOverlayStyle`, applied to a scrim (`ExportModeOverlay`) layered over the preview viewport. Driven purely by a `DataTrigger` on the existing `IsExporting` property (no new ViewModel state): fades in a dark overlay with the export icon and live progress percentage, easing outward from a slight zoom so entering/leaving export mode reads as a deliberate transition rather than an instant visibility flip. Reuses the existing `IconExport` glyph (until now defined but unused) and the `FastDuration` / `MediumDuration` timings already used elsewhere in the window - Fixed several strings in the export progress/error card and its &quot;Export to MP4&quot; button that had been left hard-coded in English when the rest of `MainWindow.xaml` was wired to `{loc:Loc}` / `{loc:LocFormat}` in the previous i18n pass — all now resolve through resource keys that already existed in `fr.json` / `en.json` but were unused (`export.progress.title`, `export.progress.percent`, `export.progress.eta`, `export.error.title`, `action.cancelExport`, `action.exportToMp4`, `statusBar.frameCount.label`, `statusBar.frameCount.separator`) - `MainWindowViewModel.FormatRemainingTime`: now takes the newly injected `LocalizationService` and resolves its previously hard-coded `&quot;Estimating...&quot;` fallback through the (likewise previously unused) `export.progress.estimating` resource key, so the export ETA label is fully localized in every state Animated splash screen shown immediately on application startup, while the FFmpeg integrity check and service initialization run in the background — first item of the v0.8.0 &quot;Polish UI/FX&quot; roadmap: - `Videotoy.App.Views.SplashWindow`: chromeless, transparent window reusing the existing card/shadow style (`CardBorderStyle`, `PopupShadow`) and the application&apos;s `IconLoop` badge, with three looping animations (continuous logo rotation, opacity pulse on the badge, a sweeping indeterminate progress indicator) plus a fade-in on load; its title and loading text are localized (`{loc:Loc Key=app.title}` / `{loc:Loc Key=splash.loading}`) and switch immediately if the language changes - `App.OnStartup`: now shows the splash first, then resolves `LocalizationRuntime`/`FfmpegIntegrityVerifier`/`MainWindowViewModel` from the DI container and runs the FFmpeg integrity check behind it; enforces a 900 ms minimum splash duration (`MinimumSplashDuration`) so the animation stays visible even when startup itself is near-instant, then shows `MainWindow` and closes the splash. The integrity-check failure dialog still shows exactly as before, with the splash closed first - New symmetrical resource key `splash.loading` (fr: &quot;Chargement...&quot;, en: &quot;Loading...&quot;) added to `fr.json` / `en.json` Runtime wiring of the i18n resources introduced previously (hot language switching, system language detection on first launch, language selector in the About window), completing the remaining v0.7.0 roadmap items: - `Videotoy.Media.LocalizationService`: loads `fr.json` / `en.json` from `Resources/Localization` next to the executable, exposes `CurrentLanguage` and `GetString(key)` / `GetFormattedString(key, args)`, and raises `LanguageChanged` from `SetLanguage(...)` so bound UI refreshes immediately, with no application restart - `Videotoy.Media.AppLanguage`: the set of supported UI languages (`English`, `French`), with `ToCode()` / `FromCode(...)` conversion to the two-letter codes used as JSON resource file names - `Videotoy.Media.LanguageSettingsEntry` + `LocalizationService`&apos;s private storage: persists the active language in `%AppData%\Videotoy\language-settings.json`, mirroring the existing `RecentFilesService` / `LoopSettingsService` storage pattern; the persisted entry distinguishes an explicit user selection from the auto-detected default, so a system-language change on a machine where the user never opened the language selector still updates on the next launch - System language detection: on first launch (no persisted selection yet), the initial language is derived from `CultureInfo.InstalledUICulture`, falling back to English when the system UI language isn&apos;t one of the languages Videotoy ships translations for - `Videotoy.App.Localization.LocExtension` (`{loc:Loc Key=...}`) and `LocFormatExtension` (`{loc:LocFormat Key=..., Path=...}`): XAML markup extensions resolving a localization key — plain or as a composite format string applied to a bound value (e.g. `CurrentFrame`) — through bindings that re-evaluate automatically on `LanguageChanged`, replacing every hard-coded string previously in `MainWindow.xaml` - `Videotoy.App.Localization.LocalizationRuntime` / `LocalizedStrings`: bridges the DI-registered `LocalizationService` singleton (attached once from `App.OnStartup`, before any window is constructed) to the XAML markup extensions, which are instantiated by the XAML parser outside of dependency injection - Language selector added to the About window: a combo box (`AboutViewModel.AvailableLanguages` / `SelectedLanguage`) listing each supported language by its own native name (&quot;Français&quot; / &quot;English&quot;), switching the active language immediately on selection - Two new symmetrical resource keys, `about.language.label` (fr: &quot;Langue&quot;, en: &quot;Language&quot;), added to `fr.json` / `en.json` `fr.json` / `en.json` localization resource files (`Videotoy.App/Resources/Localization/`) now cover every user-facing string in the interface: menus, panel labels and descriptions, status bar, export progress/errors, diagnostics messages, the About window (including its window title), and the FFmpeg startup integrity-check dialog. Both files are kept symmetrical (138 keys in each, after the language-selector additions above). Persisted &quot;Duration Mode&quot; per shader: the last-used duration mode (manual vs. seamless loop) and the last loop duration are now remembered per-shader-file and restored automatically the next time that exact shader is opened, rather than always falling back to the application defaults: - `Videotoy.Media.LoopSettingsEntry`: serializable per-shader snapshot (shader file path, `IsSeamlessLoopModeEnabled`, `LoopDurationSeconds`, last-updated timestamp) - `Videotoy.Media.LoopSettingsService`: JSON persistence in `%AppData%\Videotoy\loop-settings.json`, keyed on the shader&apos;s absolute file path (case-insensitive match), mirroring the existing `RecentFilesService` / `ExportPresetService` storage pattern; entries beyond 200 are pruned, oldest first - `MainWindowViewModel.RestoreLoopSettings`: looked up on every `LoadShaderFile` call and applied to `IsSeamlessLoopModeEnabled` / `LoopDurationSeconds` when a saved entry exists for that file; a no-op the first time a given shader is opened - `MainWindowViewModel.PersistLoopSettings`: saved on every change to either `LoopDurationSeconds` or `IsSeamlessLoopModeEnabled` for the currently loaded shader, so the last state is captured even if the user never actually exports Assisted detection of a shader&apos;s native loop period (optional, heuristic): the render settings panel can now suggest a loop duration derived directly from the shader&apos;s own source code, for shaders that animate through simple periodic expressions on `iTime`: - `Videotoy.Core.LoopPeriodDetector`: recognizes `sin(iTime * K)`, `cos(K * iTime)`, `mod(iTime, K)` and `fmod(iTime, K)` (free spacing, free operand order for `sin`/`cos`) in the shader&apos;s `Common` code and every pass; converts each match to an implied period (`2*pi / K` for `sin`/`cos`, `K` directly for `mod`/`fmod`); deliberately restricted to these simple, unambiguous forms — anything more complex (`iTime` combined with other variables, nested functions, custom uniforms, etc.) is silently left undetected rather than risking a false suggestion. When several independent periodic patterns are found, the longest detected period is proposed as the default suggestion, since it most often matches the overall visual loop perceived by the user - `MainWindowViewModel.ApplyLoopPeriodDetection`: run once per shader load (`LoadShaderFile`), populating `HasDetectedLoopPeriod` / `DetectedLoopPeriodSeconds` / `HasMultipleDetectedLoopPeriods` / `DetectedLoopPeriodSourceText` — purely informational, never itself touching `LoopDurationSeconds` or the selected duration mode - `ApplyDetectedLoopPeriodCommand`: the only way the detected period ever reaches `LoopDurationSeconds`, mirroring the existing `ApplyAssistedLoopRoundingCommand` pattern — an explicit, user-triggered action, never applied automatically - Render settings panel: new &quot;Detected native loop period&quot; hint under the seamless-loop fields, showing the source expression/pass it was found in, a note when multiple candidates were found, and an `Apply` button Three aspect-ratio resolution presets in the &quot;Resolution&quot; combo box, alongside the existing Preview/SD/HD/Full HD/4K UHD/Custom entries: `Screen4By3` (1440 x 1080), `Screen16By9` (1920 x 1080), and `Smartphone9By16` (1080 x 1920, portrait) — plain additions to `ResolutionPresetOption.All`, so no other code change was needed: the combo box is already bound to `ResolutionPresets` (`=&gt; All`), and preset persistence already round-trips any `Key` through `FromKey` Loop seam preview: a &quot;Loop Seam Preview&quot; card in the render settings panel, &quot;Seamless loop&quot; duration mode only, renders the loop&apos;s first frame (t = 0) and its actual last exported frame side by side for visual validation before committing to a full export: - `MainWindowViewModel.GenerateLoopSeamPreviewCommand`: walks the shared live-preview `MultiPassRenderer` through the *entire* loop timeline (`Videotoy.Core.LoopCalculator.computeFrameCount` / `buildFrameTimeline`, same construction as the real export), capturing frame 0 and the final frame — deliberately not just those two frames in isolation, since a shader with a self-referencing buffer (Buffer A/B/C/D ping-pong feedback) accumulates state frame by frame and a direct jump to the last timestamp would show a buffer state the actual export never produces. Runs synchronously on the UI thread (the shared D3D11 device isn&apos;t thread-safe), re-rendering the current live-playback frame afterward so the viewport isn&apos;t left showing the loop&apos;s last frame once the comparison is generated - `LoopSeamStartFrameImageSource` / `LoopSeamEndFrameImageSource`: dedicated `WriteableBitmap`s (preview resolution, BGRA32) holding the two captured frames; `HasLoopSeamPreview` drives the comparison&apos;s visibility and is reset to false whenever a setting that would invalidate it changes (shader reload, resolution/frame rate/duration mode/loop duration/exclusive-end-frame); `IsGeneratingLoopSeamPreview` disables the button and swaps its label to &quot;Generating...&quot; while the walk is in progress - `LoopSeamPreviewButtonTextConverter`: swaps the generate button&apos;s label based on `IsGeneratingLoopSeamPreview` &quot;Exclusive end frame&quot; toggle, seamless loop mode: the previously implicit &quot;no duplicated frame at the loop seam&quot; behavior is now an explicit, user-facing option (`IsLoopEndFrameExclusive`, on by default) rather than an unconditional rule: - `Videotoy.Core.Domain.DurationMode.SeamlessLoop` now carries an `excludeEndFrame: bool` alongside `loopSeconds` - `LoopCalculator.computeFrameCount`: in `SeamlessLoop` mode, `FrameCount` is `round(loopSeconds * fps)` when `excludeEndFrame` is true (default — the frame at `t = loopSeconds`, identical to `t = 0`, is never rendered), or that same count `+ 1` when false, deliberately including that duplicated end frame - `MainWindowViewModel.IsLoopEndFrameExclusive`: new panel toggle, defaults to `true`; feeds `ResolveDurationMode`&apos;s `DurationMode.NewSeamlessLoop(loopSeconds, excludeEndFrame)` call and triggers `RecalculateExportPreview` (frame count / file size estimate) like every other duration-affecting input - `ExportPreset.IsLoopEndFrameExclusive`: persisted with the rest of the duration-mode settings; not `required` (defaults to `true`) so presets saved by an earlier version still deserialize correctly - Render settings panel: new &quot;Exclusive end frame&quot; checkbox under the &quot;Seamless loop&quot; duration fields, with an explanatory note Seamless loop assisted rounding: when the requested loop duration doesn&apos;t divide evenly into whole frames at the selected export frame rate (`HasLoopRoundingMismatch`), the render settings panel now also shows the nearest loop duration that does, alongside an `Apply` button to adopt it in one click: - `Videotoy.Core.LoopCalculator.suggestAssistedLoopSeconds`: pure `loopSeconds -&gt; frameRate -&gt; float` helper returning `round(loopSeconds * frameRate) / frameRate` (never less than one frame), i.e. the closest loop duration whose exact frame count is a whole number - `MainWindowViewModel.SuggestedLoopDurationSeconds`: recomputed inside `RecalculateExportPreview` whenever `HasLoopRoundingMismatch` is true (equal to `LoopDurationSeconds` otherwise); `ApplyAssistedLoopRoundingCommand` copies it onto `LoopDurationSeconds`, which eliminates the mismatch entirely rather than just reducing it. Never applied automatically — purely an explicit, user-triggered suggestion - Render settings panel: the existing rounding-mismatch warning now also shows &quot;Suggested: X s (exact frame count)&quot; with an `Apply` button, disabled while an export is running Roadmap check-off: &quot;Manual duration&quot; mode (fixed duration entered directly in seconds or frames) confirmed complete — already fully implemented as part of the v0.6.0 render settings panel work (`ManualDurationValue` / `ManualDurationUnit`, `ResolveDurationMode`, `ExportSettingsValidator`&apos;s `InvalidDuration` check, preset persistence, and the &quot;Manual duration&quot; radio button + seconds/frames combo in the panel); no code change required for this entry, only the `ROADMAP.md` check-off Live custom uniforms preview with dynamically generated sliders: the render settings panel now exposes a &quot;Custom Uniforms&quot; card whenever the loaded shader declares at least one custom uniform, letting the user adjust values in real time against the live preview only (never the exported video, which always renders from the shader&apos;s declared defaults): - `Videotoy.Core.CustomUniformParser`: recognizes a dedicated GLSL comment convention — `// uniform: float MySpeed = 1.0 [0.0, 5.0] &quot;Speed&quot;` or `// uniform: vec3 TintColor = (1.0, 0.5, 0.2) [0.0, 1.0]` — in the `Common` code or any pass, supporting `float`/`vec2`/`vec3`/ `vec4`, an optional `[min, max]` range applied per component (defaults to `[-10.0, 10.0]` when omitted), and an optional quoted display label (falls back to the uniform&apos;s name); malformed declarations are simply ignored rather than raised as shader errors, since the convention lives entirely inside an ordinary GLSL comment and never affects Shadertoy compatibility of the source file itself - `Videotoy.Core.GlslToHlslTranspiler`: emits a second HLSL constant buffer (`cbuffer CustomUniforms : register(b1)`) declaring every custom uniform detected in a given pass (Common + that pass&apos;s own source), alongside the existing `ShadertoyUniforms` (`register(b0)`); skipped entirely when the shader exposes none. `TranspileResult.CustomUniforms` exposes the parsed declarations for that pass; the new `projectCustomUniformsOf` / `projectCustomUniforms` helpers return the name-deduplicated union across every already-transpiled pass of a project, in stable first-seen order - `MultiPassRenderer`: allocates a dynamic `register(b1)` constant buffer sized from the shader&apos;s custom uniform count (a fixed 16-byte slot per uniform, regardless of its component count, to sidestep any per-type HLSL packing arithmetic), re-created on every `Initialize` call so a previous shader&apos;s uniforms never leak into the next one. `CustomUniformDeclarations` exposes the current shader&apos;s declarations; `SetCustomUniformComponent(name, componentIndex, value)` updates a single component&apos;s live value with no shader recompilation, uploaded to the GPU on every `RenderFrame` call (`UpdateCustomUniforms`) so the very next preview frame reflects the change immediately - `CustomUniformSliderViewModel` / `CustomUniformGroupViewModel` (`Videotoy.App.ViewModels`): one slider view model per scalar component (x/y/z/w), grouped by uniform; `MainWindowViewModel` exposes them as `CustomUniformGroups` (rebuilt in full — via `ReloadCustomUniformGroups` — every time a shader is loaded from `MultiPassRenderer.CustomUniformDeclarations`) and `HasCustomUniforms`, which drives the card&apos;s visibility. Each slider&apos;s `Value` change is wired straight back to `MultiPassRenderer.SetCustomUniformComponent` - `Theme.xaml`: new `FieldSliderStyle` (flat rounded track, accent round thumb) matching the existing `FieldTextBoxStyle`/`FieldComboBoxStyle` visual language, used by every dynamically generated uniform slider - Render settings panel: &quot;Custom Uniforms&quot; card between &quot;Frame Rate&quot; and &quot;Duration Mode&quot;, listing one labelled group per custom uniform with one slider (+ live numeric readout) per component; hidden entirely when `HasCustomUniforms` is false Export presets: &quot;Export Presets&quot; card in the render settings panel to save and reload named snapshots of the export configuration (`Videotoy.Media.ExportPreset` / `ExportPresetService`, persisted to `%AppData%\Videotoy\export-presets.json`, mirroring the storage pattern already used by `RecentFilesService`): - `MainWindowViewModel.SaveExportPresetCommand`: saves resolution preset (+ custom width/height), frame rate preset (+ custom value), duration mode (manual/seamless loop) with its value/unit, and low-spec mode under the name typed into `NewExportPresetName`; replaces any existing preset with the same name (case-insensitive) rather than accumulating duplicates (`ExportPresetService.SaveOrReplace`). Deliberately excludes the output folder and file name — those stay per-export, not part of a reusable preset - `LoadExportPresetCommand`: applies `SelectedExportPreset` back onto every panel input it covers, through the normal properties (not the backing fields) so `RecalculateExportPreview` stays in sync automatically; `DeleteExportPresetCommand` removes a preset. Both disabled until a preset is selected (`SelectedExportPreset is not null`) - `ResolutionPresetOption.Key` / `FrameRatePresetOption.Key`: stable machine identifiers (`&quot;FullHd1080&quot;`, `&quot;Fps30&quot;`, ...), separate from the user-facing `DisplayName`, used to serialize/deserialize which preset a saved `ExportPreset` refers to (`FromKey`, falling back to `Custom` for an unrecognized key so a preset saved by a future version with more presets doesn&apos;t crash on load) `en.json` / `fr.json`: added `panel.exportPresets.*` and `status.exportPreset*` keys (not yet wired to a localization service, consistent with the other pre-existing unused keys pending v0.7.0) Render settings panel: full export configuration UI (v0.6.0) replacing the previously hardcoded preview-resolution/30 fps/`SaveFileDialog` export path: - **Resolution**: combo box of presets (`Preview 800x450`, `SD 854x480`, `HD 1280x720`, `Full HD 1920x1080`, `4K UHD 3840x2160`, `Custom...`); selecting `Custom...` reveals width/height text inputs (`MainWindowViewModel.CustomResolutionWidth` / `CustomResolutionHeight`) - **Frame rate**: combo box of presets (24/25/30/60/`Custom...`); selecting `Custom...` reveals a numeric FPS input (`CustomFrameRateValue`) - **Duration**: &quot;Manual duration&quot; now lets the user enter the export length directly in seconds or frames (`ManualDurationValue` / `ManualDurationUnit`, converted to seconds against the effective frame rate before being handed to `DurationMode.Manual`); &quot;Seamless loop&quot; keeps using `LoopDurationSeconds`, now with a rounding-mismatch warning shown when `LoopCalculator.computeFrameCount` reports `HasRoundingMismatch` (`HasLoopRoundingMismatch`) - **Frame count &amp; file size preview**: a &quot;Frame Count &amp; File Size&quot; card shows the exact `EstimatedTotalFrames` (from `Videotoy.Core.LoopCalculator.computeFrameCount`) and a rough `EstimatedFileSizeText` (&quot;~12.4 MB&quot; / &quot;~1.8 GB&quot;), both recalculated live from every input that affects them (resolution, frame rate, duration mode/value/unit, and whether an audio track will be muxed) - **Output**: a &quot;Folder&quot; text field plus a `Browse...` button (`BrowseOutputDirectoryCommand`, backed by the .NET 8 WPF `Microsoft.Win32.OpenFolderDialog`) replacing the old export-time `SaveFileDialog`, and a &quot;File name&quot; text field (`MainWindowViewModel.OutputDirectory` / `OutputFileName`); the file name now defaults to the loaded shader&apos;s title, sanitized against `Path.GetInvalidFileNameChars()` (`SanitizeAsFileName`), on every successful shader load `Videotoy.Core.ExportFileSizeEstimator` (`Videotoy.Core`): pure `Resolution -&gt; FrameRate -&gt; RateControlMode -&gt; frameCount -&gt; includeAudio -&gt; float` byte-count estimator (`estimateFileSizeBytes`) plus a `&quot;~X MB&quot;`/`&quot;~X GB&quot;` formatter (`formatEstimatedFileSize`); intentionally approximate (a coarse CRF-to-bitrate table, documented as such) since no real encode has run yet at configuration time `MainWindowViewModel.ExportVideoCommand` now builds its `ExportSettings` entirely from the new panel state (resolution/frame rate/duration mode/output folder &amp; file name) instead of hardcoded preview values, and validates it up front via `Videotoy.Core.ExportSettingsValidator.validate` before starting the export — an invalid configuration (e.g. missing output folder or file name) now surfaces immediately as an export error instead of failing deep inside the FFmpeg pipeline `Theme.xaml`: new `FieldTextBoxStyle`, `FieldComboBoxStyle` and `ComboBoxItemFieldStyle` control styles (rounded, bordered, accent-colored focus/selection states matching the existing card/button styling), used by every new input in the render settings panel `en.json` / `fr.json`: added `panel.resolution.*`, `panel.frameRate.custom`, `panel.durationMode.unit.*`, `panel.durationMode.loop*`, `panel.framePreview.*`, and `panel.output.*` keys (not yet wired to a localization service, consistent with the other pre-existing unused keys pending v0.7.0) &quot;Include audio track&quot; export option: when the loaded shader declares an `iChannel` audio source, `MainWindowViewModel.HasAudioChannel` (recomputed on every successful shader load, via the same `ResolveExportAudioSourceFilePath` resolution already used for muxing) drives a new, otherwise-hidden &quot;Audio&quot; card in the render settings panel with an &quot;Include audio track&quot; checkbox bound to `IncludeAudioInExport` (defaults to `true` on every new shader load). `ExportVideoCommand` now only passes the resolved audio source path down to `VideoExportPipeline.RunAsync` when both `HasAudioChannel` and `IncludeAudioInExport` are true; unchecking it produces a silent, video-only export without touching any other export parameter Render settings panel: &quot;Audio&quot; card (checkbox + explanatory caption), shown only when `HasAudioChannel` is true, disabled while an export is already running — same visibility/disable pattern already used by the export progress and error cards `en.json` / `fr.json`: added `panel.audio`, `panel.audio.includeAudioTrack`, and `panel.audio.includeAudioTrack.description` keys (not yet wired to a localization service, consistent with the other pre-existing unused keys pending v0.7.0) Strict audio/render-timeline alignment: `VideoExportPipeline.RunAsync` no longer takes a pre-built `FfmpegAudioTrackOptions`, but a plain `audioSourceFilePath` string; it now computes the audio track&apos;s muxed duration itself, from the frame count actually rendered rather than the raw requested duration `Videotoy.Core.LoopCalculator.effectiveDurationSeconds`: pure `FrameCountResult -&gt; FrameRate -&gt; float` helper returning `frameCount / frameRate`, i.e. the duration truly covered by the frames that will be rendered and written, as opposed to the requested `DurationMode` seconds value. The two can differ very slightly in `SeamlessLoop` mode whenever `loopSeconds * fps` isn&apos;t a whole number (`FrameCountResult.HasRoundingMismatch`), since the frame count is then rounded; muxing audio against the requested duration instead of this effective one could leave a few extra silent milliseconds — or cut the last audio samples short — right at the loop seam `MainWindowViewModel.ResolveExportAudioSourceFilePath` (renamed from `ResolveExportAudioTrack`): now resolves and returns only the audio source file path, deliberately not a duration — `VideoExportPipeline` is the only place with access to the actual rendered frame count, so it alone builds the final `FfmpegAudioTrackOptions` with the correct effective duration `Videotoy.Core.ExportSettingsValidator.resolveDurationSeconds`&apos;s doc comment now explicitly warns against using the requested duration to align a muxed audio track, pointing to `effectiveDurationSeconds` instead Audio track muxing on export: when the loaded shader declares an `iChannel` of type `Music`/`MusicStream`, `MainWindowViewModel.ResolveExportAudioTrack` resolves the declared channel path (`Videotoy.Core.ShaderModel.firstAudioChannelPath`, new helper picking the first audio channel across every pass) against the shader&apos;s own directory — the same resolution logic already used by `ShaderFileService` at load time — and passes it down to `VideoExportPipeline.RunAsync` as an optional `FfmpegAudioTrackOptions` (source file path + export duration in seconds). No audio track is produced when the shader has no audio `iChannel`, or when the resolved source file no longer exists on disk `FfmpegEncodingOptions.AudioTrack` (`FfmpegAudioTrackOptions`, `Videotoy.Ffmpeg`): carries the resolved audio source path and the target export duration through to `FfmpegService` `FfmpegService.Start`&apos;s argument list now conditionally adds the audio source file as a second FFmpeg input (`-i &lt;path&gt;`) alongside the raw video stream already piped through stdin (`-f rawvideo ... -i pipe:0` as input 0); both streams are muxed and encoded by the same FFmpeg process via `-map 0:v -map 1:a`, `-c:a aac -b:a 192k`, in a single encoding pass — no intermediate file, no second FFmpeg invocation. The audio track is encoded to AAC (standard for the MP4 container) regardless of its original format (WAV/MP3/OGG), which FFmpeg decodes itself Audio/video alignment: `-t &lt;exportDurationSeconds&gt;` combined with `-shortest` on the FFmpeg command line caps the muxed output at the export&apos;s exact duration and the same `t = 0` origin as the deterministic render timeline, whether the source audio file is longer (truncated) or shorter (output ends with the shorter stream, no silence padding added) than the exported video `Videotoy.Core.ExportSettingsValidator.resolveDurationSeconds`: pure `ExportSettings -&gt; float` helper unwrapping `DurationMode` (`Manual` or `SeamlessLoop`) into a plain duration in seconds, following the same C#-friendly boundary style already used by the rest of this module, instead of letting `Videotoy.App` pattern-match directly on the `DurationMode` discriminated union `Videotoy.Core.Domain.PerformanceMode` (`Normal` / `LowSpec of throttleMillisecondsPerFrame: int`), added as a new field on `ExportSettings`; `ExportSettingsValidator.resolveThrottleMilliseconds` resolves it to a plain `int` (0 for `Normal`) for the C# side, following the existing boundary style, and `validate` rejects a negative `LowSpec` throttle as `InvalidThrottleDuration` `VideoExportPipeline.RunAsync` now awaits `Task.Delay(throttleMs, cancellationToken)` after writing each frame when `PerformanceMode` is `LowSpec`, deliberately slowing the render/encode cadence so a modest machine&apos;s CPU/GPU is never pegged for the whole export; the render loop was already strictly sequential (render frame → write to FFmpeg&apos;s stdin pipe → next frame, all awaited in order) so this mode changes only the pacing, never drops or reorders a frame, and stays independent of any real-time playback loop — consistent with `FrameSequenceRenderer`&apos;s deterministic, frame-index-driven timeline. The delay observes the same cancellation token as the rest of the export, so `CancelExport` remains immediately responsive even mid-throttle `MainWindowViewModel.IsLowSpecModeEnabled`: toggles `PerformanceMode` between `Normal` and `LowSpec` (fixed 50 ms/frame throttle pending a configurable value in the future export settings panel) on every new `ExportVideoCommand` run Render settings panel: &quot;Low-spec mode&quot; checkbox with an explanatory caption, disabled while an export is already running `en.json` / `fr.json`: added `panel.performance`, `panel.performance.lowSpecMode`, and `panel.performance.lowSpecMode.description` keys (not yet wired to a localization service, consistent with the other pre-existing unused keys pending v0.7.0) `Videotoy.Ffmpeg` project files reconstructed (`.csproj` plus every class it exposes — `FfmpegLocator`, `FfmpegIntegrityException`/`Verifier`, `FfmpegEncodingOptions`, `FfmpegEncodingException`, `FfmpegService`, `VideoExportProgress`, `VideoExportPipeline`) after they were found missing from the source tree despite being referenced by `Videotoy.sln`, `App.xaml.cs`, and `MainWindowViewModel.cs`; rebuilt from the FFmpeg process/pipe/argument-list/DI behaviour already documented for these types earlier in this file `FfmpegStderrDiagnosis` / `FfmpegStderrParser` (`Videotoy.Ffmpeg`): `FfmpegService` now pumps `ffmpeg.exe`&apos;s stderr on a dedicated background task and accumulates it into a bounded tail buffer; on a non-zero exit code, the tail is matched against a table of known FFmpeg failure patterns (unwritable output path, disk full, unsupported codec, invalid resolution, invalid input stream) to produce a `FfmpegErrorCategory` plus a short, UI-ready `Summary` sentence, falling back to a generic message with the raw tail lines when no known pattern matches; surfaced end to end as `FfmpegEncodingException.Diagnosis` `FfmpegService.Cancel()`: closes the stdin pipe and kills the FFmpeg process tree (`Process.Kill(entireProcessTree: true)`), tolerating a process that has already exited or an already-broken pipe, and always disposes and clears the process handle afterwards so a cancelled export never leaves an orphaned `ffmpeg.exe` running or blocks a subsequent export; idempotent and safe to call when no export is in progress. `VideoExportPipeline.RunAsync` now calls it from every non-success exit path (`OperationCanceledException`, `FfmpegEncodingException`, and any other exception raised during rendering or writing), not just the success path already covered by `FfmpegService.FinishAsync` `MainWindowViewModel.CancelExportCommand`: cancels `_exportCancellationTokenSource`, enabled only while `IsExporting` is true; `ExportVideoCommand`&apos;s `catch` blocks now distinguish `OperationCanceledException` (status message only) from `FfmpegEncodingException` (populates `HasExportError` / `ExportErrorSummary` from `Diagnosis.Summary`) from any other failure (falls back to `ex.Message`), resetting both on every new export attempt Render settings panel: &quot;Cancel export&quot; button in the export progress card, and a red-bordered error card (`HasExportError` / `ExportErrorSummary`) shown when an export fails `en.json` / `fr.json`: added `action.cancelExport`, `status.cancellingExport`, and `export.error.title` keys (not yet wired to a localization service, consistent with the other pre-existing unused keys pending v0.7.0) `ExportSettings` (`Videotoy.Core.Domain`) extended into the full set of export parameters: `VideoCodec` (`H264`/`H265`), `RateControlMode` (`ConstantRateFactor` or `TargetBitrate`, mutually exclusive), and `ContainerFormat` (`Mp4`) replace the previous free-form `CodecName` string and fixed CRF-only field `Videotoy.Core.ExportSettingsValidator`: validates resolution, frame rate, duration, output path and CRF/bitrate range (`validate`/`isValid` returning `ExportSettingsIssue list`), resolves the output file path from directory/file name/container extension, and maps `VideoCodec` / `RateControlMode` to their FFmpeg-facing primitives (codec library name, nullable CRF, nullable target bitrate) so the C# side never pattern matches directly on the F# discriminated unions, consistent with the existing `UniformBuilder` boundary style `FfmpegEncodingOptions.FromExportSettings`: builds encoder options straight from a validated `ExportSettings`, raising `ArgumentException` before any FFmpeg process is started if the settings are invalid `FfmpegService.Start` now builds the FFmpeg argument list via `ProcessStartInfo.ArgumentList` instead of a single concatenated `Arguments` string, so output paths containing spaces are passed safely without manual quoting; adds `-b:v` (target bitrate) as an alternative to `-crf`, and `-movflags +faststart` for the MP4 container `VideoExportPipeline.RunAsync` now takes a single `ExportSettings` instead of a separately constructed `FfmpegEncodingOptions` plus duration/frame rate, making `ExportSettings` the single source of truth passed down from the future export settings panel `VideoExportPipeline` (`Videotoy.Ffmpeg`): drives the full frame-by-frame export end to end — `FrameSequenceRenderer` renders each deterministic frame on the GPU, reads the back-buffer via `OffscreenRenderContext` (already in the FFmpeg-compatible BGRA byte layout, no extra pixel format conversion needed), and streams the pixels straight into `FfmpegService.WriteFrameAsync`&apos;s stdin pipe, with no intermediate frame files ever written to disk; reports `VideoExportProgress` (current/total frame index, time) via `IProgress&lt;T&gt;`, propagates FFmpeg&apos;s exit code as `FfmpegEncodingException` on failure, and calls `FfmpegService.Cancel` to kill the process tree on cancellation `Videotoy.Ffmpeg` now references `Videotoy.Rendering` to wire the render and encode stages together Known limitation: `FrameSequenceRenderer` currently drives a single `IShaderRenderer` pass and does not yet go through `MultiPassRenderer`, so exporting a shader with Buffer A/B/C/D feedback passes is not wired up end to end yet Embedded `ffmpeg.exe` now packaged from `Videotoy.App` (moved from the `Videotoy.Ffmpeg` library project, which is never itself the output directory) so the binary is present both in local builds and in the Inno Setup installer output, alongside a companion `ffmpeg.exe.sha256` hash file `tools/ffmpeg/generate-hash.ps1`: PowerShell helper producing `ffmpeg.exe.sha256` (`sha256sum`-compatible format) from the embedded `ffmpeg.exe`, to be re-run whenever the binary is updated `Videotoy.Core.ExportProgressEstimator`: pure `estimateRemainingSeconds` (linear extrapolation of the average seconds-per-frame observed so far onto the remaining frames, returned as a C#-friendly `Nullable&lt;float&gt;` following the `ExportSettingsValidator` boundary style) and `progressFraction` helpers `VideoExportProgress` (`Videotoy.Ffmpeg`) extended with `ElapsedSeconds`, `EstimatedRemainingSeconds`, and the computed `CurrentFrameNumber` / `ProgressFraction` members; `VideoExportPipeline.RunAsync` now times the export with a `Stopwatch` and reports the ETA on every frame via `IProgress&lt;VideoExportProgress&gt;` `MainWindowViewModel.ExportVideoCommand` now runs a real export end to end (`SaveFileDialog` for the output path, `IShaderRenderer` init/load of the `Image` pass, `VideoExportPipeline.RunAsync`) and exposes `IsExporting`, `ExportCurrentFrame`, `ExportTotalFrames`, `ExportProgressPercent`, and `ExportRemainingTimeText` for data binding; known limitation: until the export settings panel (v0.6.0) lands, the export reuses the 800x450 preview resolution, a fixed 30 fps, and &quot;Manual duration&quot; driven by `LoopDurationSeconds` Render settings panel: export progress card (percentage, flat `ExportProgressBarStyle` progress bar, current/total frame counter, ETA) shown while `IsExporting` is true `en.json` / `fr.json`: added `status.exportComplete`, `status.exportFailed`, `status.exportCancelled`, and `export.progress.*` keys (not yet wired to a localization service, consistent with the other pre-existing unused keys pending v0.7.0) ### Fixed `Videotoy.Core.ShadertoyJsonParser`: `open Videotoy.Core.ShaderModel` brings `IssueSeverity.Error` (a nullary union case) into scope, which shadowed `FSharp.Core`&apos;s `Result.Error` and made every `Error [ ... ]` call fail to compile (`FS0003: This value is not a function and cannot be applied`); all four call sites now use the fully qualified `Result.Error` to disambiguate `Videotoy.Core.Domain`: converted from a top-level `module` to a plain `namespace` — since the file only ever declared pure data types (`Resolution`, `ExportSettings`, `DurationMode`, ...) with no functions, compiling it as a module wrapped every type as a *nested type* of a generated `Domain` class, which made every `using Videotoy.Core.Domain;` in C# fail with `CS0138` (&apos;Domain&apos; is a type, not a namespace) and cascade into `CS0246` for `DurationMode`, `FrameRate`, `ExportSettings`, etc. across `FrameSequenceRenderer`, `MainWindowViewModel`, `VideoExportPipeline`, and `FfmpegService` `Videotoy.Core.ShaderModel` mixes types and functions and must stay a `module` (F# namespaces cannot hold `let` bindings), so instead `MultiPassRenderer.cs` and `ShaderIssueViewModel.cs` were switched from `using Videotoy.Core.ShaderModel;` + unqualified `ShaderProject` / `ShaderIssue` to the fully-qualified `Videotoy.Core.ShaderModel.X` form already used successfully elsewhere (`Videotoy.Media.ShaderFileService`) `Videotoy.Media.LoadedShader.HasErrors` called `issue.Severity.IsErrorIssue`, but `IsErrorIssue` is a member of `ShaderIssue` itself (not of `IssueSeverity`); fixed to `issue.IsErrorIssue` Removed `Videotoy.Ffmpeg.FfmpegEncodingService` / its `FfmpegEncodingOptions`: a leftover early prototype of the FFmpeg process launcher, superseded by `FfmpegService` (registered in DI, used by `VideoExportPipeline`) but never deleted, which duplicated the `FfmpegEncodingOptions` type (`CS0101`) already declared in `FfmpegService.cs` `Videotoy.Rendering` (`OffscreenRenderContext`, `MultiPassRenderer`, `D3D11ShaderRenderer`) mismatched the installed Vortice.Direct3D11 bindings in several places, confirmed against the upstream `Mappings.xml`: - `Texture2DDescription.Width`/`.Height` are `uint` in this Vortice version; `OffscreenRenderContext.Resize` now casts from `RenderTargetSize`&apos;s `int` fields instead of assigning directly - `SamplerDescription`&apos;s comparison field keeps its native name `ComparisonFunc` (its *type* is the renamed `ComparisonFunction` enum, but the field itself was never renamed) and its upper mip bound is `MaxLOD`, not `ComparisonFunction`/`MaxLod` as originally written; fixed in both `MultiPassRenderer` and `D3D11ShaderRenderer` - `ID3D11DeviceContext.Map`&apos;s last parameter is `Vortice.Direct3D11.MapFlags`, ambiguous with `Vortice.DXGI.MapFlags` once both namespaces are imported; `OffscreenRenderContext.ReadPixelsRgba` now fully qualifies it - `PSSetShaderResource`/`PSSetSampler` take a `uint` slot index; `MultiPassRenderer.RenderSlot` now casts the buffer channel index (`int`) accordingly - `OffscreenRenderContext.BindRenderTarget` also silences a possible-null-reference warning on `_renderTargetView` with `!`, since `EnsureInitialized()` already guarantees it is set - `MultiPassRenderer.RenderSlot`&apos;s SRV-unbind call (`PSSetShaderResource(slot, null)`) silences `CS8625` with `null!`, since the generated binding doesn&apos;t mark the parameter nullable even though passing null to unbind is the intended, supported usage `Videotoy.App.App.ConfigureServices` registered `MainWindow` (bare, unqualified) for DI, but `MainWindow` lives in `Videotoy.App.Views` (see `OnStartup`, which correctly resolves `Views.MainWindow`); the unqualified reference only compiled by accident as part of the temporary WPF `x:Class` codegen project and failed the real build with `CS0246`. Fixed to `Views.MainWindow` for consistency with `OnStartup` `FfmpegIntegrityVerifier` (`Videotoy.Ffmpeg`): recomputes the embedded `ffmpeg.exe`&apos;s SHA-256 hash via `FfmpegLocator.ComputeSha256` and compares it against `tools/ffmpeg/ffmpeg.exe.sha256`, throwing `FfmpegIntegrityException` on mismatch or on a missing hash file `App.OnStartup` now runs `FfmpegIntegrityVerifier` before the main window is shown; a failed check displays an error dialog and shuts the application down instead of proceeding with a potentially corrupted or tampered binary Inno Setup script (`installer/Videotoy.iss`) fails to compile with an explicit `#error` if `ffmpeg.exe` is missing from the `Release` build output, instead of silently producing an installer without it `FfmpegEncodingService` renamed to `FfmpegService` (`Videotoy.Ffmpeg`): launches the embedded `ffmpeg.exe` process and streams raw RGB/BGRA frames directly to its stdin pipe (`WriteFrameAsync`) with no intermediate frame files ever written to disk; `Start` guards against a second concurrent run, `WriteFrameAsync` fails fast if the process has already exited, and `FinishAsync` returns FFmpeg&apos;s exit code after closing the pipe and awaiting process completion GLSL Shadertoy → HLSL transpiler (`Videotoy.Core.GlslToHlslTranspiler`): source-level translation of vector/matrix types (`vec2`/`vec3`/`vec4`, `mat2`/`mat3`/`mat4`, `ivec*`, `uvec*`, `bvec*`), intrinsic renaming (`mix`→`lerp`, `fract`→`frac`, `mod`→`fmod`, `atan`→`atan2`, `inversesqrt`→`rsqrt`, `discard`→`clip`), `mainImage` → `PSMain` (`SV_Target`/`SV_Position`) entry-point rewriting, and `iChannelN` texture/sampler declarations with `texture(iChannelN, uv)` → `iChannelN.Sample(iChannelNSampler, uv)` rewriting wrapped in the Shadertoy `cbuffer` (`iResolution`, `iTime`, `iTimeDelta`, `iFrame`, `iMouse`, `iDate`, `iSampleRate`, `iChannelResolution`) Transpilation diagnostics reported as `ShaderIssue` (missing `mainImage`, unsupported constructs), merged into the existing shader issues panel `ShaderFileService` now transpiles every loaded pass to HLSL and exposes the per-pass `TranspileResult` (`HlslPasses`) alongside the parsed project Offscreen D3D11 rendering pipeline (`Videotoy.Rendering`): - `OffscreenRenderContext`: hardware D3D11 device creation with automatic WARP (software) fallback, an offscreen `Texture2D` render target plus matching staging texture for CPU readback, `Resize` to switch between the fixed 800x450 preview target and an arbitrary export resolution without recreating the device, and row-pitch-aware pixel readback - `ShadertoyUniformsBuffer`: 128-byte, 16-byte-aligned struct mirroring the transpiler&apos;s `cbuffer ShadertoyUniforms` layout exactly - `D3D11ShaderRenderer` (`IShaderRenderer` implementation): compiles a fullscreen-triangle vertex shader and the transpiled HLSL pixel shader via `Vortice.D3DCompiler`, uploads per-frame uniforms to a dynamic constant buffer, draws, and returns the rendered pixels - `RenderTargetSize`: resolution value type with an `IsValid` guard and an `800x450` preview default - `Videotoy.Core.GlslToHlslTranspiler`&apos;s `cbuffer` layout corrected to explicit 16-byte register packing (128 bytes total) to match the new `ShadertoyUniformsBuffer` byte-for-byte `IShaderRenderer` now takes a `RenderTargetSize` and exposes `Resize`, used by `App.xaml.cs` to register `D3D11ShaderRenderer` as the default renderer (`NullShaderRenderer` remains available as a no-op fallback) `FrameSequenceRenderer` (`Videotoy.Rendering`): drives a full export pass frame-by-frame in memory from `Videotoy.Core.LoopCalculator`&apos;s deterministic timeline (`computeFrameCount` / `buildFrameTimeline`), where each frame&apos;s `iTime`/`iTimeDelta` is derived purely from its frame index and the target frame rate, independent of wall-clock or machine speed; each call to `RenderedFrame` yields the rendered RGBA pixels for one frame with no real-time playback loop involved - `RenderedFrame`: immutable record (`Index`, `TimeSeconds`, `PixelsRgba`) carrying one rendered frame&apos;s data in memory Multi-pass rendering with ping-pong render targets: - `Videotoy.Core.PassGraph`: computes the topological execution order of Buffer A/B/C/D + Image passes from their `iChannelN` buffer references (buffer-name matching is whitespace/case-insensitive to tolerate Shadertoy export variants such as `&quot;BufferA&quot;` vs `&quot;Buffer A&quot;`), distinguishes a self-referencing pass (legitimate ping-pong feedback, e.g. Buffer A reading its own previous frame) from a genuine multi-pass circular dependency (raised as an error), and resolves per-channel buffer bindings for the renderer - `OffscreenRenderContext` now exposes a `ShaderResourceView` on its color texture and accepts an externally-owned `ID3D11Device` / `ID3D11DeviceContext`, so multiple render targets across passes can sample each other&apos;s output on a single shared device - `MultiPassRenderer` (`Videotoy.Rendering`): compiles and renders every pass of a `ShaderProject` in dependency order on one shared D3D11 device; self-referencing buffers get a front/back pair of render targets (front = last completed frame, readable; back = current frame&apos;s write target), swapped after every pass has rendered so a buffer&apos;s feedback always samples the previous frame&apos;s stable result, never the frame currently being written; returns the final `Image` pass&apos;s RGBA pixels Real-time preview playback loop with a scrubbable timeline: - `PreviewClock` (`Videotoy.Rendering`): UI-framework-agnostic playback clock exposing play/pause/seek/stop and automatic looping over a configurable duration; advances purely from externally-supplied real-time deltas (`Advance(deltaSeconds)`), independent of any particular timer implementation - `MainWindow` now drives `PreviewClock` from `CompositionTarget.Rendering` (WPF&apos;s vsync-aligned render loop), measuring the actual elapsed wall-clock time via `Stopwatch` each frame - `MainWindowViewModel`: renders the current preview frame through `MultiPassRenderer` into a `WriteableBitmap` bound to the viewport, with `TogglePlayback`/`StopPlayback`/`BeginScrub`/`EndScrub` commands and a `Seek` method used while the user drags the timeline - Viewport UI: the static &quot;Preview rendering will be available in a future version&quot; placeholder is replaced by a live `Image` bound to the rendered bitmap, with a play/pause button, a stop button, a `Slider` timeline with progress cursor, and the current playback time - `PlaybackIconConverter`: swaps the play/pause button glyph based on `IsPlaying` - Timeline scrubbing pauses `PreviewClock` for the duration of the drag (mouse-down to mouse-up on the slider) so the user&apos;s cursor position is never overwritten by the ongoing playback update, and resumes playback afterwards only if it was already running ## [0.3.0] - 2026-08-30 ### Added New `Videotoy.Media` project: texture loading, audio decoding, deterministic audio-reactive texture generation, and recent-files persistence Shader loading via the &quot;File &gt; Open Shader...&quot; menu (`OpenFileDialog`) Drag &amp; drop validation restricted to `.glsl`, `.frag`, `.json` and `.shadertoy` files, with visual drop-effect feedback Shadertoy JSON export parser (`ShadertoyJsonParser`): multi-pass wiring (Image, Buffer A/B/C/D, Common), `iChannel` input resolution (buffer, texture, video, cubemap, music/musicstream) Lightweight structural shader validation (`ShaderValidator`): missing `mainImage` entry point, unbalanced braces/parentheses, stray `#version` directives — surfaced as line-numbered errors/warnings Dedicated, collapsible &quot;Shader Issues&quot; panel listing parsing/validation errors and warnings per pass, with line numbers Extended Shadertoy uniform model (`UniformValues` / `UniformBuilder`): `iResolution`, `iTime`, `iTimeDelta`, `iFrame`, `iMouse`, `iDate`, `iSampleRate`, per-channel resolutions Texture loading for `iChannel` image inputs (`TextureLoader`, WIC-based, BGRA32 output) Audio decoding for `iChannel` audio inputs (`AudioTrackLoader`): WAV, MP3 and OGG via NAudio / NAudio.Vorbis Deterministic 512x2 audio spectrum/waveform texture generation (`AudioSpectrumTextureGenerator`), computed from `iTime` per frame rather than real-time playback Recent shaders list: menu entries backed by JSON persistence in `%AppData%\Videotoy\recent-shaders.json` ## [0.2.0] - 2026-08-30 ### Added Custom window chrome (no native title bar) with Windows 11 Mica backdrop and rounded corners via DWM interop Fixed 800x450 preview viewport, centered and non-resizable Top menu bar (File, Edit, Render, Help) with vector icon glyphs Bottom status bar showing status message, current frame, and FPS Collapsible settings panel with animated width transition Single light theme (brushes, shadows, glow effect, control styles) About window bound to application metadata (name, SemVer version, copyright, email, website) Basic drag &amp; drop file handling on the main window ## [0.1.0] - 2026-08-30 ### Added Solution structure with four projects: `Videotoy.App` (WPF, C#), `Videotoy.Core` (F#), `Videotoy.Rendering` (C#), `Videotoy.Ffmpeg` (C#) Shared MSBuild configuration (`Directory.Build.props`, `.editorconfig`) Automatic SemVer versioning scaffolding Base repository documentation: `README.md`, `CHANGELOG.md`, `COMPILATION.md`, `LICENSE` (MIT) Continuous integration workflow (Windows build) Base folder structure (`/src`, `/assets`, `/tools/ffmpeg`, `/docs`, `/installer`)</description>
  </item>
  <item>
    <title>sophia-win11 v1.2.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=sophia-win11</link>
    <guid isPermaLink="false">sophia-win11-1.2.0.0</guid>
    <pubDate>Sat, 22 Aug 2026 00:00:00 GMT</pubDate>
    <description>Added an About page, reachable from the navigation menu, showing the app name and current version, an animated Art Déco banner, the Sophia Script and UI Windows 11 copyright notices with clickable links to the original project and the app&apos;s website, and a mention of the MIT license. A new button lets you export the full license text and the list of third-party libraries used by the app to a text file of your choice, so you no longer need to open the installation folder to read them. The list of third-party libraries and their licenses was also audited and completed to cover every library actually shipped in the app.</description>
  </item>
  <item>
    <title>sophia-win11 v1.0.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=sophia-win11</link>
    <guid isPermaLink="false">sophia-win11-1.0.0.0</guid>
    <pubDate>Sat, 22 Aug 2026 00:00:00 GMT</pubDate>
    <description>Release candidate: feature freeze (v0.3.0.0 → v0.5.0.0 portage, v0.6.0.0 → v0.9.0.0 UI/theme/animation), full codebase review, and bug-fix pass. Zero residual comments, zero hardcoded visual values outside the theme dictionaries, project-wide. ### Fixed `SessionService.ApplySessionAsync` now only creates a Windows System Restore point when the session&apos;s tweak selection contains at least one `Medium`- or `High`-risk tweak, mirroring `TweakBase.ApplyAsync`&apos;s per-tweak snapshot gating — previously it created a restore point unconditionally for every session, including single `Low`-risk registry toggles, making every Apply click pay the restore-point cost (several seconds, plus Windows&apos; 24h `SystemRestorePointCreationFrequency` throttle) `TweakRowViewModel.ApplyAsync`/`RevertAsync` now route through `ISessionService.ApplySessionAsync`/`RollbackSessionAsync` instead of calling `Tweak.ApplyAsync()`/`RevertAsync()` directly, so the v0.5.0.0 safety-net engine (conflict detection, restore points, health diagnostics) actually runs for real UI apply/revert clicks rather than only through the automatic per-tweak snapshot; a `TweakConflictException` now surfaces as a distinct &quot;Conflict detected&quot; snackbar instead of the generic failure message `DashboardViewModel.LoadProfileAsync` now resolves a loaded profile&apos;s tweak IDs and applies them as a single `ISessionService.ApplySessionAsync` session instead of looping `tweak.ApplyAsync()` per tweak, so a profile&apos;s tweaks get one conflict check/restore point instead of none; a `TweakConflictException` shows a distinct &quot;conflicting tweaks&quot; snackbar Added a Preview (Dry-Run) button to `TweakRowView`, calling `Tweak.PreviewAsync()` and surfacing the result via a longer-duration `ISnackbarService` toast — the v0.5.0.0 `ITweak.PreviewAsync` path was previously unreachable from the UI **Critical**: `ShellViewModel.InitializeAsync()` had no error handling around the tweak catalog load; since it&apos;s called from an `async void` event handler (`MainWindow.xaml.cs`&apos;s `OnLoaded`), any exception during catalog parsing (a malformed entry in the 97-tweak hand-authored JSON, a missing embedded resource) would crash the entire application on every launch with no recovery path. Now caught and surfaced as a status message + error snackbar instead `RegistryTweak.IsAppliedCoreAsync` and `ConflictDetectionService`&apos;s apply-value comparison used `Equals(object, object)` on registry values, which is reference equality for arrays (`byte[]`/`string[]`) — a `Binary`-kind tweak (e.g. the shipped `CapsLock` scancode-map tweak) would always report `IsAppliedAsync() == false` even immediately after a successful apply, and two tweaks writing byte-for-byte identical binary/multi-string values to the same registry target would be falsely flagged as conflicting. Added `RegistryValueComparer.AreEqual` (structural `SequenceEqual` for array types, falls back to `Equals` otherwise) and used it in both places ### Removed `IThemeService`/`ThemeService` and `IAnimationService`/`AnimationService` (`SophiaWin11.Core`): dead code orphaned since `MainViewModel` was deleted in v0.6.0.0&apos;s UI rewrite — the real Art Déco theme is applied statically via `App.xaml` `ResourceDictionary` merging (v0.7.0.0) and the real reduced-motion gate is `SophiaWin11.UI/Animation/MotionPolicy.cs` (v0.8.0.0); `ThemeService.ApplyTheme()` did nothing beyond flipping a bool, and `AnimationService.AnimationsEnabled` was a hardcoded-`true` field nothing real ever read `IBackupService`/`BackupService` (`SophiaWin11.Core`): both methods were `NotImplementedException` stubs since v0.2.0.0, never called from anywhere, fully superseded by the working `ITweakSnapshotService` (per-tweak registry snapshot) and `IRestorePointService` (real Windows System Restore points) ### Known gaps at release The manual test campaign across Windows 11 25H2 and Windows 11 Enterprise LTSC 2024 (Home/Pro/Enterprise) called for by this milestone was not performed — it requires a dedicated VM fleet not available in this environment. The &quot;stable 2h+, no memory leak (dotnet-trace)&quot; acceptance criterion has only partial evidence: a 2-hour trace was started against the running app but the process was closed externally after ~14 minutes (confirmed unrelated to the application — a clean Generic Host shutdown, no exception). Those 14 minutes ran clean with no errors, but this falls short of the full 2h+ requirement.</description>
  </item>
  <item>
    <title>sophia-win11 v0.9.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=sophia-win11</link>
    <guid isPermaLink="false">sophia-win11-0.9.0.0</guid>
    <pubDate>Sat, 22 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Added 6 real Lottie/Bodymovin animated assets under `SophiaWin11.UI/Assets/Animations/` (44 KB total, well under the 5 MB budget): `splash-loop.json` (app-load ornament), `status-success.json`, `status-failure.json`, `status-progress.json` (fast-operation in-progress), `loading-mascot.json` (long-running `PowerShellNativeTweak` operations), `about-banner.json` (produced now, no host page until v1.2.0.0) `tools/animations/generate_lottie_assets.py`: standalone Python 3 stdlib-only script that emits valid Bodymovin JSON directly (no After Effects/GUI exporter available) using only Skottie-confirmed-supported features (shapes, trim paths, transforms — no raster/text/expressions); colors sourced from `DesignTokens.xaml` hex values kept as named constants in the script `SophiaWin11.Tests/Animation/LottieAssetTests.cs`: parses every asset through the real `SkiaSharp.Skottie.Animation.Create` parser (not a hand-rolled schema check), asserts sane `Duration`/`Fps`/`InPoint`/`OutPoint`, plus a total-size-under-5MB assertion — 7 new tests `COMPILATION.md`: new &quot;Animation asset pipeline&quot; section documenting the generation method, export/embedding path, validation approach, and a full animation style guide table (duration, loop, easing, `DesignTokens.xaml` token references per asset) `MainWindow.xaml`&apos;s splash/loading state now plays the real `splash-loop.json` through `LottieAnimationPresenter`, replacing v0.8.0.0&apos;s placeholder procedural `LoopingOrnament` rotation; the redundant stock `ui:ProgressRing` shown alongside it was removed as visually redundant with the new animation `TweakRowView`/`TweakRowViewModel`: apply/revert now shows `status-progress.json` for fast (`RegistryTweak`) operations vs `loading-mascot.json` for long-running (`PowerShellNativeTweak`) ones, then `status-success.json`/`status-failure.json` on completion (auto-reset after 2.5s) — no default `ProgressBar`/`ProgressRing` anywhere in this flow ### Changed `Directory.Build.props` version bumped to `0.9.0.0` ### Notes The roadmap&apos;s &quot;icônes de statut animées par catégorie&quot; was interpreted as 3 generic states (success/failure/in-progress) reused across every category, not 3×8=24 category-specific animated variants — the 8 static category icons from v0.7.0.0 (`ArtDecoIcons.xaml`) are unrelated and out of scope here; documented in `COMPILATION.md` Fixed during review: the delegated work left `MainWindow.xaml.cs` with a missing `using SophiaWin11.UI.Animation;` (broke the build, `PageTransitions.Enter` calls unresolved) — restored</description>
  </item>
  <item>
    <title>MAMESET-Cleaner v7.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=mameset-cleaner</link>
    <guid isPermaLink="false">mameset-cleaner-7.0.0</guid>
    <pubDate>Fri, 21 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Interface redesign: dense, classic RomCenter-inspired look This release restyles the interface toward a denser, more information-rich look inspired by RomCenter — a layout and density change only. The app&apos;s existing color theme is unchanged. *Highlights** The Results view is now a compact grid with a colored status column (green/red/amber/grey), always paired with a short text code (`OK`/`MISS`/`BAD`/`N/A`) so status is never conveyed by color alone. New CRC and size columns are also shown, and the header now has a sort-direction indicator. The sidebar is now a flatter toolbar (square rows, a solid pressed-in outline for the active view) instead of the previous rounded icon rail. A new persistent status bar at the bottom of the window always shows live scan counts (OK/Missing/Corrupted/Unreferenced), no matter which section you&apos;re viewing. Settings, Filters, Plugins, and About are all tightened toward the same denser proportions. No changes to scanning, filtering, deduplication, or verification behavior — this release is entirely about layout and information density. See `CHANGELOG.md` for the full version history.</description>
  </item>
  <item>
    <title>MAMESET-Cleaner v6.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=mameset-cleaner</link>
    <guid isPermaLink="false">mameset-cleaner-6.0.0</guid>
    <pubDate>Fri, 21 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Correctness release: 1G1R deduplication now actually works across all 87 systems This release fixes a significant correctness gap: 1G1R duplicate detection previously only worked for MAME. Every other supported system (No-Intro, TOSEC, and Redump-based — 86 of the 87 systems) silently performed zero deduplication, because those DAT formats never declare the parent/clone relationships MAME&apos;s own files do. *Highlights** 1G1R grouping now works correctly for all 87 systems. Regional releases (USA/Europe/Japan/etc.) are grouped and deduplicated everywhere, while multi-disc releases are carefully kept separate — never merged. Region priority is now fully configurable in Settings (add, remove, reorder), instead of being fixed to World &gt; USA &gt; Europe &gt; Japan. A new optional preferred-language setting breaks ties within a region. Official releases are now preferred over prototypes/betas/demos/unlicensed copies by default when picking which copy to keep, with a toggle to disable this for curated proto/beta collections. Added an optional deep-verification mode that recomputes SHA1 (not just CRC32 and size) for extra confidence — off by default since it&apos;s slower on large sets. The Results view now explains why a ROM was flagged as corrupted (wrong size, CRC32, or SHA1) instead of a bare status. No changes to scanning, filtering, or cleanup mechanics beyond what&apos;s described above. See `CHANGELOG.md` for the full version history.</description>
  </item>
  <item>
    <title>MAMESET-Cleaner v3.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=mameset-cleaner</link>
    <guid isPermaLink="false">mameset-cleaner-3.0.0</guid>
    <pubDate>Thu, 20 Aug 2026 00:00:00 GMT</pubDate>
    <description>MAMESET Cleaner now covers the full No-Intro, TOSEC and Redump catalogs: 87 console and computer systems, on top of its native MAME support. ## What&apos;s new since v2.0.0 **87 systems available** for download from the &quot;Plugins&quot; section, up from 30 at v2.0.0 — every major Nintendo, Sega, Sony, SNK and Atari console, plus dozens of classic 1st/2nd-generation consoles, handhelds and 8/16-bit home computers (full list on the [project website](https://patrickjaillet.github.io/MAMESET-Cleaner)). **The &quot;Plugins&quot; section and the &quot;Système actif&quot; selector in Settings are now grouped by manufacturer with a search box**, so the catalog stays usable at this scale instead of an unmanageable flat list. **Fixed a real cross-plugin identity bug**: `abi_stable`&apos;s plugin loader used to cache the first-loaded plugin and silently keep returning it for every subsequent, different plugin loaded in the same process — meaning switching between two console systems in one session could fail to actually switch. Fixed and covered by a dedicated regression test. Over 270 automated tests now cover the software, including all 87 console plugins, each loaded and exercised as a real compiled library. ## System requirements Windows 10 or Windows 11 (64-bit). ## Installation Two options: **`MAMESET-Cleaner-Setup-v3.0.0.exe`**: standard Windows installer (silent install/uninstall tested). **`mameset_cleaner.exe`**: portable executable, no installation required. Console plugins are downloaded separately, on demand, from the application&apos;s &quot;Plugins&quot; section.</description>
  </item>
  <item>
    <title>MAMESET-Cleaner v2.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=mameset-cleaner</link>
    <guid isPermaLink="false">mameset-cleaner-2.0.0</guid>
    <pubDate>Thu, 20 Aug 2026 00:00:00 GMT</pubDate>
    <description>MAMESET Cleaner becomes a multi-console ROM set cleaning tool. Beyond its original MAME support, the application can now install a plugin for another console system directly from its new &quot;Plugins&quot; section, and clean that system&apos;s ROM set with the same duplicate detection, filtering, cleanup and reporting tools already used for MAME. ## What&apos;s new since v1.0.0 **Plugin system**: install, update and remove console-specific plugins from a new &quot;Plugins&quot; section, with SHA-256 verification on every download and a non-blocking progress bar. **30 console systems available today**, spanning Nintendo (NES/Famicom, SNES, N64, Game Boy/Color, Game Boy Advance, DS, GameCube/Wii), Sega (Master System, Mega Drive/Genesis, Game Gear, Sega CD, Saturn, Dreamcast), Sony (PlayStation, PlayStation 2, PSP), SNK/Atari/other manufacturers (Neo Geo AES/MVS, Neo Geo Pocket, Atari 2600/5200/7800/Lynx/Jaguar, TurboGrafx-16/PC Engine, WonderSwan, 3DO), and classic 8/16-bit computers (Commodore 64, Amiga, ZX Spectrum, Amstrad CPC, MSX/MSX2). **A &quot;Système actif&quot; selector** in Settings to switch between MAME and any installed plugin before scanning. **Hardening**: a plugin can no longer claim another system&apos;s identity or impersonate the built-in MAME system; a plugin encountering an internal error is now reported cleanly instead of being able to affect the rest of the application. Over 150 automated tests now cover the software, including every console plugin loaded and exercised as a real compiled library. ## System requirements Windows 10 or Windows 11 (64-bit). ## Installation Two options: **`MAMESET-Cleaner-Setup-v2.0.0.exe`**: standard Windows installer (silent install/uninstall tested). **`mameset_cleaner.exe`**: portable executable, no installation required. Console plugins are downloaded separately, on demand, from the application&apos;s &quot;Plugins&quot; section.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.18</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.18</guid>
    <pubDate>Wed, 19 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.18 — August 19, 2026 ### Fixed **A hexadecimal integer literal in a shader&apos;s code (e.g. `0x1F`) could be turned into a broken slider.** Dragging it would corrupt the surrounding source text (e.g. `0x1F` becoming `5x1F`, no longer valid code). Hexadecimal literals are no longer picked up as sliders at all. **A very first assignment to any `iChannel` — made before ever switching between the Image/Buffer A-D tabs — could silently attach to the wrong pass** (Buffer A instead of the pass actually shown). No error, nothing visibly wrong on screen; fixed so the textures panel is correctly synced with the displayed pass from the moment the window opens. **Scrolling the mouse wheel or using the keyboard arrows directly on an integer slider&apos;s track could do nothing for many notches in a row** (for a typical range, up to ~30 notches before the first visible change) — the value did move on the paired spin box just fine, only the slider track itself felt unresponsive. Both now respond identically, one unit per notch. **A shader with several dozen sliders simultaneously animated by keyframes could visibly stutter while playing**, occasionally dropping an entire frame&apos;s worth of time (measured up to ~43ms) just updating the sliders panel, before the next frame had even started rendering. Reworked so this never exceeds about 10ms even in that scenario. **An emptied pass tab (a Buffer nobody&apos;s using anymore) kept its last-compiled shader silently running and rendering every frame in the background**, costing GPU time for nothing. Clearing a tab&apos;s code now actually tears down its render pipeline. **`.bmp`, `.gif`, and `.webp` image files could be selected in an `iChannel` picker but failed to load** — the software&apos;s own file filters advertised `.bmp` support that wasn&apos;t actually there. All three now load correctly. ### New **A per-slider interpolation curve for keyframe animation**: choose Linear, Smooth (ease), or Stepped from the keyframe menu, with a live graph showing the actual curve shape as you pick. **Adjustable preview resolution**, independent of the window size: a new control in the status bar renders the live preview at 100/75/50/25% to keep things smooth on a heavy shader, always showing the resolution actually in use. **Audio `iChannel` volume and mute**, adjustable right from the textures panel, independent of the system volume — saved with the project. **Procedural textures (checkerboard, white noise, value noise) now have adjustable pattern size and random seed**, instead of fixed built-in values. **Cubemap assignment now shows a 6-face preview thumbnail** (not just one face), and checks every face&apos;s file and dimensions *before* closing the dialog — a missing file or a mismatched face size is caught immediately, naming exactly which face and why. **Video, webcam, and audio `iChannel` slots now show a live thumbnail** of their actual content (a real decoded frame, or a waveform sparkline) instead of a generic icon. **Dropping an unsupported file onto a texture slot now explains why it was refused**, naming the file extension, instead of silently doing nothing.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.17</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.17</guid>
    <pubDate>Wed, 19 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.17 — August 19, 2026 ### Fixed **Audio and video files assigned to an `iChannel` could silently fail to load.** A file path containing a drive letter (e.g. `C:\Music\track.mp3` — every Windows path) could be misread as a web address instead of a local file, depending on exactly how it was typed/browsed to. When it happened, the file simply never played: no error dialog, just silence — meaning a shader written to react to music could receive no audio data at all, no matter how correctly it was written. Now fixed for both audio and video file assignments. **A shader that compiled and rendered correctly on shadertoy.com could fail to compile here** with a cryptic internal error, on one specific (if uncommon) pattern: a `mat2`/`mat3`/`mat4` built from more values than it actually needs (e.g. `mat2(z, -z.y, z)`, a compact way to write a rotation matrix from an existing vector) — legal GLSL, but not something this software&apos;s shader compiler could parse. Such shaders now compile correctly.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.16</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.16</guid>
    <pubDate>Wed, 19 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.16 — August 19, 2026 ### New **A music track can now be added to an exported video (MP4).** *File → Export a video (MP4)…* gains a &quot;Music&quot; section: pick an audio file (.mp3/.wav/.ogg/.flac) and it gets mixed into the exported video as its audio track. Four options tune it: **volume** (a simple gain in dB), **start point** (skip the track&apos;s intro so the export starts on the chorus/drop instead of second 0), **loop** (on by default — a track shorter than the video repeats seamlessly to cover it; turned off, the rest of the export just plays silent instead), and **audio quality** (128/192/320 kbps). The video&apos;s own length is never affected by the music either way. The headless `--export-mp4` command-line export gained matching `--audio`/`--audio-volume`/`--audio-start`/`--audio-loop`/`--audio-bitrate` options. **New visual theme across the whole interface**, with a modern &quot;glass&quot; look: translucent, rounded, softly-bordered panels, menus and buttons, plus redesigned sliders — a slimmer track with a colored fill showing the current position at a glance, and a rounder, easier-to-grab handle that highlights on hover. **A &quot;Dé-golf&quot; button now sits next to the &quot;Golf&quot; button**, both in the toolbar and the *Edit* menu. It reformats the current tab&apos;s code — proper indentation, one instruction per line, spaced-out operators — making a golfed (or just messily pasted) shader readable again, without renaming anything or changing what it compiles to. As with Golf, if the reformatted code somehow failed to compile, nothing would be changed in the editor. ## Version 0.1.15 — August 18, 2026 ### New **The currently displayed pass can now be exported as a standalone HLSL or Metal (MSL) shader file**, via `File → Export Compiled Shader To → HLSL (.hlsl)` / `→ Metal (.metal)`, next to the existing golfed-code export. This is meant for pasting into a game engine or a native app targeting DirectX/Unity/Unreal (HLSL) or Metal (MSL) — **not** for continuing to edit the shader in this software: the exported file is a one-time, faithful translation of the shader as compiled at the moment of export, and this software has no way to read HLSL or MSL back in, so it can&apos;t be pasted back into the editor afterward. This limitation is shown up front, before the save dialog even opens. Custom `iChannel`/uniform bindings translate to `Texture2D`/`SamplerState` (HLSL) or `texture2d&lt;float&gt;`/`sampler` (MSL) using generic binding conventions that may need manual adjustment for the target engine. This export always translates whichever GLSL/WGSL source is currently compiled, golfed or not (your choice) — golfing is a text-level GLSL minifier and is never applied again as part of the HLSL/MSL translation. There is also no guarantee that recompiling the exported file in a third-party engine renders pixel-for-pixel identically to this software&apos;s own preview: the translation targets functional correctness, not a bit-exact match — worth checking case by case in your target engine.</description>
  </item>
  <item>
    <title>MAMESET-Cleaner v1.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=mameset-cleaner</link>
    <guid isPermaLink="false">mameset-cleaner-1.0.0</guid>
    <pubDate>Wed, 19 Aug 2026 00:00:00 GMT</pubDate>
    <description>Première version stable de MAMESET Cleaner. MAMESET Cleaner nettoie et organise un set de ROMs MAME : détection des doublons (logique 1G1R), filtrage par genre/langue/région/statut/fabricant/année, nettoyage sécurisé (corbeille, sauvegarde préalable, confirmation obligatoire) et rapports détaillés. ## Fonctionnalités principales Scan du dossier de ROMs (.zip, .7z, dossiers non compressés) avec détection réelle des fichiers corrompus Détection des doublons selon une priorité régionale personnalisable Filtrage multi-critères combinables Nettoyage réel avec corbeille Windows par défaut, sauvegarde optionnelle et vérification d&apos;intégrité automatique après nettoyage Rapports JSON/CSV détaillés Interface français/anglais, thème clair/sombre ## Configuration requise Windows 10 ou Windows 11 (64 bits). ## Installation Deux options : **`MAMESET-Cleaner-Setup-v1.0.0.exe`** : installateur Windows classique (installation et désinstallation silencieuses testées). **`mameset_cleaner.exe`** : exécutable portable, aucune installation requise. Voir le [README](https://github.com/Patrickjaillet/MAMESET-Cleaner#readme) pour le détail des fonctionnalités et [docs/COMPILATION.md](https://github.com/Patrickjaillet/MAMESET-Cleaner/blob/master/docs/COMPILATION.md) pour compiler depuis les sources.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.15</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.15</guid>
    <pubDate>Tue, 18 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.15 — August 18, 2026 ### New **The currently displayed pass can now be exported as a standalone HLSL or Metal (MSL) shader file**, via `File → Export Compiled Shader To → HLSL (.hlsl)` / `→ Metal (.metal)`, next to the existing golfed-code export. This is meant for pasting into a game engine or a native app targeting DirectX/Unity/Unreal (HLSL) or Metal (MSL) — **not** for continuing to edit the shader in this software: the exported file is a one-time, faithful translation of the shader as compiled at the moment of export, and this software has no way to read HLSL or MSL back in, so it can&apos;t be pasted back into the editor afterward. This limitation is shown up front, before the save dialog even opens. Custom `iChannel`/uniform bindings translate to `Texture2D`/`SamplerState` (HLSL) or `texture2d&lt;float&gt;`/`sampler` (MSL) using generic binding conventions that may need manual adjustment for the target engine. This export always translates whichever GLSL/WGSL source is currently compiled, golfed or not (your choice) — golfing is a text-level GLSL minifier and is never applied again as part of the HLSL/MSL translation. There is also no guarantee that recompiling the exported file in a third-party engine renders pixel-for-pixel identically to this software&apos;s own preview: the translation targets functional correctness, not a bit-exact match — worth checking case by case in your target engine.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.14</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.14</guid>
    <pubDate>Tue, 18 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.14 — August 18, 2026 ### New **WGSL fragment shaders are now accepted as a third input style, alongside Shadertoy and plain GLSL, with no manual conversion.** Pasting or typing a WGSL fragment entry point (`@fragment fn ... -&gt; @location(0) vec4&lt;f32&gt; { ... }`) now compiles and renders directly, just like the GLSL/Shadertoy styles introduced in 0.1.13: - the built-in values (`iTime`, `iResolution`, `iMouse`, `iChannel0-3`, ...) are exposed through a `globals` struct, made available only for the fields the code actually uses; - custom `var&lt;uniform&gt;` declarations are accepted as well (currently given a default value of 0, same limitation as for plain GLSL — not wired to the sliders panel yet); - the footer indicator gains a third icon (🟪 *WGSL*) alongside 🌈 *Shadertoy* and 📄 *GLSL*, with the same live-updating tooltip explaining what triggered the detection.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.13</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.13</guid>
    <pubDate>Mon, 17 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.13 — August 17, 2026 ### New **GLSL and Shadertoy shaders are now both accepted automatically, with no manual conversion.** Until now, every pass had to follow the Shadertoy convention (a `mainImage` function). Pasting or typing a &quot;raw&quot; GLSL fragment shader — with its own `void main()`, its own `#version` directive, its own `uniform` declarations, or the older `gl_FragColor`/`gl_FragData` output style — now compiles directly, without editing it by hand first. The software detects which style is being used and adapts on the fly: - the shader&apos;s own `#version` is kept if present, otherwise one is added automatically; - Shadertoy&apos;s built-in values (`iTime`, `iResolution`, `iMouse`, `iChannel0-3`, ...) are made available only for the ones the code actually uses; - the older `gl_FragColor`/`gl_FragData[0]` output style is translated automatically into the modern form; - the shader&apos;s own custom `uniform` declarations are now accepted as well (currently given a default value of 0 — not wired to the sliders panel yet); - compile-error messages point to the correct line in the editor in both styles, exactly as before. - The `Common` tab and Buffer A-D passes keep working exactly as before, independently of which style each pass uses. **A small indicator now appears in the footer** (🌈 *Shadertoy* / 📄 *GLSL*) showing which style was detected for the pass currently being edited, updating live shortly after you stop typing or as soon as you switch tabs. Hovering over it explains, in plain language, which part of the code triggered that detection (e.g. &quot;detected via `mainImage()`&quot;).</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.12</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.12</guid>
    <pubDate>Sun, 16 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.12 — August 16, 2026 ### New **An audio file (.mp3/.wav/.ogg/.flac) can now be used as a source for an `iChannel`**, exactly as on shadertoy.com: a new option &quot;Audio (file)...&quot; appears in the dropdown menu for each slot, between &quot;Video (file)...&quot; and &quot;Webcam&quot; (drag-and-dropping an audio file directly onto a slot also works). The chosen file plays in a loop and is **audible** during preview — unlike a video, which remains muted — since that is the whole point of a sound-reactive shader. The shader can then sample, just like on Shadertoy, the frequency spectrum (`texture(iChannelX, vec2(u, 0.25)).x`) and the waveform (`texture(iChannelX, vec2(u, 0.75)).x`) of the playing track. ⚠️ **Note for this version**: exact spectrum scaling (matching volume level to amplitude intensity) is tuned visually rather than guaranteed pixel-perfect to shadertoy.com due to the lack of an officially published formula; live microphone input is not provided (audio files only); and there is no UI volume slider yet (playback follows system volume).</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.11</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.11</guid>
    <pubDate>Sun, 16 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.11 — August 16, 2026 ### Fixed **Small shader constants could be zeroed out as soon as their slider was moved.** A ray marching epsilon, an anti-aliasing bias, or any other very small number (`0.0001`, `1e-5`...) kept its true value in the code as long as its slider wasn&apos;t touched — but upon the very first movement, however small, it could end up silently rewritten as `0.0`, without any error message (the default displayed precision did not adapt to the magnitude of the number). The default number of decimals now adjusts to the literal&apos;s original value, keeping behavior unchanged for common values. **Moving a slider with the mouse wheel or keyboard could leave orphan `-` signs accumulating in the code.** The protection intended to prevent resynchronization during slider manipulation was only triggered for a classic mouse drag; mouse wheel scrolling and keyboard arrow keys, which produce the exact same kind of rapid burst of edits, slipped through. This is now handled in the same way regardless of the method used to move the slider. **The color picker (swatch next to a `vec3`/`vec4`) could display a different color than the one actually used by the shader, without ever auto-correcting.** A bright color chosen for an originally dark constant could exceed the expected range for the associated sliders; the display was then silently clamped, but the written code still contained the unclamped value — creating a permanent mismatch between what was displayed and what was calculated. The color written in code now always matches the displayed color. **`vec3(-1.0, 0.5, 0.2)` and other vectors with negative components were never recognized as a single color/X-Y group.** Nothing was lost (each component remained editable as a separate slider), but grouping into a single control — the flagship feature of vector sliders — was never triggered whenever a component was negative, which is very common for directions or offsets. This is now fixed: these vectors are grouped like any other. **A corrupted or manually tampered project file could crash or corrupt the sliders panel UI.** A `NaN`/`Infinity` boundary slipped into a project&apos;s `.json` file passed through existing checks and could inject non-finite values into the interface sliders. Such inputs are now simply ignored on opening, without affecting the rest of the project. **Switching passes (Image/Buffer A-D) mid-slider drag could, in rare cases, rewrite the wrong area of the code.** The risk only concerned a very narrow window (switching tabs without releasing a mouse drag, usually when combining keyboard + mouse, or on a touch screen), but is now explicitly prevented. ### Added **`vec4(r, g, b, a)` constants (color + transparency) now group into a single control**, featuring the same color picker as `vec3`, instead of remaining as 4 separate numerical sliders. **Right-clicking a component of a vector slider (`vec2`/`vec3`/`vec4`)**: it is now possible to adjust its bounds and decimal precision, exactly as was already possible on a simple numerical slider. The &quot;Reset category&quot;, &quot;Randomize category&quot;, and &quot;Clear keyframes&quot; buttons now display a tooltip clarifying that they apply to all sliders in the category, including those currently hidden by the search filter.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.10</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.10</guid>
    <pubDate>Sun, 16 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.10 — 16 août 2026 ### New **New set of major European and Asian language files.** Norwegian, Swedish, German, Spanish, Italian, Portuguese, Japanese, Chinese (Simplified), Korean, Hindi</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.9</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.9</guid>
    <pubDate>Sun, 16 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.9 — 16 août 2026 ### Corrigé **Un shader pouvait s&apos;afficher délavé, presque blanc, alors qu&apos;il est vif et saturé sur shadertoy.com.** En cause : quand un shader calcule lui-même le canal alpha de `fragColor` (au lieu de le laisser à 1, ce que font la plupart des shaders sans même y penser), le logiciel transmettait cette valeur telle quelle jusqu&apos;à l&apos;affichage — qui la traitait comme une vraie transparence et laissait transparaître le fond clair de la fenêtre derrière l&apos;image. Shadertoy, lui, ignore toujours ce canal pour l&apos;image finale affichée (son visualiseur est opaque par construction) : le rendu y reste donc plein et saturé quoi que le shader écrive dans `fragColor.a`. La passe Image (celle qui s&apos;affiche réellement) est désormais forcée opaque de la même façon, ce qui aligne le rendu sur celui de shadertoy.com. Les passes Buffer A-D ne sont **pas** concernées : plusieurs techniques de shaders s&apos;en servent pour stocker des données dans leur propre canal alpha d&apos;une passe à l&apos;autre, et cette valeur reste donc intacte pour elles. ## Version 0.1.8 — 16 août 2026 ### Nouveau **Sélecteur de langue dans les Préférences.** *Fichier → Préférences…* propose maintenant un menu déroulant pour choisir la langue de l&apos;interface, avec la liste des langues effectivement présentes sur le disque (pas de langue &quot;à moitié installée&quot; dans la liste). Le choix est mémorisé et repris au prochain lancement. **Le changement ne s&apos;applique pas à chaud** : comme les menus, boutons et messages déjà affichés ne se retraduisent pas tout seuls, un message le rappelle au moment de valider — il suffit de relancer le logiciel pour voir la nouvelle langue partout.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.7</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.7</guid>
    <pubDate>Sun, 16 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.7 — 16 août 2026 ### Nouveau **Premières briques pour les langues.** Le logiciel sait maintenant charger un fichier de traduction (`lngs/fr.json`, `lngs/en.json`) au démarrage, avec repli automatique sur le français si une traduction est incomplète. **Rien n&apos;est encore visible dans l&apos;interface** : les menus, boutons et messages restent pour l&apos;instant en français en dur, en attendant qu&apos;ils soient progressivement branchés sur ce système. Pas de sélecteur de langue dans les Préférences pour le moment non plus — ce sera la dernière pièce du puzzle une fois les textes migrés. ### Corrigé **Export vidéo : la fenêtre d&apos;encodage restait bloquée à 100 %.** Une fois l&apos;encodage terminé, la boîte de dialogue *Exporter une vidéo (MP4)* pouvait rester affichée indéfiniment sans jamais se fermer, obligeant à forcer la fermeture du logiciel. En cause : la sortie d&apos;erreur de l&apos;outil d&apos;encodage n&apos;était jamais lue pendant l&apos;export, ce qui pouvait le faire se bloquer silencieusement juste avant de se terminer, même après avoir atteint 100 %. Elle est maintenant systématiquement vidée en tâche de fond, et l&apos;export se termine et se ferme normalement.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.6</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.6</guid>
    <pubDate>Sat, 15 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.6 — 15 août 2026 ### Nouveau **Export vidéo : l&apos;assemblage en `.mp4` fonctionne enfin.** La limitation annoncée dans la version précédente est levée : *Fichier → Exporter une vidéo (MP4)…* produit maintenant directement un fichier `.mp4` prêt à partager, sans étape manuelle avec un outil externe. - Une **barre de progression annulable** affiche séparément l&apos;avancement du rendu (&quot;Rendu : 120/300 images&quot;) puis celui de l&apos;encodage. Annuler à tout moment interrompt proprement l&apos;opération en cours et ne laisse aucun fichier temporaire sur le disque. - L&apos;encodage utilise un réglage de qualité (CRF) plutôt qu&apos;un débit fixe, pour un résultat visuellement régulier quel que soit le shader exporté. - Aucune installation séparée n&apos;est nécessaire : l&apos;outil d&apos;encodage est fourni directement avec le logiciel. - Une nouvelle option en ligne de commande (`--export-mp4`) permet d&apos;exporter une vidéo sans ouvrir l&apos;interface, utile pour traiter plusieurs shaders à la suite. ### Modifié **Le golfeur produit un code encore plus court**, avec trois nouvelles optimisations toujours actives (comme le retrait des commentaires ou le raccourcissement des nombres, sans nouvelle case à cocher) : - une affectation qui reprend la même variable à gauche et à droite (`x = x + valeur;`) est récrite sous sa forme composée, plus courte (`x += valeur;`) ; - un constructeur de vecteur dont tous les arguments sont identiques (`vec3(v, v, v)`) est réduit à la forme courte équivalente (`vec3(v)`) ; - le mot-clé `in` devant un paramètre de fonction (`in vec2 fragCoord`), optionnel en GLSL, est retiré automatiquement (`vec2 fragCoord`).</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.5</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.5</guid>
    <pubDate>Sat, 15 Aug 2026 00:00:00 GMT</pubDate>
    <description>## Version 0.1.5 — 15 août 2026 ### Nouveau **Export vidéo (première étape).** Un nouveau menu *Fichier → Exporter une vidéo (MP4)…* ouvre une fenêtre de réglages où choisir : - la **durée** de l&apos;export, en secondes ou directement en nombre d&apos;images ; - la **cadence** (24, 30 ou 60 images par seconde, ou une valeur libre) ; - la **résolution** de sortie, indépendante de la taille actuelle de l&apos;aperçu ; - le **niveau de compression**, avec trois réglages simples (Qualité, Équilibré, Taille minimale) ou un réglage précis pour les personnes qui savent ce qu&apos;elles font. Une estimation de la taille du fichier final s&apos;affiche en direct pendant que vous ajustez les réglages. ⚠️ **À savoir dans cette version** : l&apos;assemblage automatique en un véritable fichier `.mp4` n&apos;est pas encore disponible. Le logiciel produit pour l&apos;instant une série d&apos;images numérotées (une par image de l&apos;animation) et vous indique où les retrouver une fois la capture terminée ; il faut encore les assembler soi-même avec un outil externe. L&apos;export direct en `.mp4` arrivera dans une prochaine mise à jour. **Fenêtre &quot;À propos&quot; mise à jour** (menu *Aide → À propos*) : elle affiche maintenant le nom complet du logiciel, le numéro de version, les informations de copyright et une adresse de contact.</description>
  </item>
  <item>
    <title>Le-Petit-Editeur-GLSL v0.1.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=le-petit-editeur-glsl</link>
    <guid isPermaLink="false">le-petit-editeur-glsl-0.1.0</guid>
    <pubDate>Sat, 15 Aug 2026 00:00:00 GMT</pubDate>
    <description>*Full Changelog**: https://github.com/Patrickjaillet/Le-Petit-Editeur-GLSL/commits/v0.1.0</description>
  </item>
  <item>
    <title>retrotools26 v0.1.7</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=retrotools26</link>
    <guid isPermaLink="false">retrotools26-0.1.7</guid>
    <pubDate>Fri, 14 Aug 2026 00:00:00 GMT</pubDate>
    <description>## [0.1.7] - 2026-08-14 ### Fixed Games tab Grid view still overlapped the details panel with a large game list after the v0.1.6 fix: the per-row card count calculation counted only each card&apos;s content width (180px), not its rendered width including the card frame&apos;s inner margin (10px each side) — one card too many fit in the calculation and still overflowed the column. Now uses the full rendered card width and the real item spacing read from `ui.spacing()` instead of a guessed constant; verified with a 200-game scrollable list.</description>
  </item>
  <item>
    <title>retrotools26 v0.1.6</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=retrotools26</link>
    <guid isPermaLink="false">retrotools26-0.1.6</guid>
    <pubDate>Fri, 14 Aug 2026 00:00:00 GMT</pubDate>
    <description>## [0.1.6] - 2026-08-14 ### Fixed Games tab Grid view: two rendering bugs found via a real user report (34410 games, TOSEC Amiga DAT). The status tile on each card used `Frame` + `centered_and_justified` (same bug as `platform_badge::draw`, fixed the same way — exact-size `Painter` allocation). Separately, `horizontal_wrapped` inside the `ScrollArea` nested in the grid&apos;s half-width column was wrapping against the whole window&apos;s width instead of the constrained column width, so cards kept flowing past the column boundary and under the details panel instead of wrapping; replaced with explicit row-chunking based on a freshly measured `ui.available_width()`. Landing page download buttons pointed at the old v0.1.0 release assets — never updated across the v0.1.1-v0.1.4 releases. Now point at the current release.</description>
  </item>
  <item>
    <title>retrotools26 v0.1.5</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=retrotools26</link>
    <guid isPermaLink="false">retrotools26-0.1.5</guid>
    <pubDate>Fri, 14 Aug 2026 00:00:00 GMT</pubDate>
    <description>## [0.1.5] - 2026-08-14 ### Fixed `platform_badge::draw` used `centered_and_justified`, which claims all *available* space in its parent `Ui` — harmless in a plain vertical layout, but it stretched the badge into a full-width colored bar wherever it sat inside an `egui::Grid` cell (Platforms tab) or next to a heading (Dashboard, Games), discovered while taking real screenshots for the README. Now allocates its exact size via the `Painter`, same approach as `shader_preview::draw`. ### Changed `README.md` rewritten as a pure end-user showcase (what the app does, not how to build it) with 4 real screenshots (Dashboard, Games, Platforms, 1G1R Builder), replacing the previous 2-screenshot version with build-instructions front and center.</description>
  </item>
  <item>
    <title>retrotools26 v0.1.4</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=retrotools26</link>
    <guid isPermaLink="false">retrotools26-0.1.4</guid>
    <pubDate>Fri, 14 Aug 2026 00:00:00 GMT</pubDate>
    <description>## [0.1.4] - 2026-08-14 ### Fixed CI was red on every commit since early in the project and had gone unnoticed because local checks never ran with the same strict flags: `cargo clippy --workspace --all-targets -- -D warnings` failed on two long-standing warnings (`ThemePreference`&apos;s and `ToastManager`&apos;s manual `Default` impls, both now `#[derive(Default)]`; `ToastManager::report` was genuinely unused code and removed), and `cargo fmt --all -- --check` failed across most of the codebase (now reformatted to rustfmt defaults, no behavior change)</description>
  </item>
  <item>
    <title>retrotools26 v0.1.3</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=retrotools26</link>
    <guid isPermaLink="false">retrotools26-0.1.3</guid>
    <pubDate>Fri, 14 Aug 2026 00:00:00 GMT</pubDate>
    <description>## [0.1.3] - 2026-08-14 ### Fixed Dashboard, Platforms, Plugins, Settings, Download and About tabs now scroll (each wrapped in its own `egui::ScrollArea::vertical()`) — on a small or restored-from-minimized window, content past the bottom of the visible area was previously unreachable with no scrollbar; the Games and 1G1R tabs already had their own internal scrolling and are unaffected ### Added New &quot;Download&quot; tab (`crates/ui/src/views/download.rs`): official/community links for everything this app works with but doesn&apos;t bundle — DAT sources (No-Intro, Redump, TOSEC), frontends/distributions (Batocera, Recalbox, Lakka, EmulationStation-DE), RetroArch cores and shaders, RetroAchievements account creation, and the third-party RVZ/CSO conversion tools; deliberately excludes ROM download sites, with a visible note explaining why — this app manages ROMs you already own, it doesn&apos;t point at copyright-infringing sources</description>
  </item>
  <item>
    <title>p5ccs v1.0.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=p5ccs</link>
    <guid isPermaLink="false">p5ccs-1.0.0.0</guid>
    <pubDate>Thu, 13 Aug 2026 00:00:00 GMT</pubDate>
    <description>First stable release of Processing 5 - Creative Coding Station: a native Windows creative-coding IDE for [p5.js](https://p5js.org/), built with C# / WPF / WPF-UI. Offline-first — the p5.js runtime, code editor, and export pipeline all run locally with zero network dependency. ## Highlights since 0.9.9-rc.1 This release is exclusively release engineering (feature freeze was declared in 0.9.9-rc.1): Version bumped to `1.0.0.0` throughout, verified against the compiled binary&apos;s actual `FileVersionInfo`. The installer now reads its version straight off the published executable&apos;s file version resource instead of a hand-maintained constant, which had silently drifted 3 releases stale before this release caught it. The installer switched to a per-user install (no admin/UAC elevation required) — the previous admin-required install was found, via real installer testing, to hang indefinitely on a UAC consent prompt in any non-interactive session. ## Installation Download the installer for your architecture below and run it — no administrator rights required. See [docs/KNOWN-LIMITATIONS.md](docs/KNOWN-LIMITATIONS.md) for exactly what has and hasn&apos;t been verified on real hardware for each Windows version/architecture combination. Full changelog: [docs/CHANGELOG.md](docs/CHANGELOG.md)</description>
  </item>
  <item>
    <title>retrotools26 v0.1.2</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=retrotools26</link>
    <guid isPermaLink="false">retrotools26-0.1.2</guid>
    <pubDate>Thu, 13 Aug 2026 00:00:00 GMT</pubDate>
    <description>## [0.1.2] - 2026-08-14 Adds the &quot;Modules Futurs Retrogaming&quot; batch (Phases 11-20): Batocera/ Recalbox/Lakka export, ScreenScraper metadata scraping, ES-DE smart collections, save backup/restore, controller profile library, shader overrides, a core-compatibility advisor, RVZ/CSO conversion, SD/USB card imaging, and RetroAchievements integration — plus generated platform badges and shader preset previews in the UI. ### Added Static shader preset previews (`crates/ui/src/shader_preview.rs`): a small synthetic reference image shown next to each shader association and in the preset picker in Settings, illustrating the preset&apos;s category (CRT/scanline/pixel-art upscaler) with scanline overlays, a vignette, or a blocky-vs-smooth split — not a real screenshot, since this project doesn&apos;t bundle third-party shader/game screenshots; the file-name classification is a pure, unit-tested function kept separate from the drawing code Procedurally generated platform badges (`crates/ui/src/platform_badge.rs`): a deterministic colored circle with 2-4 letter initials derived from the platform&apos;s name, shown in the Platforms, Dashboard and Games tabs — real console/company logos are trademarks and can&apos;t legally be bundled in this public repository under any license this project could choose, so this sidesteps that entirely while still giving every platform a distinct visual identity RetroAchievements integration (`retrotools-plugin-retroachievements`): syncs and locally caches known RetroAchievements-compatible ROM hashes per platform (via an editable platform → console-id table) using your own username/API key (encrypted, Settings), then cross-references the current 1G1R selection and reports any kept game with no known hash; a new opt-in tie-breaker, `RulePriority::prefer_retroachievements_compatible` (off by default, changes nothing unless enabled), lets 1G1R selection itself prefer an RA-compatible alternate when otherwise tied — both read the same on-disk hash cache without sharing Rust state; RetroAchievements compatibility now also shows in the Games tab&apos;s detail panel SD/USB card imaging (`retrotools-plugin-sdcard-imager`): checksum verification of a downloaded Batocera/Recalbox/Lakka base image (`sdcard verify`), removable-USB-disk detection (`sdcard list-devices`), and raw image writing (`sdcard write`) gated by a typed double confirmation of the exact device id (nothing is written if `--device`/`--confirm` don&apos;t match) — the destructive write path is deliberately plain CLI-invoked functions rather than the generic plugin contract, since `PluginContext` has no room for that confirmation; `sdcard-inject` (a normal plugin) then mirrors a staging folder (built 1G1R set plus any Export/Controllers/Shaders output) onto the freshly-imaged, already-mounted partition; a manual real-hardware validation procedure is documented in `docs/PLUGIN_DEV.md` since writing to a real device is intentionally not automated in tests RVZ (GameCube/Wii) and CSO (PSP) conversion: `convert to-rvz`/`from-rvz` (via a third-party `DolphinTool`) and `convert to-cso`/`from-cso` (via a third-party `maxcso`), symmetric to the existing CHD conversion commands; neither tool is bundled with this app (unlike UnRAR/chdman/7za) — `docs/COMPILATION.md` explains how to obtain and point the app at them (`RETROTOOLS_DOLPHINTOOL_PATH`/`RETROTOOLS_MAXCSO_PATH`, a `resources/` folder, or `PATH`), and each command fails with a clear message rather than silently if the tool isn&apos;t found Core compatibility advisor (`retrotools-plugin-core-advisor`): a local, user-imported JSON database mapping platform/game → recommended libretro core (with a confidence level, a free-text note, and a &quot;known problematic&quot; flag), edited from a new Settings section; `core-advisor-report` cross-references the games the last scan actually matched (via the new `PluginContext::match_report`, no re-scanning) against that database, writes a plain-text report, and generates a per-game `.opt` core-options override at RetroArch&apos;s exact expected path for any entry with specific options; the recommended core and its note now also show in the Games tab&apos;s detail panel RetroArch shader override generation (`retrotools-plugin-shaders`): a library of `.glslp`/`.slangp` presets (3 realistic starters — CRT geom, scanlines, scale2x — whose shader paths were verified against the real `libretro/slang-shaders` folder layout so they actually resolve on a standard RetroArch install, though nothing is copied from that repository itself, plus import of externally-supplied preset files), a saved shader → core / shader → game association list edited from a new Settings section, and `shaders-export` which writes RetroArch override files at the exact path RetroArch itself expects (`&lt;core&gt;/&lt;core&gt;.cfg` for a whole core, `&lt;core&gt;/&lt;content dir&gt;/&lt;game&gt;.cfg` for one game); every generated file is recorded in a manifest so `shaders-clean` can remove exactly what this tool produced without ever touching a hand-written override `PluginContext` gained a `match_report: Option&lt;&amp;MatchReport&gt;` field so a plugin can cross-reference per-ROM scan status (matched/corrupt/unknown/missing) without re-scanning — needed by the upcoming core-compatibility and RetroAchievements modules; the UI now threads its live `MatchReport` through, the CLI passes `None` (it doesn&apos;t currently keep one around between commands)</description>
  </item>
  <item>
    <title>retrotools26 v0.1.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=retrotools26</link>
    <guid isPermaLink="false">retrotools26-0.1.0</guid>
    <pubDate>Thu, 13 Aug 2026 00:00:00 GMT</pubDate>
    <description># Retro Tools 2026 v0.1.0 First release: DAT parsing (No-Intro/Redump/TOSEC/MAME), ROM scanning and hashing (incl. ZIP/7Z/RAR/CHD), 1G1R selection with configurable region/language rules, file-build operations with undo, a static plugin system, a full CLI, an egui desktop UI, and Windows packaging (installer + portable build). ### Added Replaced the two literal &quot;Placeholder preview&quot; mockups in `docs/screenshot1.png`/`screenshot2.png` with real captures of the running app (Dashboard and 1G1R Builder, with an actual DAT imported); corrected a stale claim in `docs/PLUGIN_DEV.md` that CHD conversion was unimplemented (it was added in Phase 7, `retrotools_core::convert`) Real GitHub-Releases-backed auto-updater (`retrotools_common::updater::GitHubReleaseSource`): checks the public Releases API (no auth token needed) and compares versions; wired into the UI (startup check when `check_updates_on_startup` is on and a repository is configured in Settings, with a toast when an update is available) and the CLI (`check-update [--repository owner/repo]`); no repository is guessed by default (`AppConfig::update_repository` starts `None`) Windows installer (`packaging/installer.iss`, Inno Setup 7): per-user install (no admin rights), bundles `resources/*.exe` and docs, Start Menu/desktop shortcuts; unsigned (no code-signing certificate available) Portable build (`packaging/make_portable.ps1` + `retrotools_common::config::is_portable_mode()`): dropping a `portable.txt` marker next to the exe switches every config/cache/log/DAT path to `&lt;exe_dir&gt;/data/` instead of the per-user profile Multi-resolution app icon (`crates/ui/assets/icon.ico`, 16/32/48/256), procedurally generated (no source logo existed), embedded into the exe (`crates/ui/build.rs` via `winresource`) and set as the runtime window icon Full-pipeline integration test (`crates/core/tests/integration_pipeline.rs`): scan → match → 1G1R preview/selection → filter → plan → execute against real files on disk, plus post-transfer verification and undo, using only the public API — the previous test suite only ever covered one module at a time Large-scale synthetic-DAT non-regression test (`crates/core/tests/synthetic_no_intro_regression.rs`): a generated ~750-game, ~150-family DAT modeled on real No-Intro naming conventions (region/revision/beta tags), checked for zero lost or duplicated families after 1G1R selection, with a runtime ceiling to catch algorithmic regressions; real No-Intro/Redump sample DATs aren&apos;t bundled (third-party copyright, no network access here to fetch them) `criterion` benchmarks for hashing (`crates/core/benches/hashing.rs`, 1/8/32 MB buffers) and DAT parsing (`crates/core/benches/dat_parsing.rs`, 500/3000-game synthetic DATs), runnable via `cargo bench -p retrotools-core` Headless automated UI smoke tests (`crates/ui/src/main.rs::ui_smoke_tests`): drive every tab&apos;s `show()` against a real `egui::Context` with no window/event loop, across several frames, with and without an imported DAT, both in Expert and Wizard 1G1R modes; `egui_kittest` isn&apos;t published for the pinned egui/eframe 0.28, so this is a small hand-rolled harness instead Security review of archive extraction and file-build path construction: confirmed no zip-slip vector (entries are always streamed to a caller-supplied writer, never used to build a filesystem path) and added adversarial tests proving a malicious DAT-supplied game/rom name (`../../../../etc/evil`, backslash traversal) can never escape the configured destination root Test proving a build batch degrades gracefully when one transfer fails (destination blocked by a pre-existing file, the same failure class as a full disk or a permissions error): the failing item is reported per-item, the rest of the batch still completes ROM/ISO format converter (`retrotools_core::convert`): `convert_to_chd`/`convert_from_chd`, reusing the already-bundled `chdman.exe` (the same tool used for CHD extraction since Phase 2); new CLI `convert to-chd &lt;source&gt; &lt;dest.chd&gt;` and `convert from-chd &lt;source.chd&gt; &lt;dest_dir&gt;`; verified with a real round trip (raw image → CHD → extracted back → byte-for-byte identical). CSO (PSP) and RVZ (Dolphin) are still out of scope — each needs its own dedicated codec (`maxcso`, `DolphinTool`) that isn&apos;t bundled and isn&apos;t a byproduct of any other feature Missing-DAT detection and assisted download (`retrotools_core::dat_library::platforms_missing_dat`): flags ROM subfolders with no matching DAT imported yet; new CLI `dat detect-missing &lt;roms_root&gt; &lt;dat_dir&gt; [--assist]` and a new &quot;Missing DATs&quot; section in the UI&apos;s Platforms tab (`Detect missing DATs...` button, then a per-platform `Download &amp; import` button when a same-named DAT source is already tracked) — reuses the existing tracked-source infrastructure rather than attempting real DAT discovery, since no unauthenticated No-Intro/Redump/TOSEC API exists for that; verified end-to-end (CLI and UI) against a real local HTTP server Games grid view (`views/games.rs::show_grid`), selectable alongside the existing list view via a List/Grid toggle; card-based layout with a status-colored placeholder tile (no real artwork yet — no scraper until Phase 7) Wizard mode for the 1G1R tab (`views/onegameonerom.rs::wizard`, driven by the new `AppState::wizard_step`/`WizardStep`): a step-by-step Platform → Scan → Rules → Preview → Build flow with a clickable breadcrumb and Back/Next navigation, reusing the same section-rendering functions as the existing single-page layout Expert mode toggle in the 1G1R tab (`AppState::expert_mode`, on by default): switches between the wizard and the original all-on-one-page layout Command palette (`Ctrl+Shift+P` or the new 🔍 button): fuzzy-filtered command list with arrow-key navigation and Enter/click execution — jump to any tab, import a DAT, toggle expert mode, toggle light/dark theme Additional keyboard shortcuts: `Alt+1` through `Alt+7` jump directly to each tab Minimal i18n layer (`crates/ui/src/i18n.rs`): `Language`/`Key`/`t()`, with a language selector in Settings that switches the UI immediately; covers tabs, the dashboard and a handful of key buttons/labels rather than every string, matching the roadmap&apos;s &quot;structure ready&quot; goal Adjustable UI scale for accessibility (`AppConfig::ui_scale`, a Settings slider applied via `egui::Context::set_pixels_per_point` and persisted across restarts) Light fade-in transition when switching tabs (`egui::Context::animate_bool_with_time` in `app.rs::central_content`), as lightweight visual feedback without pulling in a tweening library Duplicate ROM detection (`retrotools-core::matcher::find_duplicate_matches`): identifies extra copies of a ROM that already matched a DAT entry elsewhere (as opposed to `unknown`, which is content the DAT doesn&apos;t recognize at all) and picks a deterministic file to keep; the CLI `clean` command gained `--duplicates` (opt-in, off by default to stay backward-compatible) and `--no-unknown`, trashing both categories through the same undo-able safe-delete path with the reason labeled in its output PDF export for scan reports (`ScanReport::to_pdf`, via `printpdf`&apos;s HTML-to-PDF renderer, reusing the existing `to_html` layout instead of a separate PDF-specific one); new CLI `scan --pdf &lt;path&gt;` option RAR and CHD archive support (`retrotools-core::external_tools`, extending `archive.rs`): shells out to bundled third-party CLI tools under `resources/` (`UnRAR.exe` for read-only RAR extraction, `chdman.exe` for CHD disk images — see `docs/COMPILATION.md` for licensing notes and packaging instructions) since neither format has a maintained pure-Rust decoder; tool resolution checks an env var override, a `resources/` folder near the executable, then `PATH`; verified with a real create→scan→hash→extract→compare round trip against a CHD built by `chdman` itself Real-time scan throughput (files/s, MB/s) in the 1G1R tab&apos;s progress indicator (`AppState::scan_speed`) DAT auto-update from a tracked download URL (`retrotools-core::dat_update`): register a name + direct DAT/ZIP URL once, then re-fetch it on demand and compare its version against the local `DatCache` to report whether anything actually changed, without ever re-importing an unchanged DAT; new CLI commands `dat source-add`/`dat source-list`/`dat source-remove`/`dat update`/`dat update-all`, and a matching &quot;DAT update sources&quot; section in the Settings tab (background thread, doesn&apos;t block the UI) Internal plugin architecture (`retrotools-plugin-api`): a `Plugin` trait, `PluginContext` and `PluginRegistry` let optional modules extend the app without modifying `retrotools-core` or `retrotools-ui`; plugins are statically-linked crates registered at startup rather than dynamically loaded `.dll`s (see `docs/PLUGIN_DEV.md` for the rationale and a step-by-step guide) Playlist Generator plugin (`retrotools-plugin-playlists`): generates RetroArch `.lpl`, LaunchBox XML and ES-DE `gamelist.xml` playlists from the current 1G1R selection BIOS Manager plugin (`retrotools-plugin-bios`): verifies a folder of BIOS files against a BIOS DAT (the same Logiqx/XML format No-Intro publishes for BIOS packs) by reusing the core scan/match engine, so no BIOS checksum is ever hardcoded New CLI commands `plugin list` and `plugin run &lt;id&gt; &lt;dat&gt; --output &lt;dir&gt; [--source &lt;dir&gt;] [--profile &lt;name&gt;]` New **Plugins** tab in the UI, listing every registered plugin with a `Run` button; validated end-to-end (source/output folder pickers, real files written to disk, live &quot;last run&quot; status per plugin) &quot;Fix report&quot; (`retrotools-core::fix`): turns a scan/match result into a precise, actionable list of what&apos;s needed to complete a set — ROMs to obtain (missing) and files to replace (present but not matching the DAT) — exportable as text/CSV, shown in the 1G1R tab and via the new CLI `fix` command Set comparator (`retrotools-core::compare`): diffs two ROM scans by content hash (added/removed/changed, with rename detection via matching file names), exposed via the new CLI `compare` command Real-time folder watcher (`retrotools-core::watcher`, backed by `notify`) and periodic auto-rescan, both toggleable from the 1G1R tab — a folder change now triggers an automatic re-scan (3s debounce) without any user action Multi-platform library overview via the new CLI `status` command: scans every platform subfolder of a ROMs root against its matching DAT and prints a completion table for the whole collection Fixed a real contrast bug found during visual QA: `theme.rs`&apos;s selection stroke color was identical to the selection background, making selected tab/row text invisible; it&apos;s now computed for contrast against the accent color Full desktop UI wired to the core engine (`crates/ui`): a new `AppState` (`crates/ui/src/state.rs`) centralizes the imported DAT library, per-platform scan/match results, 1G1R rules and background scan/build jobs (dedicated threads + `mpsc` channels so the UI never blocks) Platforms tab: import DAT files (button or drag &amp; drop), list imported platforms with type/version/game count, select/remove Games tab: searchable, filterable (region/language/status) and sortable (name/region/status/size) game list with a details panel (regions, languages, tags, ROM files, scan status, 1G1R kept/removed state) 1G1R tab: guided flow — scan a ROM folder with a live progress indicator, edit region/language priority and exclusion rules, preview the selection (kept/removed diff with per-choice explanations), load/save rule profiles, and build the set (copy/move/hardlink/symlink, dry-run, rename-to-DAT-name, folder organization) Settings tab now edits and persists the real `AppConfig` (theme, accent color, ROM/DAT directories, log level, update check) Dashboard shows real statistics (platforms, DAT files, games tracked, completion, per-platform breakdown) instead of placeholders Toast notifications wired to real actions (DAT import, scan, build, errors) instead of being unused `Ctrl+O` keyboard shortcut to import a DAT; drag &amp; drop of `.dat`/`.xml`/`.zip` files anywhere in the window File-operation engine for materializing a 1G1R set (`retrotools-core::fileops`): copy, move, hardlink or symlink the selected ROMs, with automatic fallback to copy when the source lives inside an archive; destination filenames can follow the DAT&apos;s canonical ROM name and be organized into platform/region subfolders Dry-run mode and post-transfer verification (re-hashes the destination and compares it to the source) for every build operation Operation history with undo (`retrotools-core::undo::UndoLog`, SQLite-backed): every build/clean batch can be listed and reversed via `undo list`/`undo apply` Safe delete: the `clean` command moves files unrecognized by the DAT into a local trash folder instead of deleting them outright, reversible through the same undo log Archive rebuild (`retrotools-core::rebuild`): writes one ZIP per game from the matched ROMs, streaming directly from the original file or archive entry (no full extraction to disk) CLI commands `build1g1r`, `rebuild1g1r`, `clean`, and `undo list`/`undo apply` 1G1R selection engine (`retrotools-core::rules`): configurable region/language priority, per-category exclusion filters (Beta/Proto/Demo/Kiosk/Promo/Unlicensed/Pirate/Bad Dump), &quot;prefer parent over clone&quot; tie-breaking, and revision-tag scoring (Rev/v1.1/lettered revisions) Multi-disc/multi-file aware grouping: releases are grouped by canonical title (parentheses stripped) rather than by `cloneof`, since many real-world DATs leave clone metadata unset or inconsistent for discs beyond the first — every disc of the winning release is kept together Selection preview with before/after diff and a human-readable explanation per choice (`rules::preview_selection`, `SelectionResult`, `SelectionExplanation`) Reusable 1G1R rule profiles saved as JSON on disk, plus built-in presets &quot;Standard Europe&quot;, &quot;Standard USA&quot; and &quot;Complete - No Filter&quot; (`retrotools-core::profiles`) New Game tag fields (`is_sample`, `is_kiosk`, `is_promo`, `is_pirate`, `is_alt`) parsed from DAT ROM names, extending the existing Beta/Proto/Demo/Unlicensed/Bad Dump detection CLI commands `select1g1r` and `profile list`/`profile show` Recursive, multi-root ROM directory scanner (`retrotools-core::scan`), parallelized with `rayon` Streaming CRC32/MD5/SHA1/SHA256 hashing in a single pass (`retrotools-core::hash`), including a second &quot;headerless&quot; hash when a known ROM container header is detected ROM header detection for iNES, Lynx and Atari 7800 (`retrotools-core::header`) Archive scanning without full extraction (`retrotools-core::archive`): ZIP, 7Z and TAR entries are streamed directly into the hasher; RAR and CHD are detected but not yet extractable ROM ↔ DAT matching (`retrotools-core::matcher`): exact hash matching with a filename-based fallback that flags corrupted/renamed dumps, plus &quot;unknown&quot; (unneeded) and &quot;missing&quot; reporting and a completion percentage Scan reports exportable as CSV and HTML (`retrotools-core::report::ScanReport`) Incremental scan cache (`retrotools-core::cache::ScanCache`, SQLite): skips re-hashing files unchanged since the last scan CLI command `scan` to scan a directory, optionally match against a DAT, and write CSV/HTML reports Logiqx/XML DAT parser (`retrotools-core::dat`) supporting No-Intro, Redump, TOSEC and MAME datfiles (`&lt;datafile&gt;/&lt;game&gt;` and `&lt;mame&gt;/&lt;machine&gt;` layouts), with automatic DAT type detection and region/language/tag extraction from ROM names Support for DAT files delivered inside ZIP archives Parent/clone relationship handling (`cloneof`, `romof`) with `GameSet::find_clones_of` DAT integrity validation (required fields, CRC32 format, dangling clone references) Multi-DAT library management (`retrotools-core::dat_library::DatLibrary`) for importing and tracking several DAT files at once Local SQLite cache of parsed DAT files (`retrotools-core::cache::DatCache`, via `rusqlite`) CLI commands `dat import`, `dat import-dir` and `dat validate` for parsing and validating DAT files from the command line Cargo workspace scaffold with four crates: `retrotools-common`, `retrotools-core`, `retrotools-ui`, `retrotools-cli` Centralized SemVer versioning via the workspace `Cargo.toml`, with git commit hash and build date injected at compile time Unified application error type (`AppError`) with severity levels and user-facing messages User configuration system persisted as TOML under the platform config directory Structured logging via `tracing`, writing daily rotating log files Native desktop application shell built with egui/eframe: top navigation, light/dark/system theme with accent color, toast notification system &quot;About&quot; tab displaying copyright, license, contact email, website and repository links Placeholder data model (`Game`, `RomFile`, `Region`, `Language`, `GameSet`) and stub modules for DAT parsing, ROM scanning and 1G1R rule selection, scheduled for upcoming phases Minimal command-line interface (`retrotools-cli`) exposing version and configuration path commands Continuous integration workflow: format check, Clippy lint, Windows build and test Professional repository scaffolding: `LICENSE` (MIT), `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, issue and pull request templates, `.gitignore`, `.gitattributes`</description>
  </item>
  <item>
    <title>uShader v4.5.2</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=ushader</link>
    <guid isPermaLink="false">ushader-4.5.2</guid>
    <pubDate>Mon, 10 Aug 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to µShader, from the point of view of using the application. ## [Unreleased] ## [4.5.2] - 2026-08-10 ### Fixed Fixed the custom window controls and frame after minimizing, restoring, and maximizing the main window. Rebuilt Direct2D UI resources when the render target is recreated. Added warning markers to the `Freq-aware renaming` and `Macro CSE` options. ## [4.5.1] - 2026-08-10 ### Fixed The main window now appears before OpenGL viewport creation, shader compilation, and the optional startup update check, making installed launches substantially faster. ## [4.5.0] - 2026-08-08 ### Added **Export → Standalone HTML...** (command palette): writes a single, self-contained `.html` file (inline script, no external resources) that runs the golfed shader in a fullscreen WebGL1 canvas with `iTime`/`iResolution`/`iMouse`/`iDate`/`iFrame`/`iFrameRate` all wired up, for publishing or sharing without going through twigl.app. **Copy as Shadertoy export JSON** (command palette): the golfed shader in Shadertoy&apos;s own JSON export format, ready to paste into shadertoy.com&apos;s &quot;Import&quot; dialog. **Copy as KodeLife**, and **Copy as Bonzomatic fixed** (it previously copied the golfed code unchanged, which does not compile as-is in Bonzomatic): both now rewrite the shader&apos;s uniforms and entry point to match each tool&apos;s real convention, verified against Bonzomatic&apos;s own source and a real shared KodeLife project rather than guessed. **&quot;Golf harder — Exhaustive&quot;**, next to the existing &quot;Deep&quot; toggle: when both are on, searches 4 independent simulated-annealing walks in parallel instead of 1, for a wider search at the same per-walk budget. **Local, no-telemetry crash reporter**: writes a plain-text report (exception info + your last 20 actions) to `%APPDATA%\ushader\crash_reports\` if the app ever crashes. Nothing is ever sent anywhere automatically; use the new **&quot;Copy last crash report&quot;** command to attach it to a GitHub issue yourself. **Opt-in update check**: &quot;Check for updates now&quot; and &quot;Toggle check for updates on startup&quot; in the command palette (off by default). When a newer release is available, &quot;Download and install latest update&quot; fetches the real installer and opens it — your own confirmation is always still required, nothing installs silently. **Copy CHANGELOG.md to clipboard** (command palette). **Portable mode**: drop a `portable.txt` file next to `ushader.exe` and all local settings (recent files, keybindings, golf profiles, crash reports, update setting) are stored in a `config\` folder next to the executable instead of your Windows user profile — no installation, no registry writes, suitable for a USB stick. See `uShader-GUIDE.md` for how to assemble a portable folder. ### Changed The golf engine now catches a few more identity/simplification cases that the older pass-based golfer could not reach on its own (e.g. `(x+0)*1` inside a function call argument), via a new internal equality-saturation (&quot;e-graph&quot;) pass. Off by default for now while it continues to grow; no user-visible toggle yet. Continuous fuzz testing (random option combinations against the whole shader corpus, checked by actually compiling every result) is now part of the test suite, closing a couple of small, previously-unnoticed correctness gaps (a macro-referenced local wrongly eliminated as dead code). ## [4.4.0] - 2026-08-08 ### Added New **value sliders panel**, on the left edge of the Source and Viewport tabs: every float literal in your shader (`1.0`, `0.5`, `.25`, `1e-2`, ...) automatically gets its own draggable slider, no annotation needed. Each row shows a short snippet of the line the value comes from (so several `1.0`s in different places stay distinguishable). Dragging a slider rewrites that exact number in the Source editor in place and recompiles the live viewport as you drag, so lighting, colors, thresholds, speeds, and any other constant can be tuned while watching the result update in real time. A slider&apos;s range is scaled to its starting value (`0` gets `[-1, 1]`; a positive value `x` gets `[0, 2x]`; a negative value `x` gets a symmetric `[2x, -2x]`). Bare integers (loop bounds, array sizes, indices) and numbers inside comments are left alone. ### Fixed The very first version of the sliders panel&apos;s drag handling ran the full recompile (Rust golf engine + up to 4 GL shader compiles) synchronously from the `WM_MOUSEMOVE` handler, which runs *before* that frame&apos;s repaint inside the message loop&apos;s drain step. A slow recompile there stalled the drain loop while the OS kept queueing more mouse-move messages, so the thumb visibly froze while dragging. The recompile is now driven once per rendered frame instead (throttled, and always guaranteed to run between two paints), never blocking the message loop. Releasing a slider drag re-synced the panel&apos;s rows from the newly edited source *before* telling the panel the drag had ended, which made it recompute that row&apos;s range from the value just dragged to -- and since a positive value always sits at exactly the midpoint of its own auto-computed range, every slider snapped straight back to the center the instant you let go, regardless of where you&apos;d dragged it. Reordered so the final commit happens while the panel still reports the drag as in progress. ## [4.3.4] - 2026-08-08 ### Fixed The command palette&apos;s &quot;Import exclude list...&quot; entry didn&apos;t match its actual label (&quot;Import exclude/protected name list...&quot;) in the README. The installer&apos;s fallback version string (used only when `/DMyAppVersion` isn&apos;t passed to Inno Setup) was stuck at a very old `1.6.0.18`; it now tracks the current release. ### Removed `ushader_golf`, the C API&apos;s non-traced golf entry point, was no longer called by the application (only by two C++ tests, which now use `ushader_golf_traced` instead -- a strict superset that just ignores the trace output when passed a null out-param). Removed the dead surface from both `capi.rs` and `golf_core.h`. ### Added Rust unit tests for the FFI boundary in `capi.rs` (9 tests), closing the last gap the 4.3.x accessibility/wiring audit found: the unsafe raw-pointer-array handling in `ushader_encode_gif` had no coverage anywhere, Rust or C++, until now. Rust unit tests for `aggressive.rs`&apos;s one pure, standalone-testable helper (`shortest_scientific_form`); the module&apos;s per-pass functions remain covered end-to-end through `golfer.rs`&apos;s existing test suite. ### Fixed **Ctrl+S** always opened a &quot;Save as...&quot; dialog, even for a document already saved to a known path, and **Ctrl+Shift+S** (&quot;Save As&quot;, as documented in the README) wasn&apos;t bound to anything. Ctrl+S now saves silently to the document&apos;s existing path (falling back to the dialog only for a never-saved document), and Ctrl+Shift+S is a real, separate &quot;Save as...&quot; binding that always prompts. **Golf harder** and **Toggle Deep search** were only reachable by clicking their inspector buttons; they&apos;re now in the command palette (Ctrl+Shift+P) like every other action. The **Diff**, **Trace**, and **Stats** tabs, and the command palette itself, exposed no content to Windows screen readers (Narrator). They now register their text/rows with the app&apos;s UI Automation provider (trace steps as expandable/collapsible items, palette results as selectable items, diff/stats content as readable text). Keybindings and Minimap remain the last two panels without accessibility coverage. ### Fixed The **Viewport** tab still showed the Source/Golfed/Twigl mini previews next to the live viewport, duplicating what those three panels already show on their own tabs and leaving the main viewport needlessly cramped. The Viewport tab now shows only the live viewport, expanded to fill the space the mini previews used to occupy; the mini previews are unaffected on every other tab. ## [4.3.1] - 2026-08-08 ### Fixed MRT Twigl exports (2+ render targets) were not covered by the automatic rename-collision resolution added in 4.3.0: a local variable declared `o0`/`o1` (or `outColor0`/`outColor1`) could collide with the MRT output names without being renamed. Collision detection now also runs for MRT mode, triggering only on an actual local declaration of the colliding name (not on the plain MRT output usage every legitimate MRT shader is expected to contain), to avoid false positives. Screen-reader (Narrator) coverage was documented as complete for every control; the README now accurately states that the Diff, Trace, Stats, Keybindings, Command Palette, and Minimap panels are not yet fully covered. The application did not declare per-monitor DPI awareness, so the UI could render blurry or incorrectly sized on HiDPI displays; it now opts in to `DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2` at startup (fixed-pixel layout scaling remains future work). A stale code comment described GIF capture as unimplemented; it has shipped since 4.1.0 and the comment now reflects that. A `partial_cmp().unwrap()` in the rotation-constant catalogue lookup could panic if given a `NaN` angle; it now falls back to `Equal` instead. ### Added Two new C++ test executables, `ui_pure_logic_test` and `win32_panel_state_test`, covering previously-untested pure-logic modules (fuzzy match, unified diff, GLSL formatting/tokenizing, keybindings storage, recent files, golf-options conversion) and stateful UI panel classes (command palette, diff view, trace view, document tab strip, keybindings, stats panel, appearance panel, minimap). Randomized round-trip tests for the GIF encoder&apos;s LZW dictionary (varying code size, dimensions, frame count, and palette size) guarding the invariants several internal `.expect()` calls rely on. ## [4.3.0] - 2026-08-05 ### Fixed The Twigl export conversion never handled genuine Shadertoy-style `mainImage(out vec4 fragColor, in vec2 fragCoord)` shaders — which is what every real Shadertoy shader (and this app&apos;s own default shader) actually uses — correctly. It silently left the function wrapper untouched, so the Twigl export was effectively broken for real-world shaders regardless of the selected mode. Genuine Shadertoy source now converts correctly to and from every Twigl mode. The **Twigl** tab&apos;s &quot;Import twigl shader&quot; preview box was read-only, making it impossible to paste code copied from twigl.app — it&apos;s now editable, and your pasted text survives switching tabs or editing the Source tab until you click Import. Importing a shader exported in ES 3.00 or MRT mode produced GLSL that failed to compile back in the Source tab (leftover `#version 300 es` header, mismatched output-variable declarations, and — for Geeker/ Geekest — duplicate uniform declarations conflicting with ones the app already provides). Twigl export could silently produce a visually wrong shader when your own code already used a local variable named `r`/`m`/`t`/`f`/`b`/`o`, colliding with the Geek-family uniform/output shorthand (e.g. a raymarching shader&apos;s own grid-size variable named `r` colliding with `iResolution`&apos;s shorthand). Such collisions are now resolved automatically — your identifier is renamed (e.g. `r` becomes `r_0`) and a note in the panel tells you it happened. Golfing a shader before exporting to Twigl could rename an unrelated local variable to `r`/`m`/`t`/`f`/`b`/`o`, colliding with the Twigl uniform shorthand the same way. The mini **Twigl** preview stayed blank with no explanation whenever ES 3.00 or MRT was enabled; its label now says so explicitly instead of looking like a broken/empty preview. The Twigl budget gauge used the generic &quot;X/Twitter shader&quot; byte-budget preset for every mode instead of the mode-specific &quot;Twigl classic&quot;/ &quot;Twigl geekest&quot; preset. ## [4.2.0] - 2026-08-04 ### Added A third live preview, labeled **Twigl**, joins the existing **Source** and **Golfed** ones next to the golfing controls: it shows your shader rendered exactly as twigl.app would run it, updating immediately as you change mode/ES 3.00/MRT/Backbuffer/ Sound in the Twigl Export tab. It stays blank whenever ES 3.00 or MRT is enabled, since those produce WebGL2-only GLSL this desktop viewport can&apos;t run (a known limitation, not a bug). Each of the three previews now shows its own label (&quot;Source&quot;, &quot;Golfed&quot;, &quot;Twigl&quot;) above it, so it&apos;s always clear which is which. ## [4.1.2] - 2026-08-04 ### Fixed The **Twigl** export tab showed a completely empty panel — no mode buttons, no toggles, no Copy/Import buttons, no snippet buttons, and no preview code — making the twigl.app export feature unusable from a normal launch of the application. The panel was never initialized at startup. The byte-budget line on the Twigl tab overlapped the second row of snippet buttons instead of appearing below them. ## [4.1.1] - 2026-08-04 ### Fixed The rotation-constant entries in the **Golf Tips** tab and the &quot;over budget&quot; hint banner showed garbled characters instead of degree signs and dashes. The application now launches maximized, instead of at a small default window size. ## [4.1.0] - 2026-08-04 ### Added New **Capture GIF** action for the live viewport, with three built-in size/duration presets, animated and looping. The **Twigl Export** tab (mode, ES 3.00, MRT, backbuffer, sound) now keeps its own state per document, the same way the golfing profile already did. New **Import twigl shader** button: pastes twigl.app-formatted code and converts it back into a normal, editable shader. The &quot;Copy for twigl.app&quot; button (and the matching command-palette action) now shows a confirmation message after copying. Snippet names from the twigl snippet library are now automatically protected from renaming, avoiding a naming conflict after inserting a snippet. The &quot;Sound&quot; toggle&apos;s accessibility label now states clearly that it refers to a separate program from the one shown, specific to twigl.app. ### Fixed Twigl.app exports in **ES 3.00** mode now use current texture functions (the old ones no longer compiled in a real browser). Multi-target (**MRT**) exports in ES 3.00 now include the layout information WebGL2 requires. The **Backbuffer** and **Sound** toggles in the Twigl export panel now genuinely update the live preview. The command palette&apos;s &quot;Copy as twigl&quot; action now always matches what&apos;s shown on screen (including in MRT mode). Restoring a session on launch now correctly reopens the document and tab that were active when the app was last closed. A document that was intentionally cleared (but not saved) is no longer replaced with the default shader on the next launch. The stats panel now correctly reports whether frequency-aware renaming actually ran on the last pass. Improved the performance of the frequency-aware renaming option used by &quot;Golf harder&quot;. ## [4.0.0] - 2026-08-03 Major release: UI overhaul and twigl.app export. ### Added Tabs are now grouped (Author, Analyze, Export, Settings) for easier navigation. Multi-document editing is back: every open shader gets its own tab, and the whole session (every open document) is restored automatically on the next launch. The command palette now offers one-click shortcuts: copy as Shadertoy / Bonzomatic / bare `main()` / for twigl.app, save/load a golfing profile, import an exclude-name list, capture a PNG screenshot of the live viewport, and export a self-contained HTML session report. New **Twigl Export** tab: converts the golfed shader into the format expected by twigl.app, with a mode picker (Classic/Geek/Geeker/ Geekest), an ES 3.00 toggle, MRT/Backbuffer/Sound toggles, a live preview and a byte-budget gauge, opened via `Ctrl+Alt+T`. ## [3.1.0] - 2026-08-03 ### Added New **Golf harder** button, which automatically searches for an even smaller combination of golfing options than the current settings, with a &quot;Deep search&quot; option. The stats panel gained a new section summarizing the reduction achieved and which passes actually fired. New **Golf Tips** tab: a searchable catalogue of manual reduction techniques (rotation constants, common trigonometric identities, compact noise generators, 3D shape primitives, color gradients, gamma/tonemap corrections) — every entry can be copied or inserted straight into the editor with one click. The &quot;Trace&quot; view now shows a banner whenever the golfed result exceeds the chosen size budget, linking directly to the Golf Tips tab. The golfing controls panel is now an always-visible side panel instead of a full-screen tab, with a &quot;Golf&quot; button and a &quot;Formatted view&quot; button directly accessible. ## [3.0.0] - 2026-07-22 Major change: the application was entirely rebuilt on a native Windows interface — lighter, with no external runtime to install. ### Changed The look and shortcuts stay the same, but a few features from the previous version were not yet ported to this new interface: golfing profiles (save/load), multi-document editing and session restore, &quot;Copy as Shadertoy/Bonzomatic/main()&quot; shortcuts, exclude-name-list import, HTML session report, PNG screenshot, and live-viewport video/GIF recording. These were reintroduced in versions 3.1.0/4.0.0 above, except video/GIF recording (GIF returned in the Unreleased section above). ## [2.1.0] through [2.21.0] - 2026-07-22 Groundwork for the new native interface (see 3.0.0): no visible change in the shipped application during this period, except for the points below. ### Added Support for Windows screen readers (Narrator and similar) on the title bar buttons and checkboxes. An internal color-contrast check ensures text stays readable according to accessibility standards (WCAG AA). **Colorblind-safe status indicators** checkbox (different shapes in addition to colors) in the Appearance tab. **UI text size** slider (13 to 28 pt) in the Appearance tab, with automatic scaling based on the display. Import of a Shader Minifier–style exclude-name list. Three one-click copy shortcuts: &quot;Copy as Shadertoy&quot;, &quot;Copy as Bonzomatic&quot;, &quot;Copy as bare main()&quot;. The golfing profile format (`.ushaderprofile`) is now documented and versioned, for anyone who wants to generate or read one outside the application. ### Fixed Fixed a bug preventing screen readers from correctly detecting UI elements. ## [2.0.0] and [2.0.1] - 2026-07-19 / 2026-07-20 ### Added Export a **session report** as a self-contained HTML file (works offline, no connection needed), including the source code, the golfed code, the reduction statistics, and optionally a screenshot of the live viewport. Drag-and-drop a `.glsl` file onto the window to open it directly in a new tab. **Recent files** list, available from the File menu; entries pointing at a deleted file are removed automatically. ## [1.9.0] and [1.9.1] - 2026-07-19 ### Added **Command palette** (`Ctrl+Shift+P`): fuzzy search across every action in the app (run golf, switch tabs, toggle a pass, load/save a profile, toggle Compare mode, export...). **Customizable keyboard shortcuts** for the command palette, new tab, open, save, and close-tab, from a new &quot;Keyboard Shortcuts&quot; tab. **Minimap** for the Source and Golfed editors: a compact, colored overview of the file, useful on longer shaders. New **Diff** tab: shows at a glance what changed between the source code and the golfed code, removed text struck through in red, added text in green. ## [1.8.0] - 2026-07-19 ### Added **Command-line tool** for automatically golfing a whole folder of shaders (useful for plugging into a game or demo build pipeline), with size-budget options, a detailed report, and a preview of the differences before applying. ## [1.7.0] - 2026-07-19 ### Added **Multi-document editing**: one tab per open `.glsl` file, each with its own golfing settings. The session (every open document) is now **saved and restored automatically** on launch, with a confirmation before reopening files. A dot appears on the tab of a document with unsaved changes, and a confirmation is asked before closing it or quitting the app with unsaved changes. ## [1.6.0] - 2026-07-19 Internal groundwork for the automatic verification feature (Compare mode): no visible change yet. ## [1.5.0] - 2026-07-19 ### Added New **Trace** tab: lists, pass by pass, everything the golfing engine changed in the shader, with a before/after view per pass — including passes that made no change, shown grayed out. ## [1.4.0] and [1.4.1] - 2026-07-19 ### Added **Golfing profiles**: save and reload a set of golfing settings (`.ushaderprofile`), plus three ready-to-use built-in profiles (`Maximum`, `Safe`, `None`). ## [1.3.0] and [1.3.1] - 2026-07-19 ### Added **Compressed-size budgets**: the stats panel now also estimates the compressed size (what actually matters for code-golf/demoscene competitions), with presets (Shadertoy, X/Twitter, 4KB, 8KB, 64KB...) and a green/amber/red badge showing whether the budget is met. The application now launches maximized. ## [1.2.0] through [1.2.5] - 2026-07-18 / 2026-07-19 ### Added New **dark interface**, inspired by a professional video-editing application, with a custom title bar. Live-viewport video recording to MP4/WebM now works out of the box right after installing, with nothing extra to install. Two new golfing passes (algebraic identity simplification like `x*1` → `x`, and elimination of redundant computations), each with its own checkbox and counter. ### Fixed Fixed two cases where golfing produced an incorrect result with the new redundant-computation-elimination pass. ## [1.1.0] - 2026-07-18 ### Added **Live-viewport recording** to an animated GIF, and to MP4/WebM if `ffmpeg` is installed on the machine. ### Fixed Opening/saving files whose path contains accented or special characters (for example the &quot;µShader&quot; folder itself). A console window briefly flashed behind the main window on launch; it no longer appears. ## [1.0.0] - 2026-07-18 First stable release, with a dedicated Windows installer (`uShader-Setup-*.exe`). ## [0.8.0] - 2026-07-18 ### Added **About** window (version, copyright, contact links). Application and installer icons. ### Fixed Files whose path contains non-ASCII characters failed to load/save correctly. ## [0.7.0] - 2026-07-18 ### Added **Open/Save** buttons for the source code. **Copy** button for the golfed code. **Export (Shadertoy)** button. **Screenshot** button for the live viewport, exported as PNG. ## [0.6.0] - 2026-07-18 ### Added First golfing controls: an &quot;Aggressive golfing&quot; toggle and individual checkboxes for every transformation pass, plus a field to protect certain names. Stats panel: before/after size, reduction percentage, per-pass counters, budget badges. **Compare** mode: shows the source and golfed shaders side by side to visually confirm they render the same image. ## [0.5.0] - 2026-07-18 ### Added A real text editor for the source and golfed code, with GLSL syntax highlighting. **Formatted view** toggle to read the golfed code across multiple lines. The offending line is highlighted directly in the editor on a compile error. ## [0.4.0] - 2026-07-18 ### Added First complete graphical interface: Source / Golfed / Live viewport panels, resizable. The &quot;Run golf&quot; action actually golfs the code and shows the result. The live viewport shows the golfed shader running, with compile errors visible on screen. ## [0.3.0] - 2026-07-18 First preview: a window displays and animates a demo shader (no editing yet). ## [0.1.0] and [0.2.0] - 2026-07-18 Project kickoff: initial repository and golfing-engine structure, no usable interface yet.</description>
  </item>
  <item>
    <title>uShader v4.5.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=ushader</link>
    <guid isPermaLink="false">ushader-4.5.1</guid>
    <pubDate>Mon, 10 Aug 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to µShader, from the point of view of using the application. ## [Unreleased] ## [4.5.1] - 2026-08-10 ### Fixed The main window now appears before OpenGL viewport creation, shader compilation, and the optional startup update check, making installed launches substantially faster. ## [4.5.0] - 2026-08-08 ### Added **Export → Standalone HTML...** (command palette): writes a single, self-contained `.html` file (inline script, no external resources) that runs the golfed shader in a fullscreen WebGL1 canvas with `iTime`/`iResolution`/`iMouse`/`iDate`/`iFrame`/`iFrameRate` all wired up, for publishing or sharing without going through twigl.app. **Copy as Shadertoy export JSON** (command palette): the golfed shader in Shadertoy&apos;s own JSON export format, ready to paste into shadertoy.com&apos;s &quot;Import&quot; dialog. **Copy as KodeLife**, and **Copy as Bonzomatic fixed** (it previously copied the golfed code unchanged, which does not compile as-is in Bonzomatic): both now rewrite the shader&apos;s uniforms and entry point to match each tool&apos;s real convention, verified against Bonzomatic&apos;s own source and a real shared KodeLife project rather than guessed. **&quot;Golf harder — Exhaustive&quot;**, next to the existing &quot;Deep&quot; toggle: when both are on, searches 4 independent simulated-annealing walks in parallel instead of 1, for a wider search at the same per-walk budget. **Local, no-telemetry crash reporter**: writes a plain-text report (exception info + your last 20 actions) to `%APPDATA%\ushader\crash_reports\` if the app ever crashes. Nothing is ever sent anywhere automatically; use the new **&quot;Copy last crash report&quot;** command to attach it to a GitHub issue yourself. **Opt-in update check**: &quot;Check for updates now&quot; and &quot;Toggle check for updates on startup&quot; in the command palette (off by default). When a newer release is available, &quot;Download and install latest update&quot; fetches the real installer and opens it — your own confirmation is always still required, nothing installs silently. **Copy CHANGELOG.md to clipboard** (command palette). **Portable mode**: drop a `portable.txt` file next to `ushader.exe` and all local settings (recent files, keybindings, golf profiles, crash reports, update setting) are stored in a `config\` folder next to the executable instead of your Windows user profile — no installation, no registry writes, suitable for a USB stick. See `uShader-GUIDE.md` for how to assemble a portable folder. ### Changed The golf engine now catches a few more identity/simplification cases that the older pass-based golfer could not reach on its own (e.g. `(x+0)*1` inside a function call argument), via a new internal equality-saturation (&quot;e-graph&quot;) pass. Off by default for now while it continues to grow; no user-visible toggle yet. Continuous fuzz testing (random option combinations against the whole shader corpus, checked by actually compiling every result) is now part of the test suite, closing a couple of small, previously-unnoticed correctness gaps (a macro-referenced local wrongly eliminated as dead code). ## [4.4.0] - 2026-08-08 ### Added New **value sliders panel**, on the left edge of the Source and Viewport tabs: every float literal in your shader (`1.0`, `0.5`, `.25`, `1e-2`, ...) automatically gets its own draggable slider, no annotation needed. Each row shows a short snippet of the line the value comes from (so several `1.0`s in different places stay distinguishable). Dragging a slider rewrites that exact number in the Source editor in place and recompiles the live viewport as you drag, so lighting, colors, thresholds, speeds, and any other constant can be tuned while watching the result update in real time. A slider&apos;s range is scaled to its starting value (`0` gets `[-1, 1]`; a positive value `x` gets `[0, 2x]`; a negative value `x` gets a symmetric `[2x, -2x]`). Bare integers (loop bounds, array sizes, indices) and numbers inside comments are left alone. ### Fixed The very first version of the sliders panel&apos;s drag handling ran the full recompile (Rust golf engine + up to 4 GL shader compiles) synchronously from the `WM_MOUSEMOVE` handler, which runs *before* that frame&apos;s repaint inside the message loop&apos;s drain step. A slow recompile there stalled the drain loop while the OS kept queueing more mouse-move messages, so the thumb visibly froze while dragging. The recompile is now driven once per rendered frame instead (throttled, and always guaranteed to run between two paints), never blocking the message loop. Releasing a slider drag re-synced the panel&apos;s rows from the newly edited source *before* telling the panel the drag had ended, which made it recompute that row&apos;s range from the value just dragged to -- and since a positive value always sits at exactly the midpoint of its own auto-computed range, every slider snapped straight back to the center the instant you let go, regardless of where you&apos;d dragged it. Reordered so the final commit happens while the panel still reports the drag as in progress. ## [4.3.4] - 2026-08-08 ### Fixed The command palette&apos;s &quot;Import exclude list...&quot; entry didn&apos;t match its actual label (&quot;Import exclude/protected name list...&quot;) in the README. The installer&apos;s fallback version string (used only when `/DMyAppVersion` isn&apos;t passed to Inno Setup) was stuck at a very old `1.6.0.18`; it now tracks the current release. ### Removed `ushader_golf`, the C API&apos;s non-traced golf entry point, was no longer called by the application (only by two C++ tests, which now use `ushader_golf_traced` instead -- a strict superset that just ignores the trace output when passed a null out-param). Removed the dead surface from both `capi.rs` and `golf_core.h`. ### Added Rust unit tests for the FFI boundary in `capi.rs` (9 tests), closing the last gap the 4.3.x accessibility/wiring audit found: the unsafe raw-pointer-array handling in `ushader_encode_gif` had no coverage anywhere, Rust or C++, until now. Rust unit tests for `aggressive.rs`&apos;s one pure, standalone-testable helper (`shortest_scientific_form`); the module&apos;s per-pass functions remain covered end-to-end through `golfer.rs`&apos;s existing test suite. ### Fixed **Ctrl+S** always opened a &quot;Save as...&quot; dialog, even for a document already saved to a known path, and **Ctrl+Shift+S** (&quot;Save As&quot;, as documented in the README) wasn&apos;t bound to anything. Ctrl+S now saves silently to the document&apos;s existing path (falling back to the dialog only for a never-saved document), and Ctrl+Shift+S is a real, separate &quot;Save as...&quot; binding that always prompts. **Golf harder** and **Toggle Deep search** were only reachable by clicking their inspector buttons; they&apos;re now in the command palette (Ctrl+Shift+P) like every other action. The **Diff**, **Trace**, and **Stats** tabs, and the command palette itself, exposed no content to Windows screen readers (Narrator). They now register their text/rows with the app&apos;s UI Automation provider (trace steps as expandable/collapsible items, palette results as selectable items, diff/stats content as readable text). Keybindings and Minimap remain the last two panels without accessibility coverage. ### Fixed The **Viewport** tab still showed the Source/Golfed/Twigl mini previews next to the live viewport, duplicating what those three panels already show on their own tabs and leaving the main viewport needlessly cramped. The Viewport tab now shows only the live viewport, expanded to fill the space the mini previews used to occupy; the mini previews are unaffected on every other tab. ## [4.3.1] - 2026-08-08 ### Fixed MRT Twigl exports (2+ render targets) were not covered by the automatic rename-collision resolution added in 4.3.0: a local variable declared `o0`/`o1` (or `outColor0`/`outColor1`) could collide with the MRT output names without being renamed. Collision detection now also runs for MRT mode, triggering only on an actual local declaration of the colliding name (not on the plain MRT output usage every legitimate MRT shader is expected to contain), to avoid false positives. Screen-reader (Narrator) coverage was documented as complete for every control; the README now accurately states that the Diff, Trace, Stats, Keybindings, Command Palette, and Minimap panels are not yet fully covered. The application did not declare per-monitor DPI awareness, so the UI could render blurry or incorrectly sized on HiDPI displays; it now opts in to `DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2` at startup (fixed-pixel layout scaling remains future work). A stale code comment described GIF capture as unimplemented; it has shipped since 4.1.0 and the comment now reflects that. A `partial_cmp().unwrap()` in the rotation-constant catalogue lookup could panic if given a `NaN` angle; it now falls back to `Equal` instead. ### Added Two new C++ test executables, `ui_pure_logic_test` and `win32_panel_state_test`, covering previously-untested pure-logic modules (fuzzy match, unified diff, GLSL formatting/tokenizing, keybindings storage, recent files, golf-options conversion) and stateful UI panel classes (command palette, diff view, trace view, document tab strip, keybindings, stats panel, appearance panel, minimap). Randomized round-trip tests for the GIF encoder&apos;s LZW dictionary (varying code size, dimensions, frame count, and palette size) guarding the invariants several internal `.expect()` calls rely on. ## [4.3.0] - 2026-08-05 ### Fixed The Twigl export conversion never handled genuine Shadertoy-style `mainImage(out vec4 fragColor, in vec2 fragCoord)` shaders — which is what every real Shadertoy shader (and this app&apos;s own default shader) actually uses — correctly. It silently left the function wrapper untouched, so the Twigl export was effectively broken for real-world shaders regardless of the selected mode. Genuine Shadertoy source now converts correctly to and from every Twigl mode. The **Twigl** tab&apos;s &quot;Import twigl shader&quot; preview box was read-only, making it impossible to paste code copied from twigl.app — it&apos;s now editable, and your pasted text survives switching tabs or editing the Source tab until you click Import. Importing a shader exported in ES 3.00 or MRT mode produced GLSL that failed to compile back in the Source tab (leftover `#version 300 es` header, mismatched output-variable declarations, and — for Geeker/ Geekest — duplicate uniform declarations conflicting with ones the app already provides). Twigl export could silently produce a visually wrong shader when your own code already used a local variable named `r`/`m`/`t`/`f`/`b`/`o`, colliding with the Geek-family uniform/output shorthand (e.g. a raymarching shader&apos;s own grid-size variable named `r` colliding with `iResolution`&apos;s shorthand). Such collisions are now resolved automatically — your identifier is renamed (e.g. `r` becomes `r_0`) and a note in the panel tells you it happened. Golfing a shader before exporting to Twigl could rename an unrelated local variable to `r`/`m`/`t`/`f`/`b`/`o`, colliding with the Twigl uniform shorthand the same way. The mini **Twigl** preview stayed blank with no explanation whenever ES 3.00 or MRT was enabled; its label now says so explicitly instead of looking like a broken/empty preview. The Twigl budget gauge used the generic &quot;X/Twitter shader&quot; byte-budget preset for every mode instead of the mode-specific &quot;Twigl classic&quot;/ &quot;Twigl geekest&quot; preset. ## [4.2.0] - 2026-08-04 ### Added A third live preview, labeled **Twigl**, joins the existing **Source** and **Golfed** ones next to the golfing controls: it shows your shader rendered exactly as twigl.app would run it, updating immediately as you change mode/ES 3.00/MRT/Backbuffer/ Sound in the Twigl Export tab. It stays blank whenever ES 3.00 or MRT is enabled, since those produce WebGL2-only GLSL this desktop viewport can&apos;t run (a known limitation, not a bug). Each of the three previews now shows its own label (&quot;Source&quot;, &quot;Golfed&quot;, &quot;Twigl&quot;) above it, so it&apos;s always clear which is which. ## [4.1.2] - 2026-08-04 ### Fixed The **Twigl** export tab showed a completely empty panel — no mode buttons, no toggles, no Copy/Import buttons, no snippet buttons, and no preview code — making the twigl.app export feature unusable from a normal launch of the application. The panel was never initialized at startup. The byte-budget line on the Twigl tab overlapped the second row of snippet buttons instead of appearing below them. ## [4.1.1] - 2026-08-04 ### Fixed The rotation-constant entries in the **Golf Tips** tab and the &quot;over budget&quot; hint banner showed garbled characters instead of degree signs and dashes. The application now launches maximized, instead of at a small default window size. ## [4.1.0] - 2026-08-04 ### Added New **Capture GIF** action for the live viewport, with three built-in size/duration presets, animated and looping. The **Twigl Export** tab (mode, ES 3.00, MRT, backbuffer, sound) now keeps its own state per document, the same way the golfing profile already did. New **Import twigl shader** button: pastes twigl.app-formatted code and converts it back into a normal, editable shader. The &quot;Copy for twigl.app&quot; button (and the matching command-palette action) now shows a confirmation message after copying. Snippet names from the twigl snippet library are now automatically protected from renaming, avoiding a naming conflict after inserting a snippet. The &quot;Sound&quot; toggle&apos;s accessibility label now states clearly that it refers to a separate program from the one shown, specific to twigl.app. ### Fixed Twigl.app exports in **ES 3.00** mode now use current texture functions (the old ones no longer compiled in a real browser). Multi-target (**MRT**) exports in ES 3.00 now include the layout information WebGL2 requires. The **Backbuffer** and **Sound** toggles in the Twigl export panel now genuinely update the live preview. The command palette&apos;s &quot;Copy as twigl&quot; action now always matches what&apos;s shown on screen (including in MRT mode). Restoring a session on launch now correctly reopens the document and tab that were active when the app was last closed. A document that was intentionally cleared (but not saved) is no longer replaced with the default shader on the next launch. The stats panel now correctly reports whether frequency-aware renaming actually ran on the last pass. Improved the performance of the frequency-aware renaming option used by &quot;Golf harder&quot;. ## [4.0.0] - 2026-08-03 Major release: UI overhaul and twigl.app export. ### Added Tabs are now grouped (Author, Analyze, Export, Settings) for easier navigation. Multi-document editing is back: every open shader gets its own tab, and the whole session (every open document) is restored automatically on the next launch. The command palette now offers one-click shortcuts: copy as Shadertoy / Bonzomatic / bare `main()` / for twigl.app, save/load a golfing profile, import an exclude-name list, capture a PNG screenshot of the live viewport, and export a self-contained HTML session report. New **Twigl Export** tab: converts the golfed shader into the format expected by twigl.app, with a mode picker (Classic/Geek/Geeker/ Geekest), an ES 3.00 toggle, MRT/Backbuffer/Sound toggles, a live preview and a byte-budget gauge, opened via `Ctrl+Alt+T`. ## [3.1.0] - 2026-08-03 ### Added New **Golf harder** button, which automatically searches for an even smaller combination of golfing options than the current settings, with a &quot;Deep search&quot; option. The stats panel gained a new section summarizing the reduction achieved and which passes actually fired. New **Golf Tips** tab: a searchable catalogue of manual reduction techniques (rotation constants, common trigonometric identities, compact noise generators, 3D shape primitives, color gradients, gamma/tonemap corrections) — every entry can be copied or inserted straight into the editor with one click. The &quot;Trace&quot; view now shows a banner whenever the golfed result exceeds the chosen size budget, linking directly to the Golf Tips tab. The golfing controls panel is now an always-visible side panel instead of a full-screen tab, with a &quot;Golf&quot; button and a &quot;Formatted view&quot; button directly accessible. ## [3.0.0] - 2026-07-22 Major change: the application was entirely rebuilt on a native Windows interface — lighter, with no external runtime to install. ### Changed The look and shortcuts stay the same, but a few features from the previous version were not yet ported to this new interface: golfing profiles (save/load), multi-document editing and session restore, &quot;Copy as Shadertoy/Bonzomatic/main()&quot; shortcuts, exclude-name-list import, HTML session report, PNG screenshot, and live-viewport video/GIF recording. These were reintroduced in versions 3.1.0/4.0.0 above, except video/GIF recording (GIF returned in the Unreleased section above). ## [2.1.0] through [2.21.0] - 2026-07-22 Groundwork for the new native interface (see 3.0.0): no visible change in the shipped application during this period, except for the points below. ### Added Support for Windows screen readers (Narrator and similar) on the title bar buttons and checkboxes. An internal color-contrast check ensures text stays readable according to accessibility standards (WCAG AA). **Colorblind-safe status indicators** checkbox (different shapes in addition to colors) in the Appearance tab. **UI text size** slider (13 to 28 pt) in the Appearance tab, with automatic scaling based on the display. Import of a Shader Minifier–style exclude-name list. Three one-click copy shortcuts: &quot;Copy as Shadertoy&quot;, &quot;Copy as Bonzomatic&quot;, &quot;Copy as bare main()&quot;. The golfing profile format (`.ushaderprofile`) is now documented and versioned, for anyone who wants to generate or read one outside the application. ### Fixed Fixed a bug preventing screen readers from correctly detecting UI elements. ## [2.0.0] and [2.0.1] - 2026-07-19 / 2026-07-20 ### Added Export a **session report** as a self-contained HTML file (works offline, no connection needed), including the source code, the golfed code, the reduction statistics, and optionally a screenshot of the live viewport. Drag-and-drop a `.glsl` file onto the window to open it directly in a new tab. **Recent files** list, available from the File menu; entries pointing at a deleted file are removed automatically. ## [1.9.0] and [1.9.1] - 2026-07-19 ### Added **Command palette** (`Ctrl+Shift+P`): fuzzy search across every action in the app (run golf, switch tabs, toggle a pass, load/save a profile, toggle Compare mode, export...). **Customizable keyboard shortcuts** for the command palette, new tab, open, save, and close-tab, from a new &quot;Keyboard Shortcuts&quot; tab. **Minimap** for the Source and Golfed editors: a compact, colored overview of the file, useful on longer shaders. New **Diff** tab: shows at a glance what changed between the source code and the golfed code, removed text struck through in red, added text in green. ## [1.8.0] - 2026-07-19 ### Added **Command-line tool** for automatically golfing a whole folder of shaders (useful for plugging into a game or demo build pipeline), with size-budget options, a detailed report, and a preview of the differences before applying. ## [1.7.0] - 2026-07-19 ### Added **Multi-document editing**: one tab per open `.glsl` file, each with its own golfing settings. The session (every open document) is now **saved and restored automatically** on launch, with a confirmation before reopening files. A dot appears on the tab of a document with unsaved changes, and a confirmation is asked before closing it or quitting the app with unsaved changes. ## [1.6.0] - 2026-07-19 Internal groundwork for the automatic verification feature (Compare mode): no visible change yet. ## [1.5.0] - 2026-07-19 ### Added New **Trace** tab: lists, pass by pass, everything the golfing engine changed in the shader, with a before/after view per pass — including passes that made no change, shown grayed out. ## [1.4.0] and [1.4.1] - 2026-07-19 ### Added **Golfing profiles**: save and reload a set of golfing settings (`.ushaderprofile`), plus three ready-to-use built-in profiles (`Maximum`, `Safe`, `None`). ## [1.3.0] and [1.3.1] - 2026-07-19 ### Added **Compressed-size budgets**: the stats panel now also estimates the compressed size (what actually matters for code-golf/demoscene competitions), with presets (Shadertoy, X/Twitter, 4KB, 8KB, 64KB...) and a green/amber/red badge showing whether the budget is met. The application now launches maximized. ## [1.2.0] through [1.2.5] - 2026-07-18 / 2026-07-19 ### Added New **dark interface**, inspired by a professional video-editing application, with a custom title bar. Live-viewport video recording to MP4/WebM now works out of the box right after installing, with nothing extra to install. Two new golfing passes (algebraic identity simplification like `x*1` → `x`, and elimination of redundant computations), each with its own checkbox and counter. ### Fixed Fixed two cases where golfing produced an incorrect result with the new redundant-computation-elimination pass. ## [1.1.0] - 2026-07-18 ### Added **Live-viewport recording** to an animated GIF, and to MP4/WebM if `ffmpeg` is installed on the machine. ### Fixed Opening/saving files whose path contains accented or special characters (for example the &quot;µShader&quot; folder itself). A console window briefly flashed behind the main window on launch; it no longer appears. ## [1.0.0] - 2026-07-18 First stable release, with a dedicated Windows installer (`uShader-Setup-*.exe`). ## [0.8.0] - 2026-07-18 ### Added **About** window (version, copyright, contact links). Application and installer icons. ### Fixed Files whose path contains non-ASCII characters failed to load/save correctly. ## [0.7.0] - 2026-07-18 ### Added **Open/Save** buttons for the source code. **Copy** button for the golfed code. **Export (Shadertoy)** button. **Screenshot** button for the live viewport, exported as PNG. ## [0.6.0] - 2026-07-18 ### Added First golfing controls: an &quot;Aggressive golfing&quot; toggle and individual checkboxes for every transformation pass, plus a field to protect certain names. Stats panel: before/after size, reduction percentage, per-pass counters, budget badges. **Compare** mode: shows the source and golfed shaders side by side to visually confirm they render the same image. ## [0.5.0] - 2026-07-18 ### Added A real text editor for the source and golfed code, with GLSL syntax highlighting. **Formatted view** toggle to read the golfed code across multiple lines. The offending line is highlighted directly in the editor on a compile error. ## [0.4.0] - 2026-07-18 ### Added First complete graphical interface: Source / Golfed / Live viewport panels, resizable. The &quot;Run golf&quot; action actually golfs the code and shows the result. The live viewport shows the golfed shader running, with compile errors visible on screen. ## [0.3.0] - 2026-07-18 First preview: a window displays and animates a demo shader (no editing yet). ## [0.1.0] and [0.2.0] - 2026-07-18 Project kickoff: initial repository and golfing-engine structure, no usable interface yet.</description>
  </item>
  <item>
    <title>uShader v4.5.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=ushader</link>
    <guid isPermaLink="false">ushader-4.5.0</guid>
    <pubDate>Sun, 09 Aug 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to µShader, from the point of view of using the application. ## [Unreleased] ## [4.5.0] - 2026-08-08 ### Added **Export → Standalone HTML...** (command palette): writes a single, self-contained `.html` file (inline script, no external resources) that runs the golfed shader in a fullscreen WebGL1 canvas with `iTime`/`iResolution`/`iMouse`/`iDate`/`iFrame`/`iFrameRate` all wired up, for publishing or sharing without going through twigl.app. **Copy as Shadertoy export JSON** (command palette): the golfed shader in Shadertoy&apos;s own JSON export format, ready to paste into shadertoy.com&apos;s &quot;Import&quot; dialog. **Copy as KodeLife**, and **Copy as Bonzomatic fixed** (it previously copied the golfed code unchanged, which does not compile as-is in Bonzomatic): both now rewrite the shader&apos;s uniforms and entry point to match each tool&apos;s real convention, verified against Bonzomatic&apos;s own source and a real shared KodeLife project rather than guessed. **&quot;Golf harder — Exhaustive&quot;**, next to the existing &quot;Deep&quot; toggle: when both are on, searches 4 independent simulated-annealing walks in parallel instead of 1, for a wider search at the same per-walk budget. **Local, no-telemetry crash reporter**: writes a plain-text report (exception info + your last 20 actions) to `%APPDATA%\ushader\crash_reports\` if the app ever crashes. Nothing is ever sent anywhere automatically; use the new **&quot;Copy last crash report&quot;** command to attach it to a GitHub issue yourself. **Opt-in update check**: &quot;Check for updates now&quot; and &quot;Toggle check for updates on startup&quot; in the command palette (off by default). When a newer release is available, &quot;Download and install latest update&quot; fetches the real installer and opens it — your own confirmation is always still required, nothing installs silently. **Copy CHANGELOG.md to clipboard** (command palette). **Portable mode**: drop a `portable.txt` file next to `ushader.exe` and all local settings (recent files, keybindings, golf profiles, crash reports, update setting) are stored in a `config\` folder next to the executable instead of your Windows user profile — no installation, no registry writes, suitable for a USB stick. See `uShader-GUIDE.md` for how to assemble a portable folder. ### Changed The golf engine now catches a few more identity/simplification cases that the older pass-based golfer could not reach on its own (e.g. `(x+0)*1` inside a function call argument), via a new internal equality-saturation (&quot;e-graph&quot;) pass. Off by default for now while it continues to grow; no user-visible toggle yet. Continuous fuzz testing (random option combinations against the whole shader corpus, checked by actually compiling every result) is now part of the test suite, closing a couple of small, previously-unnoticed correctness gaps (a macro-referenced local wrongly eliminated as dead code). ## [4.4.0] - 2026-08-08 ### Added New **value sliders panel**, on the left edge of the Source and Viewport tabs: every float literal in your shader (`1.0`, `0.5`, `.25`, `1e-2`, ...) automatically gets its own draggable slider, no annotation needed. Each row shows a short snippet of the line the value comes from (so several `1.0`s in different places stay distinguishable). Dragging a slider rewrites that exact number in the Source editor in place and recompiles the live viewport as you drag, so lighting, colors, thresholds, speeds, and any other constant can be tuned while watching the result update in real time. A slider&apos;s range is scaled to its starting value (`0` gets `[-1, 1]`; a positive value `x` gets `[0, 2x]`; a negative value `x` gets a symmetric `[2x, -2x]`). Bare integers (loop bounds, array sizes, indices) and numbers inside comments are left alone. ### Fixed The very first version of the sliders panel&apos;s drag handling ran the full recompile (Rust golf engine + up to 4 GL shader compiles) synchronously from the `WM_MOUSEMOVE` handler, which runs *before* that frame&apos;s repaint inside the message loop&apos;s drain step. A slow recompile there stalled the drain loop while the OS kept queueing more mouse-move messages, so the thumb visibly froze while dragging. The recompile is now driven once per rendered frame instead (throttled, and always guaranteed to run between two paints), never blocking the message loop. Releasing a slider drag re-synced the panel&apos;s rows from the newly edited source *before* telling the panel the drag had ended, which made it recompute that row&apos;s range from the value just dragged to -- and since a positive value always sits at exactly the midpoint of its own auto-computed range, every slider snapped straight back to the center the instant you let go, regardless of where you&apos;d dragged it. Reordered so the final commit happens while the panel still reports the drag as in progress. ## [4.3.4] - 2026-08-08 ### Fixed The command palette&apos;s &quot;Import exclude list...&quot; entry didn&apos;t match its actual label (&quot;Import exclude/protected name list...&quot;) in the README. The installer&apos;s fallback version string (used only when `/DMyAppVersion` isn&apos;t passed to Inno Setup) was stuck at a very old `1.6.0.18`; it now tracks the current release. ### Removed `ushader_golf`, the C API&apos;s non-traced golf entry point, was no longer called by the application (only by two C++ tests, which now use `ushader_golf_traced` instead -- a strict superset that just ignores the trace output when passed a null out-param). Removed the dead surface from both `capi.rs` and `golf_core.h`. ### Added Rust unit tests for the FFI boundary in `capi.rs` (9 tests), closing the last gap the 4.3.x accessibility/wiring audit found: the unsafe raw-pointer-array handling in `ushader_encode_gif` had no coverage anywhere, Rust or C++, until now. Rust unit tests for `aggressive.rs`&apos;s one pure, standalone-testable helper (`shortest_scientific_form`); the module&apos;s per-pass functions remain covered end-to-end through `golfer.rs`&apos;s existing test suite. ### Fixed **Ctrl+S** always opened a &quot;Save as...&quot; dialog, even for a document already saved to a known path, and **Ctrl+Shift+S** (&quot;Save As&quot;, as documented in the README) wasn&apos;t bound to anything. Ctrl+S now saves silently to the document&apos;s existing path (falling back to the dialog only for a never-saved document), and Ctrl+Shift+S is a real, separate &quot;Save as...&quot; binding that always prompts. **Golf harder** and **Toggle Deep search** were only reachable by clicking their inspector buttons; they&apos;re now in the command palette (Ctrl+Shift+P) like every other action. The **Diff**, **Trace**, and **Stats** tabs, and the command palette itself, exposed no content to Windows screen readers (Narrator). They now register their text/rows with the app&apos;s UI Automation provider (trace steps as expandable/collapsible items, palette results as selectable items, diff/stats content as readable text). Keybindings and Minimap remain the last two panels without accessibility coverage. ### Fixed The **Viewport** tab still showed the Source/Golfed/Twigl mini previews next to the live viewport, duplicating what those three panels already show on their own tabs and leaving the main viewport needlessly cramped. The Viewport tab now shows only the live viewport, expanded to fill the space the mini previews used to occupy; the mini previews are unaffected on every other tab. ## [4.3.1] - 2026-08-08 ### Fixed MRT Twigl exports (2+ render targets) were not covered by the automatic rename-collision resolution added in 4.3.0: a local variable declared `o0`/`o1` (or `outColor0`/`outColor1`) could collide with the MRT output names without being renamed. Collision detection now also runs for MRT mode, triggering only on an actual local declaration of the colliding name (not on the plain MRT output usage every legitimate MRT shader is expected to contain), to avoid false positives. Screen-reader (Narrator) coverage was documented as complete for every control; the README now accurately states that the Diff, Trace, Stats, Keybindings, Command Palette, and Minimap panels are not yet fully covered. The application did not declare per-monitor DPI awareness, so the UI could render blurry or incorrectly sized on HiDPI displays; it now opts in to `DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2` at startup (fixed-pixel layout scaling remains future work). A stale code comment described GIF capture as unimplemented; it has shipped since 4.1.0 and the comment now reflects that. A `partial_cmp().unwrap()` in the rotation-constant catalogue lookup could panic if given a `NaN` angle; it now falls back to `Equal` instead. ### Added Two new C++ test executables, `ui_pure_logic_test` and `win32_panel_state_test`, covering previously-untested pure-logic modules (fuzzy match, unified diff, GLSL formatting/tokenizing, keybindings storage, recent files, golf-options conversion) and stateful UI panel classes (command palette, diff view, trace view, document tab strip, keybindings, stats panel, appearance panel, minimap). Randomized round-trip tests for the GIF encoder&apos;s LZW dictionary (varying code size, dimensions, frame count, and palette size) guarding the invariants several internal `.expect()` calls rely on. ## [4.3.0] - 2026-08-05 ### Fixed The Twigl export conversion never handled genuine Shadertoy-style `mainImage(out vec4 fragColor, in vec2 fragCoord)` shaders — which is what every real Shadertoy shader (and this app&apos;s own default shader) actually uses — correctly. It silently left the function wrapper untouched, so the Twigl export was effectively broken for real-world shaders regardless of the selected mode. Genuine Shadertoy source now converts correctly to and from every Twigl mode. The **Twigl** tab&apos;s &quot;Import twigl shader&quot; preview box was read-only, making it impossible to paste code copied from twigl.app — it&apos;s now editable, and your pasted text survives switching tabs or editing the Source tab until you click Import. Importing a shader exported in ES 3.00 or MRT mode produced GLSL that failed to compile back in the Source tab (leftover `#version 300 es` header, mismatched output-variable declarations, and — for Geeker/ Geekest — duplicate uniform declarations conflicting with ones the app already provides). Twigl export could silently produce a visually wrong shader when your own code already used a local variable named `r`/`m`/`t`/`f`/`b`/`o`, colliding with the Geek-family uniform/output shorthand (e.g. a raymarching shader&apos;s own grid-size variable named `r` colliding with `iResolution`&apos;s shorthand). Such collisions are now resolved automatically — your identifier is renamed (e.g. `r` becomes `r_0`) and a note in the panel tells you it happened. Golfing a shader before exporting to Twigl could rename an unrelated local variable to `r`/`m`/`t`/`f`/`b`/`o`, colliding with the Twigl uniform shorthand the same way. The mini **Twigl** preview stayed blank with no explanation whenever ES 3.00 or MRT was enabled; its label now says so explicitly instead of looking like a broken/empty preview. The Twigl budget gauge used the generic &quot;X/Twitter shader&quot; byte-budget preset for every mode instead of the mode-specific &quot;Twigl classic&quot;/ &quot;Twigl geekest&quot; preset. ## [4.2.0] - 2026-08-04 ### Added A third live preview, labeled **Twigl**, joins the existing **Source** and **Golfed** ones next to the golfing controls: it shows your shader rendered exactly as twigl.app would run it, updating immediately as you change mode/ES 3.00/MRT/Backbuffer/ Sound in the Twigl Export tab. It stays blank whenever ES 3.00 or MRT is enabled, since those produce WebGL2-only GLSL this desktop viewport can&apos;t run (a known limitation, not a bug). Each of the three previews now shows its own label (&quot;Source&quot;, &quot;Golfed&quot;, &quot;Twigl&quot;) above it, so it&apos;s always clear which is which. ## [4.1.2] - 2026-08-04 ### Fixed The **Twigl** export tab showed a completely empty panel — no mode buttons, no toggles, no Copy/Import buttons, no snippet buttons, and no preview code — making the twigl.app export feature unusable from a normal launch of the application. The panel was never initialized at startup. The byte-budget line on the Twigl tab overlapped the second row of snippet buttons instead of appearing below them. ## [4.1.1] - 2026-08-04 ### Fixed The rotation-constant entries in the **Golf Tips** tab and the &quot;over budget&quot; hint banner showed garbled characters instead of degree signs and dashes. The application now launches maximized, instead of at a small default window size. ## [4.1.0] - 2026-08-04 ### Added New **Capture GIF** action for the live viewport, with three built-in size/duration presets, animated and looping. The **Twigl Export** tab (mode, ES 3.00, MRT, backbuffer, sound) now keeps its own state per document, the same way the golfing profile already did. New **Import twigl shader** button: pastes twigl.app-formatted code and converts it back into a normal, editable shader. The &quot;Copy for twigl.app&quot; button (and the matching command-palette action) now shows a confirmation message after copying. Snippet names from the twigl snippet library are now automatically protected from renaming, avoiding a naming conflict after inserting a snippet. The &quot;Sound&quot; toggle&apos;s accessibility label now states clearly that it refers to a separate program from the one shown, specific to twigl.app. ### Fixed Twigl.app exports in **ES 3.00** mode now use current texture functions (the old ones no longer compiled in a real browser). Multi-target (**MRT**) exports in ES 3.00 now include the layout information WebGL2 requires. The **Backbuffer** and **Sound** toggles in the Twigl export panel now genuinely update the live preview. The command palette&apos;s &quot;Copy as twigl&quot; action now always matches what&apos;s shown on screen (including in MRT mode). Restoring a session on launch now correctly reopens the document and tab that were active when the app was last closed. A document that was intentionally cleared (but not saved) is no longer replaced with the default shader on the next launch. The stats panel now correctly reports whether frequency-aware renaming actually ran on the last pass. Improved the performance of the frequency-aware renaming option used by &quot;Golf harder&quot;. ## [4.0.0] - 2026-08-03 Major release: UI overhaul and twigl.app export. ### Added Tabs are now grouped (Author, Analyze, Export, Settings) for easier navigation. Multi-document editing is back: every open shader gets its own tab, and the whole session (every open document) is restored automatically on the next launch. The command palette now offers one-click shortcuts: copy as Shadertoy / Bonzomatic / bare `main()` / for twigl.app, save/load a golfing profile, import an exclude-name list, capture a PNG screenshot of the live viewport, and export a self-contained HTML session report. New **Twigl Export** tab: converts the golfed shader into the format expected by twigl.app, with a mode picker (Classic/Geek/Geeker/ Geekest), an ES 3.00 toggle, MRT/Backbuffer/Sound toggles, a live preview and a byte-budget gauge, opened via `Ctrl+Alt+T`. ## [3.1.0] - 2026-08-03 ### Added New **Golf harder** button, which automatically searches for an even smaller combination of golfing options than the current settings, with a &quot;Deep search&quot; option. The stats panel gained a new section summarizing the reduction achieved and which passes actually fired. New **Golf Tips** tab: a searchable catalogue of manual reduction techniques (rotation constants, common trigonometric identities, compact noise generators, 3D shape primitives, color gradients, gamma/tonemap corrections) — every entry can be copied or inserted straight into the editor with one click. The &quot;Trace&quot; view now shows a banner whenever the golfed result exceeds the chosen size budget, linking directly to the Golf Tips tab. The golfing controls panel is now an always-visible side panel instead of a full-screen tab, with a &quot;Golf&quot; button and a &quot;Formatted view&quot; button directly accessible. ## [3.0.0] - 2026-07-22 Major change: the application was entirely rebuilt on a native Windows interface — lighter, with no external runtime to install. ### Changed The look and shortcuts stay the same, but a few features from the previous version were not yet ported to this new interface: golfing profiles (save/load), multi-document editing and session restore, &quot;Copy as Shadertoy/Bonzomatic/main()&quot; shortcuts, exclude-name-list import, HTML session report, PNG screenshot, and live-viewport video/GIF recording. These were reintroduced in versions 3.1.0/4.0.0 above, except video/GIF recording (GIF returned in the Unreleased section above). ## [2.1.0] through [2.21.0] - 2026-07-22 Groundwork for the new native interface (see 3.0.0): no visible change in the shipped application during this period, except for the points below. ### Added Support for Windows screen readers (Narrator and similar) on the title bar buttons and checkboxes. An internal color-contrast check ensures text stays readable according to accessibility standards (WCAG AA). **Colorblind-safe status indicators** checkbox (different shapes in addition to colors) in the Appearance tab. **UI text size** slider (13 to 28 pt) in the Appearance tab, with automatic scaling based on the display. Import of a Shader Minifier–style exclude-name list. Three one-click copy shortcuts: &quot;Copy as Shadertoy&quot;, &quot;Copy as Bonzomatic&quot;, &quot;Copy as bare main()&quot;. The golfing profile format (`.ushaderprofile`) is now documented and versioned, for anyone who wants to generate or read one outside the application. ### Fixed Fixed a bug preventing screen readers from correctly detecting UI elements. ## [2.0.0] and [2.0.1] - 2026-07-19 / 2026-07-20 ### Added Export a **session report** as a self-contained HTML file (works offline, no connection needed), including the source code, the golfed code, the reduction statistics, and optionally a screenshot of the live viewport. Drag-and-drop a `.glsl` file onto the window to open it directly in a new tab. **Recent files** list, available from the File menu; entries pointing at a deleted file are removed automatically. ## [1.9.0] and [1.9.1] - 2026-07-19 ### Added **Command palette** (`Ctrl+Shift+P`): fuzzy search across every action in the app (run golf, switch tabs, toggle a pass, load/save a profile, toggle Compare mode, export...). **Customizable keyboard shortcuts** for the command palette, new tab, open, save, and close-tab, from a new &quot;Keyboard Shortcuts&quot; tab. **Minimap** for the Source and Golfed editors: a compact, colored overview of the file, useful on longer shaders. New **Diff** tab: shows at a glance what changed between the source code and the golfed code, removed text struck through in red, added text in green. ## [1.8.0] - 2026-07-19 ### Added **Command-line tool** for automatically golfing a whole folder of shaders (useful for plugging into a game or demo build pipeline), with size-budget options, a detailed report, and a preview of the differences before applying. ## [1.7.0] - 2026-07-19 ### Added **Multi-document editing**: one tab per open `.glsl` file, each with its own golfing settings. The session (every open document) is now **saved and restored automatically** on launch, with a confirmation before reopening files. A dot appears on the tab of a document with unsaved changes, and a confirmation is asked before closing it or quitting the app with unsaved changes. ## [1.6.0] - 2026-07-19 Internal groundwork for the automatic verification feature (Compare mode): no visible change yet. ## [1.5.0] - 2026-07-19 ### Added New **Trace** tab: lists, pass by pass, everything the golfing engine changed in the shader, with a before/after view per pass — including passes that made no change, shown grayed out. ## [1.4.0] and [1.4.1] - 2026-07-19 ### Added **Golfing profiles**: save and reload a set of golfing settings (`.ushaderprofile`), plus three ready-to-use built-in profiles (`Maximum`, `Safe`, `None`). ## [1.3.0] and [1.3.1] - 2026-07-19 ### Added **Compressed-size budgets**: the stats panel now also estimates the compressed size (what actually matters for code-golf/demoscene competitions), with presets (Shadertoy, X/Twitter, 4KB, 8KB, 64KB...) and a green/amber/red badge showing whether the budget is met. The application now launches maximized. ## [1.2.0] through [1.2.5] - 2026-07-18 / 2026-07-19 ### Added New **dark interface**, inspired by a professional video-editing application, with a custom title bar. Live-viewport video recording to MP4/WebM now works out of the box right after installing, with nothing extra to install. Two new golfing passes (algebraic identity simplification like `x*1` → `x`, and elimination of redundant computations), each with its own checkbox and counter. ### Fixed Fixed two cases where golfing produced an incorrect result with the new redundant-computation-elimination pass. ## [1.1.0] - 2026-07-18 ### Added **Live-viewport recording** to an animated GIF, and to MP4/WebM if `ffmpeg` is installed on the machine. ### Fixed Opening/saving files whose path contains accented or special characters (for example the &quot;µShader&quot; folder itself). A console window briefly flashed behind the main window on launch; it no longer appears. ## [1.0.0] - 2026-07-18 First stable release, with a dedicated Windows installer (`uShader-Setup-*.exe`). ## [0.8.0] - 2026-07-18 ### Added **About** window (version, copyright, contact links). Application and installer icons. ### Fixed Files whose path contains non-ASCII characters failed to load/save correctly. ## [0.7.0] - 2026-07-18 ### Added **Open/Save** buttons for the source code. **Copy** button for the golfed code. **Export (Shadertoy)** button. **Screenshot** button for the live viewport, exported as PNG. ## [0.6.0] - 2026-07-18 ### Added First golfing controls: an &quot;Aggressive golfing&quot; toggle and individual checkboxes for every transformation pass, plus a field to protect certain names. Stats panel: before/after size, reduction percentage, per-pass counters, budget badges. **Compare** mode: shows the source and golfed shaders side by side to visually confirm they render the same image. ## [0.5.0] - 2026-07-18 ### Added A real text editor for the source and golfed code, with GLSL syntax highlighting. **Formatted view** toggle to read the golfed code across multiple lines. The offending line is highlighted directly in the editor on a compile error. ## [0.4.0] - 2026-07-18 ### Added First complete graphical interface: Source / Golfed / Live viewport panels, resizable. The &quot;Run golf&quot; action actually golfs the code and shows the result. The live viewport shows the golfed shader running, with compile errors visible on screen. ## [0.3.0] - 2026-07-18 First preview: a window displays and animates a demo shader (no editing yet). ## [0.1.0] and [0.2.0] - 2026-07-18 Project kickoff: initial repository and golfing-engine structure, no usable interface yet.</description>
  </item>
  <item>
    <title>Sliders-GL v1.0.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=sliders-gl</link>
    <guid isPermaLink="false">sliders-gl-1.0.1</guid>
    <pubDate>Tue, 28 Jul 2026 00:00:00 GMT</pubDate>
    <description>## Sliders GL v1.0.1 ### Fixed Accessibility: fixed several WCAG 2.1 AA color-contrast issues across the gray theme — control borders, status toasts, the active pass/workspace tabs, viewport HUD readouts, and status-bar text now all meet or exceed the required contrast ratios. Accessibility: the Outliner and Inspector panels are now reachable and scrollable by keyboard. See [CHANGELOG.md](https://github.com/Patrickjaillet/Sliders-GL/blob/main/CHANGELOG.md) for the full history.</description>
  </item>
  <item>
    <title>Sliders-GL v1.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=sliders-gl</link>
    <guid isPermaLink="false">sliders-gl-1.0.0</guid>
    <pubDate>Sun, 26 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to Sliders GL are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [1.0.0] — 2026-07-26 ### Added Initial public release of **Sliders GL**, a full rebrand of the Z-GL Shadertoy project into a standalone, Blender-style GLSL shader editor. Blender-identical UI architecture: topbar, tool shelf, tabbed Properties editor, status bar, draggable/collapsible areas, and area-split gizmos. Unique light/medium-gray theme. Ultra-professional, high-precision sliders for every shader uniform, with keyboard fine control and undo/redo. Monaco-based GLSL editor with syntax highlighting, inline diagnostics, autocomplete, and code formatting. ShaderToy-compatible rendering (`mainImage`, `iResolution`, `iTime`, `iMouse`, channel uniforms) and direct ShaderToy import by ID or URL. Export pipeline: current-frame image export, video recording, standalone HTML export, and full project ZIP export. Shader library and preset system. In-app Help Center with a full reference and keyboard shortcut list. &quot;About&quot; tab with copyright, contact, and website information. Offline-first architecture: all runtimes, fonts, and tooling are bundled locally — zero network dependency for core functionality. MIT license. [1.0.0]: https://github.com/Patrickjaillet/Sliders-GL/releases/tag/v1.0.0</description>
  </item>
  <item>
    <title>Numis-Euro v5.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=numis-euro</link>
    <guid isPermaLink="false">numis-euro-5.0.0</guid>
    <pubDate>Mon, 15 Jun 2026 00:00:00 GMT</pubDate>
    <description>No release notes provided.</description>
  </item>
</channel>
</rss>
