<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
<channel>
  <title>Sandefjord Software — What's New</title>
  <link>https://patrickjaillet.github.io/sandefjord-software/whats-new.html</link>
  <description>Every release from every Sandefjord Software application, newest first.</description>
  <language>en</language>
  <item>
    <title>MicroShader v4.5.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.5.0</guid>
    <pubDate>Sat, 08 Aug 2026 00:00:00 GMT</pubDate>
    <description>## [4.5.0] - 2026-08-08 ### Added **Export → Standalone HTML...** (command palette): writes a single, self-contained `.html` file (inline script, no external resources) that runs the golfed shader in a fullscreen WebGL1 canvas with `iTime`/`iResolution`/`iMouse`/`iDate`/`iFrame`/`iFrameRate` all wired up, for publishing or sharing without going through twigl.app. **Copy as Shadertoy export JSON** (command palette): the golfed shader in Shadertoy&apos;s own JSON export format, ready to paste into shadertoy.com&apos;s &quot;Import&quot; dialog. **Copy as KodeLife**, and **Copy as Bonzomatic fixed** (it previously copied the golfed code unchanged, which does not compile as-is in Bonzomatic): both now rewrite the shader&apos;s uniforms and entry point to match each tool&apos;s real convention, verified against Bonzomatic&apos;s own source and a real shared KodeLife project rather than guessed. **&quot;Golf harder — Exhaustive&quot;**, next to the existing &quot;Deep&quot; toggle: when both are on, searches 4 independent simulated-annealing walks in parallel instead of 1, for a wider search at the same per-walk budget. **Local, no-telemetry crash reporter**: writes a plain-text report (exception info + your last 20 actions) to `%APPDATA%\ushader\crash_reports\` if the app ever crashes. Nothing is ever sent anywhere automatically; use the new **&quot;Copy last crash report&quot;** command to attach it to a GitHub issue yourself. **Opt-in update check**: &quot;Check for updates now&quot; and &quot;Toggle check for updates on startup&quot; in the command palette (off by default). When a newer release is available, &quot;Download and install latest update&quot; fetches the real installer and opens it — your own confirmation is always still required, nothing installs silently. **Copy CHANGELOG.md to clipboard** (command palette). **Portable mode**: drop a `portable.txt` file next to `ushader.exe` and all local settings (recent files, keybindings, golf profiles, crash reports, update setting) are stored in a `config\` folder next to the executable instead of your Windows user profile — no installation, no registry writes, suitable for a USB stick. See `uShader-GUIDE.md` for how to assemble a portable folder. ### Changed The golf engine now catches a few more identity/simplification cases that the older pass-based golfer could not reach on its own (e.g. `(x+0)*1` inside a function call argument), via a new internal equality-saturation (&quot;e-graph&quot;) pass. Off by default for now while it continues to grow; no user-visible toggle yet. Continuous fuzz testing (random option combinations against the whole shader corpus, checked by actually compiling every result) is now part of the test suite, closing a couple of small, previously-unnoticed correctness gaps (a macro-referenced local wrongly eliminated as dead code).</description>
  </item>
  <item>
    <title>MicroShader v4.4.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.4.0</guid>
    <pubDate>Sat, 08 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Added New **value sliders panel**, on the left edge of the Source and Viewport tabs: every float literal in your shader (`1.0`, `0.5`, `.25`, `1e-2`, ...) automatically gets its own draggable slider, no annotation needed. Each row shows a short snippet of the line the value comes from (so several `1.0`s in different places stay distinguishable). Dragging a slider rewrites that exact number in the Source editor in place and recompiles the live viewport as you drag, so lighting, colors, thresholds, speeds, and any other constant can be tuned while watching the result update in real time. A slider&apos;s range is scaled to its starting value (`0` gets `[-1, 1]`; a positive value `x` gets `[0, 2x]`; a negative value `x` gets a symmetric `[2x, -2x]`). Bare integers (loop bounds, array sizes, indices) and numbers inside comments are left alone. ### Fixed The very first version of the sliders panel&apos;s drag handling ran the full recompile (Rust golf engine + up to 4 GL shader compiles) synchronously from the `WM_MOUSEMOVE` handler, which could stall the message loop long enough to visibly freeze the thumb mid-drag. The recompile is now driven once per rendered frame instead. Releasing a slider drag re-synced the panel&apos;s rows -- recomputing each slider&apos;s range from the value just dragged to -- before marking the drag as over, which snapped every slider straight back to the center of its own range on release regardless of where it had been dragged. Fixed by reordering the release handling.</description>
  </item>
  <item>
    <title>MicroShader v4.3.4</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.3.4</guid>
    <pubDate>Sat, 08 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Fixed The command palette&apos;s &quot;Import exclude list...&quot; entry didn&apos;t match its actual label (&quot;Import exclude/protected name list...&quot;) in the README. The installer&apos;s fallback version string (used only when `/DMyAppVersion` isn&apos;t passed to Inno Setup) was stuck at a very old `1.6.0.18`; it now tracks the current release. ### Removed `ushader_golf`, the C API&apos;s non-traced golf entry point, was no longer called by the application (only by two C++ tests, which now use `ushader_golf_traced` instead -- a strict superset that just ignores the trace output when passed a null out-param). Removed the dead surface from both `capi.rs` and `golf_core.h`. ### Added Rust unit tests for the FFI boundary in `capi.rs` (9 tests), closing the last gap the 4.3.x accessibility/wiring audit found: the unsafe raw-pointer-array handling in `ushader_encode_gif` had no coverage anywhere, Rust or C++, until now. Rust unit tests for `aggressive.rs`&apos;s one pure, standalone-testable helper (`shortest_scientific_form`); the module&apos;s per-pass functions remain covered end-to-end through `golfer.rs`&apos;s existing test suite.</description>
  </item>
  <item>
    <title>MicroShader v4.3.3</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.3.3</guid>
    <pubDate>Sat, 08 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Fixed **Ctrl+S** always opened a &quot;Save as...&quot; dialog, even for a document already saved to a known path, and **Ctrl+Shift+S** (&quot;Save As&quot;, as documented in the README) wasn&apos;t bound to anything. Ctrl+S now saves silently to the document&apos;s existing path (falling back to the dialog only for a never-saved document), and Ctrl+Shift+S is a real, separate &quot;Save as...&quot; binding that always prompts. **Golf harder** and **Toggle Deep search** were only reachable by clicking their inspector buttons; they&apos;re now in the command palette (Ctrl+Shift+P) like every other action. The **Diff**, **Trace**, and **Stats** tabs, and the command palette itself, exposed no content to Windows screen readers (Narrator). They now register their text/rows with the app&apos;s UI Automation provider (trace steps as expandable/collapsible items, palette results as selectable items, diff/stats content as readable text). Keybindings and Minimap remain the last two panels without accessibility coverage.</description>
  </item>
  <item>
    <title>MicroShader v4.3.2</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.3.2</guid>
    <pubDate>Sat, 08 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Fixed The **Viewport** tab still showed the Source/Golfed/Twigl mini previews next to the live viewport, duplicating what those three panels already show on their own tabs and leaving the main viewport needlessly cramped. The Viewport tab now shows only the live viewport, expanded to fill the space the mini previews used to occupy; the mini previews are unaffected on every other tab.</description>
  </item>
  <item>
    <title>MicroShader v4.3.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.3.1</guid>
    <pubDate>Sat, 08 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Fixed MRT Twigl exports (2+ render targets) were not covered by the automatic rename-collision resolution added in 4.3.0: a local variable declared `o0`/`o1` (or `outColor0`/`outColor1`) could collide with the MRT output names without being renamed. Collision detection now also runs for MRT mode, triggering only on an actual local declaration of the colliding name (not on the plain MRT output usage every legitimate MRT shader is expected to contain), to avoid false positives. Screen-reader (Narrator) coverage was documented as complete for every control; the README now accurately states that the Diff, Trace, Stats, Keybindings, Command Palette, and Minimap panels are not yet fully covered. The application did not declare per-monitor DPI awareness, so the UI could render blurry or incorrectly sized on HiDPI displays; it now opts in to `DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2` at startup (fixed-pixel layout scaling remains future work). A stale code comment described GIF capture as unimplemented; it has shipped since 4.1.0 and the comment now reflects that. A `partial_cmp().unwrap()` in the rotation-constant catalogue lookup could panic if given a `NaN` angle; it now falls back to `Equal` instead. ### Added Two new C++ test executables, `ui_pure_logic_test` and `win32_panel_state_test`, covering previously-untested pure-logic modules (fuzzy match, unified diff, GLSL formatting/tokenizing, keybindings storage, recent files, golf-options conversion) and stateful UI panel classes (command palette, diff view, trace view, document tab strip, keybindings, stats panel, appearance panel, minimap). Randomized round-trip tests for the GIF encoder&apos;s LZW dictionary (varying code size, dimensions, frame count, and palette size) guarding the invariants several internal `.expect()` calls rely on.</description>
  </item>
  <item>
    <title>MicroShader v4.3.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.3.0</guid>
    <pubDate>Wed, 05 Aug 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to µShader, from the point of view of using the application. ## [Unreleased] ## [4.3.0] - 2026-08-05 ### Fixed The Twigl export conversion never handled genuine Shadertoy-style `mainImage(out vec4 fragColor, in vec2 fragCoord)` shaders — which is what every real Shadertoy shader (and this app&apos;s own default shader) actually uses — correctly. It silently left the function wrapper untouched, so the Twigl export was effectively broken for real-world shaders regardless of the selected mode. Genuine Shadertoy source now converts correctly to and from every Twigl mode. The **Twigl** tab&apos;s &quot;Import twigl shader&quot; preview box was read-only, making it impossible to paste code copied from twigl.app — it&apos;s now editable, and your pasted text survives switching tabs or editing the Source tab until you click Import. Importing a shader exported in ES 3.00 or MRT mode produced GLSL that failed to compile back in the Source tab (leftover `#version 300 es` header, mismatched output-variable declarations, and — for Geeker/ Geekest — duplicate uniform declarations conflicting with ones the app already provides). Twigl export could silently produce a visually wrong shader when your own code already used a local variable named `r`/`m`/`t`/`f`/`b`/`o`, colliding with the Geek-family uniform/output shorthand (e.g. a raymarching shader&apos;s own grid-size variable named `r` colliding with `iResolution`&apos;s shorthand). Such collisions are now resolved automatically — your identifier is renamed (e.g. `r` becomes `r_0`) and a note in the panel tells you it happened. Golfing a shader before exporting to Twigl could rename an unrelated local variable to `r`/`m`/`t`/`f`/`b`/`o`, colliding with the Twigl uniform shorthand the same way. The mini **Twigl** preview stayed blank with no explanation whenever ES 3.00 or MRT was enabled; its label now says so explicitly instead of looking like a broken/empty preview. The Twigl budget gauge used the generic &quot;X/Twitter shader&quot; byte-budget preset for every mode instead of the mode-specific &quot;Twigl classic&quot;/ &quot;Twigl geekest&quot; preset. ## [4.2.0] - 2026-08-04 ### Added A third live preview, labeled **Twigl**, joins the existing **Source** and **Golfed** ones next to the golfing controls: it shows your shader rendered exactly as twigl.app would run it, updating immediately as you change mode/ES 3.00/MRT/Backbuffer/ Sound in the Twigl Export tab. It stays blank whenever ES 3.00 or MRT is enabled, since those produce WebGL2-only GLSL this desktop viewport can&apos;t run (a known limitation, not a bug). Each of the three previews now shows its own label (&quot;Source&quot;, &quot;Golfed&quot;, &quot;Twigl&quot;) above it, so it&apos;s always clear which is which. ## [4.1.2] - 2026-08-04 ### Fixed The **Twigl** export tab showed a completely empty panel — no mode buttons, no toggles, no Copy/Import buttons, no snippet buttons, and no preview code — making the twigl.app export feature unusable from a normal launch of the application. The panel was never initialized at startup. The byte-budget line on the Twigl tab overlapped the second row of snippet buttons instead of appearing below them. ## [4.1.1] - 2026-08-04 ### Fixed The rotation-constant entries in the **Golf Tips** tab and the &quot;over budget&quot; hint banner showed garbled characters instead of degree signs and dashes. The application now launches maximized, instead of at a small default window size. ## [4.1.0] - 2026-08-04 ### Added New **Capture GIF** action for the live viewport, with three built-in size/duration presets, animated and looping. The **Twigl Export** tab (mode, ES 3.00, MRT, backbuffer, sound) now keeps its own state per document, the same way the golfing profile already did. New **Import twigl shader** button: pastes twigl.app-formatted code and converts it back into a normal, editable shader. The &quot;Copy for twigl.app&quot; button (and the matching command-palette action) now shows a confirmation message after copying. Snippet names from the twigl snippet library are now automatically protected from renaming, avoiding a naming conflict after inserting a snippet. The &quot;Sound&quot; toggle&apos;s accessibility label now states clearly that it refers to a separate program from the one shown, specific to twigl.app. ### Fixed Twigl.app exports in **ES 3.00** mode now use current texture functions (the old ones no longer compiled in a real browser). Multi-target (**MRT**) exports in ES 3.00 now include the layout information WebGL2 requires. The **Backbuffer** and **Sound** toggles in the Twigl export panel now genuinely update the live preview. The command palette&apos;s &quot;Copy as twigl&quot; action now always matches what&apos;s shown on screen (including in MRT mode). Restoring a session on launch now correctly reopens the document and tab that were active when the app was last closed. A document that was intentionally cleared (but not saved) is no longer replaced with the default shader on the next launch. The stats panel now correctly reports whether frequency-aware renaming actually ran on the last pass. Improved the performance of the frequency-aware renaming option used by &quot;Golf harder&quot;. ## [4.0.0] - 2026-08-03 Major release: UI overhaul and twigl.app export. ### Added Tabs are now grouped (Author, Analyze, Export, Settings) for easier navigation. Multi-document editing is back: every open shader gets its own tab, and the whole session (every open document) is restored automatically on the next launch. The command palette now offers one-click shortcuts: copy as Shadertoy / Bonzomatic / bare `main()` / for twigl.app, save/load a golfing profile, import an exclude-name list, capture a PNG screenshot of the live viewport, and export a self-contained HTML session report. New **Twigl Export** tab: converts the golfed shader into the format expected by twigl.app, with a mode picker (Classic/Geek/Geeker/ Geekest), an ES 3.00 toggle, MRT/Backbuffer/Sound toggles, a live preview and a byte-budget gauge, opened via `Ctrl+Alt+T`. ## [3.1.0] - 2026-08-03 ### Added New **Golf harder** button, which automatically searches for an even smaller combination of golfing options than the current settings, with a &quot;Deep search&quot; option. The stats panel gained a new section summarizing the reduction achieved and which passes actually fired. New **Golf Tips** tab: a searchable catalogue of manual reduction techniques (rotation constants, common trigonometric identities, compact noise generators, 3D shape primitives, color gradients, gamma/tonemap corrections) — every entry can be copied or inserted straight into the editor with one click. The &quot;Trace&quot; view now shows a banner whenever the golfed result exceeds the chosen size budget, linking directly to the Golf Tips tab. The golfing controls panel is now an always-visible side panel instead of a full-screen tab, with a &quot;Golf&quot; button and a &quot;Formatted view&quot; button directly accessible. ## [3.0.0] - 2026-07-22 Major change: the application was entirely rebuilt on a native Windows interface — lighter, with no external runtime to install. ### Changed The look and shortcuts stay the same, but a few features from the previous version were not yet ported to this new interface: golfing profiles (save/load), multi-document editing and session restore, &quot;Copy as Shadertoy/Bonzomatic/main()&quot; shortcuts, exclude-name-list import, HTML session report, PNG screenshot, and live-viewport video/GIF recording. These were reintroduced in versions 3.1.0/4.0.0 above, except video/GIF recording (GIF returned in the Unreleased section above). ## [2.1.0] through [2.21.0] - 2026-07-22 Groundwork for the new native interface (see 3.0.0): no visible change in the shipped application during this period, except for the points below. ### Added Support for Windows screen readers (Narrator and similar) on the title bar buttons and checkboxes. An internal color-contrast check ensures text stays readable according to accessibility standards (WCAG AA). **Colorblind-safe status indicators** checkbox (different shapes in addition to colors) in the Appearance tab. **UI text size** slider (13 to 28 pt) in the Appearance tab, with automatic scaling based on the display. Import of a Shader Minifier–style exclude-name list. Three one-click copy shortcuts: &quot;Copy as Shadertoy&quot;, &quot;Copy as Bonzomatic&quot;, &quot;Copy as bare main()&quot;. The golfing profile format (`.ushaderprofile`) is now documented and versioned, for anyone who wants to generate or read one outside the application. ### Fixed Fixed a bug preventing screen readers from correctly detecting UI elements. ## [2.0.0] and [2.0.1] - 2026-07-19 / 2026-07-20 ### Added Export a **session report** as a self-contained HTML file (works offline, no connection needed), including the source code, the golfed code, the reduction statistics, and optionally a screenshot of the live viewport. Drag-and-drop a `.glsl` file onto the window to open it directly in a new tab. **Recent files** list, available from the File menu; entries pointing at a deleted file are removed automatically. ## [1.9.0] and [1.9.1] - 2026-07-19 ### Added **Command palette** (`Ctrl+Shift+P`): fuzzy search across every action in the app (run golf, switch tabs, toggle a pass, load/save a profile, toggle Compare mode, export...). **Customizable keyboard shortcuts** for the command palette, new tab, open, save, and close-tab, from a new &quot;Keyboard Shortcuts&quot; tab. **Minimap** for the Source and Golfed editors: a compact, colored overview of the file, useful on longer shaders. New **Diff** tab: shows at a glance what changed between the source code and the golfed code, removed text struck through in red, added text in green. ## [1.8.0] - 2026-07-19 ### Added **Command-line tool** for automatically golfing a whole folder of shaders (useful for plugging into a game or demo build pipeline), with size-budget options, a detailed report, and a preview of the differences before applying. ## [1.7.0] - 2026-07-19 ### Added **Multi-document editing**: one tab per open `.glsl` file, each with its own golfing settings. The session (every open document) is now **saved and restored automatically** on launch, with a confirmation before reopening files. A dot appears on the tab of a document with unsaved changes, and a confirmation is asked before closing it or quitting the app with unsaved changes. ## [1.6.0] - 2026-07-19 Internal groundwork for the automatic verification feature (Compare mode): no visible change yet. ## [1.5.0] - 2026-07-19 ### Added New **Trace** tab: lists, pass by pass, everything the golfing engine changed in the shader, with a before/after view per pass — including passes that made no change, shown grayed out. ## [1.4.0] and [1.4.1] - 2026-07-19 ### Added **Golfing profiles**: save and reload a set of golfing settings (`.ushaderprofile`), plus three ready-to-use built-in profiles (`Maximum`, `Safe`, `None`). ## [1.3.0] and [1.3.1] - 2026-07-19 ### Added **Compressed-size budgets**: the stats panel now also estimates the compressed size (what actually matters for code-golf/demoscene competitions), with presets (Shadertoy, X/Twitter, 4KB, 8KB, 64KB...) and a green/amber/red badge showing whether the budget is met. The application now launches maximized. ## [1.2.0] through [1.2.5] - 2026-07-18 / 2026-07-19 ### Added New **dark interface**, inspired by a professional video-editing application, with a custom title bar. Live-viewport video recording to MP4/WebM now works out of the box right after installing, with nothing extra to install. Two new golfing passes (algebraic identity simplification like `x*1` → `x`, and elimination of redundant computations), each with its own checkbox and counter. ### Fixed Fixed two cases where golfing produced an incorrect result with the new redundant-computation-elimination pass. ## [1.1.0] - 2026-07-18 ### Added **Live-viewport recording** to an animated GIF, and to MP4/WebM if `ffmpeg` is installed on the machine. ### Fixed Opening/saving files whose path contains accented or special characters (for example the &quot;µShader&quot; folder itself). A console window briefly flashed behind the main window on launch; it no longer appears. ## [1.0.0] - 2026-07-18 First stable release, with a dedicated Windows installer (`uShader-Setup-*.exe`). ## [0.8.0] - 2026-07-18 ### Added **About** window (version, copyright, contact links). Application and installer icons. ### Fixed Files whose path contains non-ASCII characters failed to load/save correctly. ## [0.7.0] - 2026-07-18 ### Added **Open/Save** buttons for the source code. **Copy** button for the golfed code. **Export (Shadertoy)** button. **Screenshot** button for the live viewport, exported as PNG. ## [0.6.0] - 2026-07-18 ### Added First golfing controls: an &quot;Aggressive golfing&quot; toggle and individual checkboxes for every transformation pass, plus a field to protect certain names. Stats panel: before/after size, reduction percentage, per-pass counters, budget badges. **Compare** mode: shows the source and golfed shaders side by side to visually confirm they render the same image. ## [0.5.0] - 2026-07-18 ### Added A real text editor for the source and golfed code, with GLSL syntax highlighting. **Formatted view** toggle to read the golfed code across multiple lines. The offending line is highlighted directly in the editor on a compile error. ## [0.4.0] - 2026-07-18 ### Added First complete graphical interface: Source / Golfed / Live viewport panels, resizable. The &quot;Run golf&quot; action actually golfs the code and shows the result. The live viewport shows the golfed shader running, with compile errors visible on screen. ## [0.3.0] - 2026-07-18 First preview: a window displays and animates a demo shader (no editing yet). ## [0.1.0] and [0.2.0] - 2026-07-18 Project kickoff: initial repository and golfing-engine structure, no usable interface yet.</description>
  </item>
  <item>
    <title>MicroShader v4.2.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.2.0</guid>
    <pubDate>Tue, 04 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Added A third live preview, labeled **Twigl**, joins the existing **Source** and **Golfed** ones next to the golfing controls: it shows your shader rendered exactly as twigl.app would run it, updating immediately as you change mode/ES 3.00/MRT/Backbuffer/ Sound in the Twigl Export tab. It stays blank whenever ES 3.00 or MRT is enabled, since those produce WebGL2-only GLSL this desktop viewport can&apos;t run (a known limitation, not a bug). Each of the three previews now shows its own label (&quot;Source&quot;, &quot;Golfed&quot;, &quot;Twigl&quot;) above it, so it&apos;s always clear which is which.</description>
  </item>
  <item>
    <title>MicroShader v4.1.2</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.1.2</guid>
    <pubDate>Tue, 04 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Fixed The **Twigl** export tab showed a completely empty panel — no mode buttons, no toggles, no Copy/Import buttons, no snippet buttons, and no preview code — making the twigl.app export feature unusable from a normal launch of the application. The panel was never initialized at startup. The byte-budget line on the Twigl tab overlapped the second row of snippet buttons instead of appearing below them.</description>
  </item>
  <item>
    <title>MicroShader v4.1.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.1.1</guid>
    <pubDate>Tue, 04 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Fixed The rotation-constant entries in the **Golf Tips** tab and the &quot;over budget&quot; hint banner showed garbled characters instead of degree signs and dashes. The application now launches maximized, instead of at a small default window size.</description>
  </item>
  <item>
    <title>MicroShader v4.1.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.1.0</guid>
    <pubDate>Tue, 04 Aug 2026 00:00:00 GMT</pubDate>
    <description>### Added New **Capture GIF** action for the live viewport, with three built-in size/duration presets, animated and looping. The **Twigl Export** tab (mode, ES 3.00, MRT, backbuffer, sound) now keeps its own state per document, the same way the golfing profile already did. New **Import twigl shader** button: pastes twigl.app-formatted code and converts it back into a normal, editable shader. The &quot;Copy for twigl.app&quot; button (and the matching command-palette action) now shows a confirmation message after copying. Snippet names from the twigl snippet library are now automatically protected from renaming, avoiding a naming conflict after inserting a snippet. The &quot;Sound&quot; toggle&apos;s accessibility label now states clearly that it refers to a separate program from the one shown, specific to twigl.app. ### Fixed Twigl.app exports in **ES 3.00** mode now use current texture functions (the old ones no longer compiled in a real browser). Multi-target (**MRT**) exports in ES 3.00 now include the layout information WebGL2 requires. The **Backbuffer** and **Sound** toggles in the Twigl export panel now genuinely update the live preview. The command palette&apos;s &quot;Copy as twigl&quot; action now always matches what&apos;s shown on screen (including in MRT mode). Restoring a session on launch now correctly reopens the document and tab that were active when the app was last closed. A document that was intentionally cleared (but not saved) is no longer replaced with the default shader on the next launch. The stats panel now correctly reports whether frequency-aware renaming actually ran on the last pass. Improved the performance of the frequency-aware renaming option used by &quot;Golf harder&quot;.</description>
  </item>
  <item>
    <title>MicroShader v4.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-4.0.0</guid>
    <pubDate>Mon, 03 Aug 2026 00:00:00 GMT</pubDate>
    <description>Major release: Phase 34 (twigl.app export parity) and Phase 38 (UI overhaul with multi-document editing) are both externally visible, workflow-changing additions. ## Highlights **twigl.app export parity** (classic/geek/geeker/geekest modes, ES 300 rewriting, MRT/backbuffer naming, tweet-budget presets). **Neyret and Inigo Quilez idiom libraries** (&quot;Golf Tips&quot; panel) — opt-in, inert suggestions, never auto-applied. **&quot;Golf harder&quot; compression-aware search**, extended with a hand-written DEFLATE encoder and a simulated-annealing deep search mode. **UI overhaul**: tab strip regrouped into Author/Analyze/Export/Settings, a new always-visible document tab strip with multi-document editing, session persistence across restarts, one-click export presets, `.ushaderprofile` save/load, exclude-list import, PNG viewport capture, and self-contained HTML session reports. **Branding/licensing cleanup** and an **offline-first audit**: confirmed zero network calls at run time across every new module. See `CHANGELOG.md` for the full per-phase changelog.</description>
  </item>
  <item>
    <title>MicroShader v3.0.2.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-3.0.2.0</guid>
    <pubDate>Sun, 02 Aug 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to µShader are documented in this file. ## [v3.0.2.0] ### Added `golf.md` Phase 31.1 (`for`-loop header golfing) is now implemented: a new `rust-core/src/loop_golf.rs` module folds the common Shadertoy/demoscene idiom of a standalone float counter incremented at the top or bottom of a `for` loop body (`i++`, `i+=1.`, or `i=i+1.`, the latter proven via `expr.rs`&apos;s `Expr` parser rather than raw-token matching) into the `for(...)` header itself, e.g. `float i=0.;for(int j=0;j&lt;8;j++){i+=1.;...}` becomes `for(float i=0.;i++&lt;8.;){...}`. The rewrite is skipped outright whenever the loop contains a `continue`, whenever the old loop variable is still read inside the body, or whenever the counter&apos;s final value is read anywhere after the loop (its scope narrows to the loop once hoisted into the header), matching this document&apos;s &quot;decline rather than risk&quot; precedent from Phase 30.4. `AggressiveOptions::loop_header_golf` (off by default, including in `Maximum`, consistent with Phase 30.1&apos;s own opt-in-only precedent for behavior-adjacent passes). Added `fixtures/loop_header_golf.glsl` (a raymarching loop and a fractal-iteration loop, the two most common Shadertoy loop shapes), Rust unit tests covering all three increment shapes plus every decline case, a `never_worsens_deflate_budget_on_the_tracked_fixture` regression test, and a new `tests/wgl_equivalence_test.cpp` block proving the fixture renders pixel-identically before and after the rewrite under the Phase 15 WGL equivalence harness. `golf.md` Phase 31.2 (`do`/`while` → `for` and `while` → `for` normalization) is now implemented in `rust-core/src/loop_golf.rs`: `golf_loop_forms` rewrites `while(cond){...}` to `for(;cond;){...}` (dropping the condition entirely for `while(true)`, since an empty `for` test clause means &quot;always true&quot;) and folds a `do{S}while(cond);` loop into `for(;S,cond;);` via the comma operator whenever `S`&apos;s statements are every one of them already representable as a bare `Expr` (reusing Phase 30.3&apos;s `classify_fusable_statement` whitelist) — both rewrites are unconditionally behavior-preserving, so unlike Phase 31.1 this pass carries no `continue`/read-after-loop safety check, only a size guard: it is skipped outright unless the rewritten header is strictly shorter in both raw character count and `budget.rs`&apos;s estimated DEFLATE size, the same char/DEFLATE-size comparison Phase 30.1 already uses. New `AggressiveOptions::loop_form_golf` toggle (off by default, including in `Maximum`). Added `fixtures/loop_form_golf.glsl` (an unbounded `while(true){...break;}` raymarch loop and a fully-fusable accumulation `do`/`while` loop), Rust unit tests for both the `while`→`for` and `do`/`while`→`for` directions, a &quot;no change when not shorter&quot; test for a non-trivial `while` condition (where the `while(`/`for(;`..`;)` boilerplate ties exactly in character count), decline tests for a brace-free `while` body and for a `do`/`while` body containing a `break` or a declaration, and a `never_worsens_deflate_budget_on_the_tracked_fixture` regression test. `golf.md` Phase 31.3 (early-return / guard-clause ternary extension) is now implemented: `try_match_ternary` in `rust-core/src/aggressive.rs` is joined by a new `try_match_guard_clause_ternary`, widening `ternary_from_if_else` to also recognize the tail-of-function `if(cond){return a;}return b;` idiom (with or without braces around the `if` arm) and collapse it to `return(cond)?a:b;` — a strictly more constrained special case of the existing assignment-pair form (single trailing `return`, no `else` branch), not a new safety class. The guard-clause form is only recognized when the second `return` sits exactly at a function&apos;s closing `}`, reusing `callgraph.rs`&apos;s existing `find_function_definitions` for that boundary check rather than inventing new function-boundary detection. No new toggle: the existing `AggressiveOptions::ternary_from_if_else` flag covers both forms. `fixtures/ternary_from_if_else.glsl` gained a third function (`clampSign`, now called from `mainImage` so it survives dead-function elimination) exercising the guard-clause case, and `rust-core/src/golfer.rs`&apos;s existing ternary test block gained seven new unit tests: braced and unbraced guard-clause collapses, and decline cases for an `else` branch being present, the guard clause not being at the tail of the function, a non-`return` second arm, a multi-term arm, and confirmation that no dedicated toggle exists. Phase 35.1 and 35.2 (Fabrice Neyret idiom library) land as a new `rust-core/src/neyret.rs` module: a 10-entry short-rotation-matrix constant catalogue with `suggest_rotation_matrix_constants(source)` detection (matches `mat2(cos(a),sin(a),-sin(a),cos(a))`-style constructions with a provably compile-time-constant angle against the catalogue, within a 0.25° tolerance), and a 5-entry `fract(sin(dot(...)))`-family hash-snippet catalogue (`hash11`/`hash12`/`hash21`/`hash22`/`hash13`) distinct from twigl&apos;s `fsnoise`. This is intentionally backend-only: 35.3 and 35.4 are not started, since they depend respectively on `golf.md` Phase 30&apos;s comma-fusion pass and Phase 33&apos;s Golf Tips panel, and neither exists in this codebase yet. See `ROADMAP.md` Phase 35 for the full status note. Phase 34 (twigl.app golfing-mode parity) is now fully implemented. `rust-core` adds the `snoise2D`/`snoise3D`/`snoise4D` snippet entries (completing the documented 10-entry geekest-mode snippet library) and exposes the full Twigl export surface through a new `capi` FFI layer (`ushader_twigl_rewrite`, `ushader_twigl_rewrite_mrt`, `ushader_twigl_snippet`, `ushader_twigl_snippets_json`, `ushader_estimate_twigl_geekest_budget`, `ushader_twigl_export_uniform_names_json`). The C++ shell gains a new &quot;Twigl&quot; tab (`Win32TwiglExportPanel`) with the Classic/Geek/Geeker/ Geekest mode ladder, an ES 300 es toggle, MRT/backbuffer/sound toggles, one-click snippet-insert buttons (via a new `Win32TextEditor::insert_text_utf8` helper), a live preview, and a byte-budget badge, opened via a new rebindable `Ctrl+Alt+T` chord. `tests/wgl_equivalence_test.cpp` now also compiles/links the `twigl_classic.glsl` and `twigl_geekest.glsl` regression fixtures under the desktop GL WGL context; `twigl_300es.glsl` remains permanently excluded (documented ES300/desktop-GL-core incompatibility). See `ROADMAP.md` Phase 34 for full details. `golf.md` Phase 29 (frequency-aware renaming &amp; swizzle-alphabet golf) is now fully implemented: - **29.1** — the opt-in, deterministic frequency-aware identifier-renaming heuristic evaluates a compression-aware alternative mapping against the existing rename order and only keeps it when the final post-pass DEFLATE estimate is strictly better; otherwise output stays on the historical mapping. `AggressiveOptions::frequency_aware_renaming` (default on in `Maximum`, off in `Safe`). Added `fixtures/frequency_renaming.glsl` and Rust regression coverage for determinism and no-regression budget checks. - **29.2** — swizzle-alphabet golf: a new `rust-core/src/swizzle.rs` module recolors every `.xyzw`/`.rgba`/`.stpq` swizzle access to whichever of the three interchangeable alphabets the caller selects, never touching struct member access. `Auto` mode tries all three against the DEFLATE estimator and keeps the smallest. Surfaced as a `swizzle_alphabet` combo in `golf_controls.cpp` / `win32_golf_controls.cpp`, persisted by a `.ushaderprofile` schema minor-version bump (backward compatible: an absent field defaults to `Auto`). Added `fixtures/swizzle_alphabet.glsl` and a new `tests/wgl_equivalence_test.cpp` block that compiles the fixture&apos;s `.xyzw` source against a hand-verified `.rgba` recoloring and proves they render pixel-identically under the Phase 15 WGL equivalence harness; the recoloring itself is golden-mastered by a new Rust test against the same fixture so the two can never drift. - **29.3** — non-constant swizzle/vector factoring: a new `factor_repeated_vector_args` pass in `aggressive.rs` generalizes `reduce_constant_vectors` to identical **pure identifier** arguments (`vec3(a,a,a)` -&gt; `vec3(a)`, `vec3(p.x,p.x,p.x)` -&gt; `vec3(p.x)`), never touching a not-all-equal or literal-only call (those remain the sole responsibility of `reduce_constant_vectors`, with no double-counting between the two stats). `AggressiveOptions::factor_repeated_vector_args` (default on). Added `fixtures/vector_argument_factoring.glsl` and four Rust unit tests. - The CLI (`golf`) now reports `vector args factored` and `swizzles recolored` in its human-readable summary and exposes `vector_args_factored`/`swizzles_recolored` as new JSON/CSV report columns, alongside the existing `--profile`/`.ushaderprofile` wiring for all three sub-phases. - See `ROADMAP.md` §6 and `golf.md` §5 for the full status note. Phase 30 (aggressive inlining &amp; cross-statement subexpression golf) remains not started; its one landed-but-unfinished pass, `fuse_statement_sequences`, stays off by default until Phase 30 ships its own fixture, tests, and checkbox. `golf.md` Phase 30.3 (statement-sequence fusion via the comma operator) is now fully implemented. The `fuse_statement_sequences` pass already existed in `aggressive.rs`; this closes it out with `fixtures/statement_fusion.glsl`, five Rust regression tests (basic fusion, declaration/`if`-brace/`return` boundaries, off-by-default, and a DEFLATE never-worse check), a &quot;Fuse statement sequences&quot; checkbox in the Win32 Controls panel, and full `.ushaderprofile` persistence (`.ushaderprofile` schema bumped to version 3). The `Maximum` built-in profile now explicitly enables it and `Safe`/ `None` explicitly disable it, closing a latent gap where the pass silently stayed on in both via `GolfPassToggles`&apos; own default. The shared `AggressiveOptions::all()`/`golf(source, true)` test helper deliberately still keeps it off, the same precedent already set for `frequency_aware_renaming`, since flipping it there would silently change the expected output of roughly a hundred pre-existing exact-string regression tests. Also backfills `factor_repeated_vector_args` and `swizzle_alphabet` (Phase 29.2/29.3) into `docs/ushaderprofile.schema.json` and `docs/ushaderprofile-schema.md`, which had been implemented in `golf_profile.cpp` but never documented. Phase 30.1 (aggressive multi-call-site inlining), 30.2 (whole-shader macro CSE), and 30.4 (declaration hoisting) remain not started. UX feedback fix: the Controls panel becomes a persistent right-side inspector instead of a full-screen tab, and two actions that were previously keyboard/command-palette-only get visible buttons. ### Changed The &quot;Controls&quot; tab is removed; `Win32GolfControls` now renders as a fixed-width (300px) inspector docked to the right edge, visible on every tab except Viewport (which keeps the full window width, needed for Compare mode&apos;s split render). All other tab content (editors, Diff, Trace, Stats, Appearance, About) narrows to make room; the Source/Golfed minimap repositions to stay left of the inspector instead of the raw window edge. The pass-checkbox list switched from two columns to one: the two-column layout was sized for a full-width tab and truncated labels like &quot;Common subexpressions&quot; once squeezed into the 300px inspector. Single column uses the inspector&apos;s full available height instead, with no truncation. ### Added A &quot;Golf&quot; button at the top of the inspector runs the same recompile action as `F5` or the command palette&apos;s &quot;Run golf&quot; entry — previously keyboard/palette-only. A &quot;Formatted view&quot; toggle button in the top-right of the Golfed tab&apos;s content area, mirroring the existing `Ctrl+Shift+F` chord — previously also keyboard/palette-only. Both new buttons (`src/ui/win32_tool_button.h/.cpp`, a small reusable owner-drawn button) are registered for UI Automation like every other Phase 26 control, verified live with the same `System.Windows.Automation` client used throughout that phase.</description>
  </item>
  <item>
    <title>WinMusic v1.0.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=winmusic</link>
    <guid isPermaLink="false">winmusic-1.0.1</guid>
    <pubDate>Sat, 01 Aug 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to WinMusic are documented in this file. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/). ## [1.0.1] - 2026-08-02 ### Fixed `scripts/build_installer.ps1`: auto-detects `ISCC.exe` (Inno Setup compiler) instead of assuming it&apos;s on `PATH`. Checks, in order: the current `PATH`, the default Inno Setup 7 (64-bit) install location (`C:\Program Files\Inno Setup 7\`), then the default Inno Setup 6 (32-bit) location (`C:\Program Files (x86)\Inno Setup 6\`). Raises a clear, actionable error (download link + `-IsccPath` override instructions) instead of PowerShell&apos;s raw &quot;term not recognized&quot; error when Inno Setup isn&apos;t found anywhere. ## [1.0.0] - 2026-08-02 First stable release. Feature set is now frozen; the `1.0.x` line will only receive bug fixes. ### Added `scripts/generate_screenshot.py`: renders `MainWindow` off-screen against a small in-memory demo library to produce a genuine, reviewable `assets/docs/screenshot.png` for the README (not part of the shipped application). ### Fixed `LibraryFilterProxyModel`: replaced the deprecated `QSortFilterProxyModel.invalidateFilter()` calls with the `beginFilterChange()`/`endFilterChange()` pair recommended since Qt 6.6, removing the last remaining deprecation warnings from the test suite. `tests/test_audio_analysis.py`: two tests had gone stale after the `PcmCapture` crash-safety fix (introduced while packaging for v0.9.5) that defers wiring libVLC&apos;s `audio_set_format`/`audio_set_callbacks` from `__init__` to an explicit `attach()` call. The tests still asserted the pre-fix, immediate-registration behaviour and were failing under a real libVLC install. Rewritten to reflect the current, correct contract (`attach()` registers the callbacks exactly once, idempotently, and only once called) instead of reverting the underlying safety fix. Minor style debt cleared project-wide (`black`, `ruff`): three over-length lines reflowed (`core/database/dao.py`, `ui/views/playlist_panel.py`), one unused import removed, import ordering fixed, and justified `noqa` annotations added for the Qt model API overrides that cannot follow Python naming/default-argument conventions (`rowCount`, `columnCount`, `headerData` in `LibraryTableModel`). Zero outstanding `black`/`ruff` findings. ### Changed Application version bumped to `1.0.0` (`main.py`, `pyproject.toml`, `installer/winmusic.iss`). `README.md` rewritten for the stable release: drops the &quot;planned for v1.0.0&quot; framing, documents the actual installer download/install steps, and embeds a real interface screenshot instead of the previous placeholder note. Full regression pass: `pytest` + `pytest-qt`, 144 tests passing, 1 skipped (a crossfade-start test that requires a live OpenGL context, unavailable in headless/offscreen environments — unrelated to this release). ## [0.9.5] - 2026-08-01 ### Added **PyInstaller `--onedir` packaging** (`winmusic.spec`, project root): freezes `src/main.py` into a windowed (`console=False`) `WinMusic.exe` plus its supporting `dist/WinMusic/` folder, using the app icon (`assets/icons/winmusic.ico`). Bundles the `shaders/` folder and `assets/icons/` as data, and explicit `hiddenimports` for the runtime dependencies PyInstaller&apos;s static analysis cannot always discover on its own (`vlc`, `pytesseract`, `imagehash`, `acoustid`, `PIL`). Optionally vendors the native runtimes the app needs at runtime (libVLC, Tesseract OCR, Chromaprint `fpcalc`) from a local `vendor/` staging folder (see `vendor/README.md`) so the packaged app stays Offline-First: any vendor sub-folder missing at build time is skipped rather than failing the build, and the matching optional feature degrades gracefully at runtime, exactly as already implemented in `core/cover_recognition/ocr.py` and `core/fingerprinting/`. **`core/app_paths.py`**: centralizes resolution of bundled-resource directories (`shaders_dir()`, `icons_dir()`, `vendor_dir()`) so the same code works both from a source checkout and from a frozen PyInstaller `--onedir` build (`is_frozen()` / `sys.executable`-relative resolution instead of a `__file__`-relative one, which is not reliable once modules run from PyInstaller&apos;s frozen archive). `ui/visualizer/shader_registry.py` and `main.py` now resolve the shaders folder and the app icon through this module instead of ad hoc `Path(__file__).resolve().parents[...]` expressions. **Inno Setup installer script** (`installer/winmusic.iss`): builds `WinMusicSetup-&lt;version&gt;.exe` from the PyInstaller `--onedir` output, with a Start Menu group + uninstaller shortcut, an optional desktop shortcut, an optional (opt-in, unchecked by default) `.mp3` file association, a clean uninstall (`[UninstallDelete]` removes the install directory only — the local database/logs/settings under `%AppData%\WinMusic` are preserved across reinstalls), 64-bit-only install (`ArchitecturesAllowed=x64compatible`), and `MinVersion=10.0` to enforce the Windows 10/11 target. **`scripts/build_installer.ps1`**: orchestrates the full release build on a Windows machine — dependency install, `pyinstaller winmusic.spec --clean --noconfirm`, then `ISCC.exe installer\winmusic.iss` — failing fast with a clear error if either step doesn&apos;t produce its expected output. **`vendor/README.md`**: documents the exact folder layout (`vendor/vlc/`, `vendor/tesseract/`, `vendor/chromaprint/fpcalc.exe`) and upstream sources expected before running the packaging build, and how to verify a produced `dist/WinMusic/` folder on a clean Windows 10/11 VM with no Python/VLC/Tesseract installed system-wide. Test suite extended with `test_v095_features.py` (frozen vs. source-checkout path resolution in `core.app_paths`, PyInstaller spec content — onedir/console/icon/datas/hidden imports, Inno Setup script content — required sections, shortcuts, opt-in `.mp3` association, clean-uninstall behaviour, build script wiring, and cross-file version consistency between `main.py`/`pyproject.toml`/`winmusic.iss`) — 14 new tests, 143 tests cumulated (VLC-backed playback tests and the OpenGL-backed visualizer tests remain environment-dependent on a real libVLC install / GPU context, unrelated to this release). ### Changed Application version bumped to `0.9.5` (`main.py`, `pyproject.toml`, `installer/winmusic.iss`). `.gitignore`: excludes vendored native runtime binaries (`vendor/*`, keeping `vendor/README.md` tracked) in addition to the existing `*.spec`/`build/`/`dist/` exclusions — none of this packaging build output or third-party binary is published to the source-free public repository (§9 of `ROADMAP.md`). ## [0.9.0] - 2026-08-01 ### Added **About tab** (`ui/about_tab.py` — `AboutTab` widget + `AboutDialog`): displays application name, version, copyright, e-mail and website (as clickable links), and a short license summary. Reachable from a new **Help &gt; About WinMusic…** menu entry. **Application and installer icons** (`assets/icons/winmusic.ico`, `assets/icons/installer.ico`): multi-resolution `.ico` files (16, 24, 32, 48, 64, 128, 256 px) generated from a single dark-themed, accent-purple design (`scripts/generate_icons.py`, Pillow-based, fully offline/reproducible). The app icon is applied to the `QApplication` window icon at startup. **Persisted user settings** (`core/database/dao.py` — `SettingsDAO` backed by the existing `app_settings` table): generic get/set/get_int/set_int/delete/all key-value accessors with `INSERT ... ON CONFLICT DO UPDATE` upsert semantics. Three settings are now persisted and restored across sessions: last scanned library folder (`library_folder`, pre-fills the folder picker), last shader used in the visualizer (`last_shader_id`, restored on startup), and playback volume (`volume`). **Local error logging** (`core/logging_setup.py`): rotating local log file (`&lt;app-data&gt;/logs/winmusic.log`, 2 MB × 3 backups, UTF-8), offline-only (no network handler is ever attached), plus a `sys.excepthook` that logs any unhandled exception before the default handler runs. Initialized at startup in `main.py`. Previously-silent `except: pass` blocks in `MainWindow` (stored cover read failures, perceptual-hash computation failures) now log a warning instead of failing silently, and scan/tag-write failures are logged with `logger.exception`/`logger.error` in addition to the existing UI message boxes. Test suite extended with `test_v090_features.py` (`SettingsDAO` CRUD/upsert/int-coercion, `configure_logging`/log file creation and content, About tab constants) — 11 new tests, 129 tests cumulated (VLC-backed playback tests remain environment-dependent on a real libVLC install, unrelated to this release). ### Changed `MainWindow`: restores persisted settings after the initial UI/library load (`_restore_settings`), and now persists the shader selection and volume as they change, and the scanned folder each time `File &gt; Scan Folder…` is used. `main.py`: application version bumped to `0.9.0`; local logging is configured before the Qt application starts. ## [0.8.0] - 2026-08-01 ### Added **Online enrichment (`core/online_enrichment/`)**, manual/on-demand only, never called automatically in the background (Offline-First): - `MusicBrainzClient`: recording search (artist + title) and free-text release search (used to search from OCR output), both against the public MusicBrainz web service (JSON API, proper `User-Agent`). - `CoverArtClient`: downloads the front cover for a MusicBrainz release id from the Cover Art Archive; raises a dedicated `CoverArtError` (including a clean 404 → &quot;no cover available&quot; case) instead of crashing the UI. - `genre_fallback.py`: full ID3v1 genre code table (0-79) used to keep genre resolution functional even fully offline / when MusicBrainz has no usable genre relation. **Cover recognition (`core/cover_recognition/`)**, fully offline: - `image_utils.py`: normalizes a pasted/imported screenshot (RGB conversion, max-dimension downscaling) before OCR/hashing. - `ocr.py`: local text extraction via `pytesseract`, degrading gracefully (`OcrUnavailableError`) instead of crashing when the local Tesseract engine is missing/broken. - `perceptual_hash.py`: pHash computation and Hamming-distance matching (`imagehash`) with a configurable confidence threshold. - `recognizer.py` (`CoverRecognizer`): orchestrates OCR + perceptual matching against the covers already known in the local library — entirely offline, no network call is ever made from this module. - `storage.py`: local on-disk storage for recognized/downloaded covers (`&lt;app-data&gt;/covers/album_&lt;id&gt;.&lt;ext&gt;`). **Cover Recognition screen** (`ui/dialogs/cover_recognition_dialog.py`): import a screenshot from a file or the clipboard, see the local OCR text (editable), review ranked offline matches against the library, optionally trigger an online MusicBrainz search (explicit button, never automatic) with a live cover-art preview from the Cover Art Archive, and confirm via a dedicated &quot;Use This Match&quot; step — nothing is written to the database or to ID3 tags until the user explicitly confirms. Reachable from the library&apos;s right-click menu (&quot;Identify Cover Art…&quot;). `AlbumDAO`: `set_cover_hash`, `set_musicbrainz_id`, `get_by_id`, `list_with_cover_hash`, `list_all`. `ArtistDAO.set_musicbrainz_id`. `TrackDAO.get()` (raw entity lookup, used to preserve the existing genre when cover recognition updates a track&apos;s artist/album/year). `id3_writer.embed_cover()`: writes/replaces the front-cover `APIC` frame on a track&apos;s ID3 tags (used after a confirmed cover match). Database migration `v2`: adds `albums.cover_hash` (+ index) so perceptual-hash matching against the library works fully offline. Test suite extended with `test_online_enrichment.py` (MusicBrainz / Cover Art Archive clients, mocked HTTP, no real network calls) and `test_cover_recognition.py` / `test_v080_features.py` (OCR, pHash, recognizer, DAO roundtrips, `embed_cover`) — 109 tests passing (VLC playback tests remain environment-dependent on a real libVLC install, unrelated to this release). ### Changed `LibraryView` gained a `cover_recognition_requested` signal and an &quot;Identify Cover Art…&quot; entry in the track context menu. `MainWindow` now owns `app_data_dir` (derived from the SQLite database path) to resolve where recognized/downloaded covers are stored locally. ## [0.7.0] - 2026-08-01 ### Added Final 5 audio-reactive GLSL shaders (`src/shaders/*.frag`, GLSL 330 core, same &quot;Image&quot;-pass-only convention as the first 5), completing the full set of 10 required by the roadmap: Liquid Metal Bass Ripple (bump-mapped specular ripple driven by bass), Starfield Treble Sparkle (warp-speed starfield with treble-driven twinkle), Voronoi Audio Morph (animated Voronoi cells reacting to mid/onset), Fractal Julia Audio Zoom (Julia set fractal whose `c` parameter and zoom are modulated by bass/mid/onset, capped at 48 iterations for performance), Waveform Ribbon 3D Illusion (5 layered sine ribbons with a pseudo-3D depth/perspective illusion). `ShaderVisualizerWidget` (`ui/visualizer/shader_widget.py`) now renders through an off-screen `QOpenGLFramebufferObject` sized at a configurable `render_scale` (default `0.75`, clamped `0.5`–`1.0` via `set_render_scale()`) before presenting the result upscaled to the widget&apos;s full resolution through a dedicated present pass. This cuts the number of fragment-shader invocations roughly quadratically with the scale factor, targeted at integrated/laptop GPUs, while keeping visual density (bar counts, cell counts, etc.) unaffected since none of the shaders hard-code pixel counts. Smooth crossfade transitions between shaders: `set_shader()` now keeps the outgoing shader&apos;s program alive and renders both the outgoing and incoming shader into separate off-screen framebuffers for `TRANSITION_DURATION_MS` (550 ms), blending them via a small present shader (`u_tex_from` / `u_tex_to` / `u_mix`) instead of hard-cutting on shader switch, whether triggered from the panel selector or programmatically. Test suite extended (shader registry/GLSL validation tests now cover all 10 shaders instead of 5, plus new coverage for `render_scale` clamping and for the crossfade transition state machine) — 79 tests cumulated, 100% passing (the crossfade test is skipped, rather than failed, in environments without a real OpenGL context, e.g. headless CI without a GPU/X server). ### Changed `shader_registry.SHADER_REGISTRY` now lists all 10 shaders; the shader selector in `VisualizerPanel` and the full-screen visualizer therefore expose all 10 without any UI code changes, since both already iterate `list_shaders()`. ### Added `ShaderVisualizerWidget` (`ui/visualizer/shader_widget.py`): a `QOpenGLWidget`-based renderer targeting OpenGL 3.3 core. Compiles a shared full-screen-triangle vertex shader against a loadable GLSL fragment shader (`.frag`), driven by a dedicated 60 FPS (~16 ms) `QTimer` render loop that pauses automatically while the widget is hidden. Falls back to a safe solid-color program and emits `shader_compile_failed` if a shader fails to compile/link, instead of crashing the UI. Shader registry (`ui/visualizer/shader_registry.py`): a Qt-independent module listing the available shaders, resolving their file paths under `src/shaders/`, and validating/loading their GLSL source. First 5 audio-reactive GLSL shaders (`src/shaders/*.frag`, GLSL 330 core, Shadertoy-style &quot;Image&quot; pass only, no `iChannel`), all driven by the `u_bass` / `u_mid` / `u_treble` / `u_amplitude` / `u_onset` / `u_onset_strength` / `u_time` / `u_resolution` uniforms fed from the v0.5.0 `UniformBus`: Spectrum Bars Radial, Plasma Waves, Tunnel Warp, Particle Field Pulse, Kaleidoscope Frequency. `VisualizerPanel` (`ui/views/visualizer_panel.py`): shader selector (`QComboBox`) plus a live embedded preview, added to `MainWindow` between the library/playlist splitter and the player bar. Full-screen visualizer mode (`FullscreenVisualizerWindow`): opened via the panel&apos;s &quot;Fullscreen&quot; button or the new **Visualizer → Fullscreen** menu action (`F11`), closes on `Esc`. Test suite extended (15 new tests: shader registry listing/lookup, GLSL source validation for all 5 shaders — required uniforms present, valid `#version`/`main()` — widget shader switching, panel selector wiring) — 71 tests cumulated, 100% passing. ### Fixed `PcmCapture.__init__` (`core/audio_analysis/pcm_capture.py`): current `python-vlc` releases require the `opaque` parameter to be passed explicitly to `MediaPlayer.audio_set_callbacks()` (it has no default value); the call was missing it, raising `TypeError: ... missing 1 required positional argument: &apos;opaque&apos;` at startup on real Windows installs. `PcmCapture`: the play callback given to `audio_set_callbacks()` must be a ctypes function pointer (`CFUNCTYPE(None, c_void_p, c_void_p, c_uint, c_int64)`), not a plain bound method; libVLC rejected it with `ctypes.ArgumentError: ... &apos;method&apos; object cannot be interpreted as ctypes.c_void_p`. The callback is now wrapped accordingly and the reference kept alive on the instance. `PcmCapture._play_callback`: `samples` (the pointer to the raw audio buffer) and `count` (the per-channel sample count) were swapped when computing the byte length and reading from memory, which would have crashed or read garbage as soon as real playback reached the callback (previously untested, since prior tests only exercised the callback indirectly via `feed_raw()`). Test stub (`FakeVlcPlayer` in `tests/test_audio_analysis.py`) updated to match the real 6-argument `audio_set_callbacks` signature, and a new test now drives the callback through the real ctypes function pointer (as libVLC would) to cover this class of regression going forward. ## [0.5.0] - 2026-08-01 ### Added Real-time PCM capture (`core/audio_analysis/pcm_capture.py`): hooks into libVLC&apos;s audio callback API (`audio_set_format` / `audio_set_callbacks`) to tap the currently-playing PCM stream directly — no microphone or WASAPI loopback device required. `AudioPlayer.enable_pcm_capture()` wires this transparently onto the existing playback engine. FFT band extraction (`core/audio_analysis/fft_analyzer.py`): windowed real FFT (`scipy.fft.rfft`) extracting bass (20–250 Hz), mid (250–4000 Hz) and treble (4000–20000 Hz) band energies plus overall RMS amplitude from each analysis frame. Onset (beat) detection with temporal smoothing (`core/audio_analysis/onset_detector.py`): spectral-flux based detector with an adaptive threshold (rolling mean + sensitivity × standard deviation) and a minimum inter-onset interval to avoid duplicate triggers on sustained transients. Uniforms bus (`core/audio_analysis/uniform_bus.py`): thread-safe, EMA-smoothed, clamped-to-[0,1] snapshot (`u_bass`, `u_mid`, `u_treble`, `u_amplitude`, `u_onset`, `u_onset_strength`, `u_time`) ready for consumption by the shader rendering engine (v0.6.0+). `AudioAnalysisEngine` (`core/audio_analysis/engine.py`): orchestrates capture → FFT → onset detection → uniforms bus on each `tick()`; wired into `MainWindow` on a dedicated 60 FPS (~16 ms) timer, active only while a track is playing. Test suite extended (18 new tests: PCM capture/downmix, FFT band discrimination, onset detection/suppression, uniform bus smoothing/clamping, end-to-end engine tick) — 56 tests cumulated, 100% passing. ## [0.4.0] - 2026-08-01 ### Added Manual metadata editing (`EditMetadataDialog`): form to edit title, artist, album, genre, track number and year; writes both the SQLite record (`TrackDAO.update_metadata`) and the file&apos;s ID3 tags via the new `core/metadata/id3_writer.py` (`mutagen`-based, TIT2/TPE1/TALB/TCON/TRCK/TYER). Custom genre management (`GenreManagerDialog` + `GenreDAO.rename/delete`): create, rename and delete genres from the Library menu; deleting a genre unlinks it from tracks instead of deleting them. Playlists (`PlaylistDAO`, `PlaylistPanel`): create, rename, delete; add/remove tracks; drag-and-drop reordering (`reorder`); export to and import from M3U files (`core/library/playlist_io.py`). Rating system (1–5 stars) via right-click context menu on the library table (`LibraryView` context menu: Play, Edit Metadata, Add to Playlist, Set Rating, Delete from Library); ratings persisted through `TrackDAO.set_rating` (clamped to 0–5). Play count already incremented by `PlayerController` since v0.2.0 and now surfaced/edit-able through the same table. `TrackDAO.get_with_details`, `TrackDAO.delete`, `PlaylistDAO.tracks_with_details` and related joined-query helpers. Main window now uses a splitter: library table on the left, playlist panel on the right. Test suite extended (10 new tests: ID3 write, genre CRUD, track metadata update/rating, playlist CRUD/reorder, M3U export/import) — 38 tests cumulated, 100% passing. ## [0.3.0] - 2026-08-01 ### Added Main application window (`MainWindow`, PySide6 `QMainWindow`): library view on top, persistent player bar at the bottom, `File` menu (Scan Folder, Refresh Library, Exit), status bar for scan feedback. Single frozen dark theme (`ui/themes/dark_theme.py`): fixed color palette, `QPalette` + global stylesheet applied via `Fusion` style — no light/dark switch, per project conventions (§8 of ROADMAP). Library table view (`LibraryView`, `LibraryTableModel`, `LibraryFilterProxyModel`): sortable columns (title, artist, album, genre, year, duration, bitrate, rating, play count) via clickable headers, backed by `TrackDAO.list_with_details()` (SQL join across artists/albums/genres). Instant search bar filtering across title/artist/album/genre as the user types, combined with dropdown filters for genre, artist and year (`LibraryFilterProxyModel`). Player bar (`PlayerBar`): transport controls (previous/play-pause/next), volume slider, seek slider with live position/duration labels, wired to `PlayerController`. `TrackDAO.list_with_details()`: joined query returning display-ready rows for the library table. Folder scanning triggered from the UI (`File &gt; Scan Folder…`), reusing `LibraryScanner`, with result/error feedback in the status bar. Test suite extended (8 new tests) covering the table model and the filter/sort proxy (search, genre/artist/year filters, combined filters) — 37 tests cumulated, 100% passing. ## [0.2.0] - 2026-08-01 ### Added `AudioPlayer`: low-latency playback engine wrapping `python-vlc` (load, play, pause, resume, toggle_pause, stop, seek by ms or ratio, volume get/set, state, end/error event hooks). `PlaybackQueue`: playback queue with add/remove/clear and next/previous navigation. `PlaybackHistory`: bounded in-memory playback history. `PlayerController`: orchestrates the player, the queue and the history; increments `play_count` in the database on playback and automatically advances to the next queued track when one ends. Test suite extended to 29 tests, including real headless playback tests against libVLC (dummy audio/video output) and controller integration tests. ### Fixed Potential libVLC deadlock: control calls (`play`, `set_media`, ...) triggered from within a `MediaPlayerEndReached`/`MediaPlayerEncounteredError` callback are now dispatched on a dedicated thread instead of running directly on libVLC&apos;s internal event thread. `Database` connections are now safe to use from the audio callback thread (`check_same_thread=False` + internal lock serializing all statements). ## [0.1.0] - 2026-08-01 ### Added Project scaffolding (`src/`, `pyproject.toml`, `black`/`ruff` configuration). SQLite database layer with a versioned migration system (`core/database`). Initial schema: `artists`, `albums`, `genres`, `tracks`, `playlists`, `playlist_tracks`, `app_settings`. Data models: `Artist`, `Album`, `Genre`, `Track`, `Playlist`. Data-access layer (DAO) for artists, albums, genres and tracks with idempotent get-or-create semantics. ID3 tag reader based on `mutagen` (title, artist, album, genre, track number, year, duration, bitrate). Recursive local-folder scanner that indexes `.mp3` files into the database, updates existing entries, and removes entries for files that no longer exist on disk. Unit test suite (`pytest`) covering the database, the DAO layer and the scanner (11 tests).</description>
  </item>
  <item>
    <title>ShadertoyCameraPath v2.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=shadertoycamerapath</link>
    <guid isPermaLink="false">shadertoycamerapath-2.0.0</guid>
    <pubDate>Wed, 29 Jul 2026 00:00:00 GMT</pubDate>
    <description># Shadertoy Camera Path &amp; Keyframe FX Timeline Studio — v2.0.0 &quot;Ultimate Edition&quot; This release ships the standalone Windows binary (`shadertoy-camera-path-studio.exe`) only. The Inno Setup installer (`.stcp` file association, Start Menu shortcut) is **not** attached to this release yet — it needs to be built on a machine with Inno Setup 6 installed, which this build environment doesn&apos;t have. Run the binary directly (portable mode); see README.md for details. Full per-phase changelog below. -- ## [2.0.0] — 2026-07-29 &quot;Ultimate Edition&quot; ### Added **Phase 16 — v2.0.0 &quot;Ultimate Edition&quot; Certification &amp; Release:** - **16.1 Cross-phase regression:** `cargo test --workspace --all-features --no-fail-fast` passes with zero failures across all five crates. New `slang-codegen/tests/glsl_slang_parity.rs` re-validates GLSL ⇄ Slang round-trip parity at the shared-IR/embedded-literal level (both backends are never actually compiled/executed in this environment — see the test&apos;s own doc comment for exactly what is and isn&apos;t claimed). Offline-isolation audit: confirmed no network-capable crate (`reqwest`/`hyper`/`tokio`/`ureq`/`tungstenite`/`curl`/TLS) appears anywhere in `Cargo.lock`; the one pre-existing caveat is the off-by-default `puffin-profiling` feature&apos;s local-only TCP listener, unchanged from Phase 14. Stress certification reuses Phase 14&apos;s existing 10k-keyframe stress-test generator/test rather than adding a new mechanism - **16.2 Documentation:** new `docs/user_guide.md` covering every panel added in Phases 7–15; new `docs/plugin_sdk.md` stating plainly that the plugin system is a design note, not a shipped feature; `docs/performance.md` gained a section pointing at the Phase 14 Diagnostics panel as the live complement to the static benchmarks. New in-app **Getting Started** panel (View menu) — an offline, paginated Next/Back/topic-list walkthrough, no video/network. Tooltips (`.on_hover_text`) added to every action button across all 14 panel files with interactive controls — Expression Graph (including one tooltip per node type in the node palette), Timeline, Curve Editor, FX Triggers, Diagnostics, Physics &amp; Mocap, Multi-Camera, Asset Library, Layers/Sequences/Variants/History, Export/Preview, Preferences, 3D Viewport, and Code Export — roughly 98 tooltip sites, up from 12 across 3 files. `about.rs`/`getting_started.rs` (self-explanatory static content) and `menu_bar.rs` (egui menus don&apos;t reliably show hover tooltips while open, and labels are already descriptive) were deliberately left alone - **16.3 Final packaging:** `installer/setup.iss`&apos;s `AppVersion` bumped to `2.0.0`; the new docs files are now included in the installed file set. No new bundled binaries exist to add (no `slangc`, no plugin samples) — offline-first isolation at installer level is unchanged from Phase 6 - **16.4 v2.0.0 release:** performed with explicit user authorization. Workspace version bumped to `2.0.0` across all crates; repository URL corrected (`ShadertoyCameraPath`, not the stale `Z-GL-Shadertoy` placeholder) in `Cargo.toml`, `README.md`, `ROADMAP.md`, and the in-app About tab; git repository initialized, reviewed for stray/sensitive files before staging (a personal test certificate, an old project zip backup, a Claude Code session lock file, and unrelated leftover Next.js scaffold files were excluded via `.gitignore`, not committed), single initial commit, pushed to `github.com/Patrickjaillet/ShadertoyCameraPath`, tagged `v2.0.0`, release binary built (`cargo build --release`), and published to GitHub Releases - Tests: 1 new cross-backend parity test **Phase 11 — Slang Language Compatibility Layer (Export &amp; Import):** - New `slang-codegen` workspace crate, sharing `glsl-codegen`&apos;s `TimelineIr`/`ChannelIr`/`KeyframeIr` directly rather than duplicating it. `KeyframeIr` gained optional `in_tangent`/`out_tangent` fields and `InterpolationKindIr` a `CubicBezier` variant (distinct from `Unsupported`) so the shared IR can carry Bezier control-point data — the GLSL backend&apos;s existing zero-fallback behavior for Bezier channels is unchanged, while Slang gets a real cubic-Bernstein template, closing that gap on the Slang side only - **11.1 Export pipeline:** `evaluateTimeline`-equivalent generation, per-channel accessors (`getFOV`, `getCameraPosition`, `getCameraRotation`, `getTrigger`, `getAudioEnvelope`), Catmull-Rom/B-Spline/SLERP/Cubic-Bezier templates in Slang/HLSL syntax (`float3`/`float4`, `lerp`, `atan2`, HLSL-style array initializers instead of GLSL&apos;s `T[N](...)` constructor call), `[shader(&quot;fragment&quot;)]`/`[shader(&quot;compute&quot;)]` entry-point stubs, a `SlangTargetProfile` enum (GLSL 450/ES 300, HLSL SM5/SM6, SPIR-V 1.5, Metal — profile choice recorded in the header comment only), Debug/Readable/Minified verbosity (reusing `glsl-codegen`&apos;s minifier), and a `wrapMod` helper reproducing GLSL&apos;s floored-modulo `mod()` for `Cycle`/`PingPong` extrapolation (Slang/HLSL&apos;s `fmod` truncates toward zero and would wrap the wrong direction) - **11.2 Offline validation:** new `vendor_slangc.sh`/`.bat` scripts fetch the official prebuilt `slangc.exe` (`shader-slang/slang` v2026.14, Windows x86_64) into gitignored `vendor/slangc/` — never bundled into the shipped installer. `slang-codegen/tests/slangc_validation.rs` shells out to it and verifies the generated Slang really compiles for Glsl450/HlslSm5/HlslSm6/Spirv15/Metal, skipping gracefully on a machine that hasn&apos;t run the vendor script; CI fetches and caches it so this runs for real on every push. `entry::validator::validate_structural_balance` still runs as a fast additional syntax check in the golden-file tests. `SlangTargetProfile::known_gap()` for `GlslEs300` documents a real gap found while building this test: `slangc`&apos;s CLI exposes no GLSL ES profile at all (only desktop `glsl_150`..`glsl_460`), so that one profile is excluded from the compile test - **11.3 Import:** a Rust-native parser (`slang_codegen::import`) for the documented subset this crate&apos;s own export produces (`NAME_TIMES[N]`/`NAME_VALUES[N]` array pairs), with per-channel error diagnostics for malformed/mismatched arrays and partial import. Every imported channel reconstructs as `Linear` interpolation regardless of the original mode — keyframe times/values round-trip exactly (verified by an export → import → export test), but the original basis-function template isn&apos;t recovered - **11.4 UI:** a single unified &quot;Code Export&quot; panel (`app/src/ui/code_export_panel.rs`, replacing the earlier separate `glsl_panel.rs`/`slang_panel.rs`) with a GLSL/Slang target toggle and one shared Debug/Readable/Minified verbosity dropdown — the first verbosity *control* in the UI at all; both prior panels had hardcoded Readable with no picker. &quot;GLSL Export&quot;/&quot;Slang Export&quot; menu entries and `Ctrl+4`/`Ctrl+0` shortcuts still work, each pre-selecting that target. &quot;Import Slang…&quot; is a button shown when the Slang target is selected. The &quot;About&quot; tab now reports the `slangc` version that validated the build (via `app/build.rs` shelling out to `vendor/slangc/bin/slangc.exe -v` at compile time, baked in as `env!(&quot;SLANGC_VALIDATION_VERSION&quot;)`), falling back to an explicit &quot;not available&quot; message when built without `slangc` vendored. Bug caught while wiring this up: `slangc -v` prints its version to stderr, not stdout — `build.rs` was written checking stderr first after confirming this directly, not assuming it - Tests: 39 new tests in `slang-codegen` (31 unit + 4 golden-file + 2 round-trip + 1 real `slangc` compile check + 1 GLSL⇄Slang parity check) **Phase 15 — Accessibility, Customization &amp; Professional Input Devices:** - **15.1 Accessibility:** eframe&apos;s `&quot;accesskit&quot;` feature is enabled, giving every existing labeled widget an automatically-derived accessible name for screen readers with no per-widget app code. Keyboard-only navigation uses `egui`&apos;s built-in Tab/Shift+Tab focus-cycling, extended by the new unified keybinding dispatch (see 15.2) so panel switches and transport commands also have a keyboard path. `theme::UiDensity` (Compact/Comfortable/Spacious) scales spacing/padding/interact-size on top of the White theme. `theme::TRACK_COLORS_COLORBLIND_SAFE` (Okabe-Ito palette) is a selectable alternative to the default track-color palette, wired into the Timeline track headers and Curve Editor curve lines - **15.2 Keybinding &amp; Workflow Profiles:** new `app/src/keybindings.rs` (`BindableKey`/`KeyCombo`/`Action`/`KeyBindings`) replaces the previous hardcoded shortcut `if` blocks in `app.rs` with a remappable, persisted (JSON) action-dispatch table; `default_profile`/`blender_style_profile`/`adobe_style_profile` presets are selectable (not auto-applied). New `app/src/workspace_layout.rs` (`WorkspaceLayout`/`WorkspaceLayoutStore`) saves/restores named presets of active panel + Timeline/Curve Editor pan-zoom + 3D viewport camera state — honestly scoped to this app&apos;s single-active-panel architecture rather than a docking-layout system, since `egui_dock` is a listed but entirely unused dependency - **15.3 Professional Input Devices:** new `app/src/input_devices/midi.rs` (via `midir`) parses MIDI CC messages and maps them to app targets (default: CC1 mod-wheel scrubs the playhead); new `app/src/input_devices/gamepad.rs` (via `gilrs`) polls stick axes each frame to orbit/pan the 3D viewport camera, with deadzone handling. Both degrade gracefully with a status label when no device is connected. All CC-parsing, mapping, and stick-to-camera math is pure and hardware-independent, giving the `app` crate its first-ever unit test suite - **15.4 Multi-Monitor Support:** `StudioApp::show_floating_viewport` pops the 3D viewport out into a real OS-level `egui` viewport (draggable to any monitor), falling back to an embedded `egui::Window` on backends without multi-viewport support; a fullscreen toggle sends `ViewportCommand::Fullscreen(true)` to that floating viewport specifically. Per-monitor DPI scaling is automatic and native to `winit`/`egui-winit`; a manual `Preferences::dpi_scale_override` slider (`ctx.set_zoom_factor`) is layered on top for users who want a different scale than native - New &quot;Preferences&quot; panel (`app/src/ui/preferences_panel.rs`, View menu entry) exposes all of the above: density/palette toggles, keybinding grid with click-to-rebind, workspace layout save/apply/delete, MIDI/gamepad enable + status, floating-viewport + fullscreen + DPI-override controls - New dependencies: `midir`, `gilrs` (app crate only), plus eframe&apos;s `&quot;accesskit&quot;` feature - Tests: 23 new unit tests (keybinding matching/rebind/save-load, workspace layout upsert/find/remove/round-trip, MIDI CC parsing/mapping, gamepad deadzone/orbit math) **Phase 14 — Advanced Diagnostics, Profiling &amp; Auto-Optimization:** - **14.1 Real-Time Performance HUD:** `profile_timeline_evaluation` times a real (wall-clock, if coarse at curve-evaluation timescales) evaluation pass over every non-muted track, reporting total duration, active keyframe count, and a per-track breakdown; `PerformanceHistory` is a session-scoped capped ring buffer with an offline CSV export. App: a &quot;Perf HUD&quot; viewport toggle overlays eval/frame time and keyframe count live; the Diagnostics panel shows the per-track cost table, a rolling frame-time graph, and &quot;Export Session Log (CSV)&quot;. Note: there&apos;s no separately measurable &quot;GPU frame time&quot; in this architecture — the 3D viewport is CPU-painter-drawn, not a real wgpu render pass — so only CPU-side timings are reported, documented as such rather than fabricated - **14.2 Shader Complexity Analysis:** `estimate_instruction_count` is a documented text-level heuristic (statement/operator/call counts), not a real compiler-verified instruction count; `platform_tier_report` checks it against rough guideline thresholds for Mobile/Integrated-GPU/Desktop. `detect_optimization_suggestions` finds constant-folding no-ops (`* 1.0`, `+ 0.0`, …) and always-true/always-false `if` branches (with real brace-matching for safely removing dead `if (false)` blocks); `apply_with_diff` performs the byte-range replacements and returns a before/after preview for the one-click &quot;Apply Suggested Optimizations&quot; action. New module `glsl-codegen/src/analysis/` (`complexity.rs`, `suggestions.rs`) - **14.3 Curve Simplification &amp; LOD:** `simplify_curve` runs an iterative (non-recursive, stack-overflow-safe) Douglas-Peucker pass over each keyframe&apos;s `(time, value)` or `(time, x, y, z)` point, preserving both timing and shape within a tolerance — exact when kept segments are evaluated with linear interpolation, documented as approximate for curved interpolation modes between kept keyframes. `LodPreset` (Desktop/Integrated GPU/Mobile) wraps built-in tolerance presets. New module `core-engine/src/curve_simplify.rs`. App: tolerance slider + live preview + per-preset buttons in the Diagnostics panel, applied to the selected curve via undo-tracked `FullSnapshot` - **14.4 Memory &amp; Stability Profiling:** new optional `heap-profiling` Cargo feature (mirrors the existing `puffin-profiling` pattern) wires `dhat` as the global allocator and writes `dhat-heap.json` for offline inspection with the dhat viewer — opt-in, adds nothing to a normal build. A panic hook (`app/src/crash_report.rs`) writes a local, never-transmitted crash report (timestamp, panic location/message, backtrace) to the app data directory; the Diagnostics panel lists captured reports. `generate_stress_test_project` builds a synthetic multi-track, multi-thousand-keyframe timeline for stability testing, exposed both as a core-engine test fixture (asserts a 10k-keyframe evaluation pass completes without panicking within budget) and as a &quot;Generate Stress-Test Project&quot; Diagnostics panel action - New module `core-engine/src/diagnostics/` (`performance.rs`, `stress_test.rs`). App: new &quot;Diagnostics&quot; panel bringing all of the above together - Tests: 8 new performance/stress-test unit tests, 7 curve-simplification tests, 12 shader-analysis tests **Phase 4.3 — Tangent handle gizmos &amp; multi-keyframe group manipulation:** the 3D viewport&apos;s translation gizmo now actually works — fixed a pre-existing bug where the picked axis was never stored, making the drag code unreachable. `ViewportState::active_gizmo_handle: Option&lt;GizmoHandle&gt;` is now set by hit-testing on drag-start (`Position(axis)`, `Group(axis)`, `InTangent`, `OutTangent`) and cleared on drag-stop. Selecting a single `Vec3`/`CubicBezier` keyframe now shows its incoming/outgoing tangent handles as small diamonds with a connector line, freely draggable in the camera&apos;s yaw-relative plane; selecting multiple keyframes shows one group gizmo at their centroid that moves them all together, preserving relative offsets. New `Command::MoveTangent` undo entry for tangent edits. ### Changed **Native file picker replaces typed-path dialogs:** Open/Save Project, audio import, BVH mocap import, and asset-library trajectory-template export/import now use a real native Windows file dialog (`rfd`) instead of a text field the user had to type a path into by hand. This supersedes the earlier &quot;simple path-entry dialog pattern&quot; (documented in the Phase 3.5 status note above) that was originally chosen specifically to avoid adding a file-dialog dependency; `rfd` is a native OS-dialog wrapper with no network calls, so it doesn&apos;t conflict with the offline-first requirement. `ProjectPathDialog`/`ProjectDialogMode`/`AudioImportDialog` and their `path_buf` fields are removed from `AppState` in favor of a plain `Option&lt;String&gt;` error per feature, shown in a small dismissible window only on failure — the OS dialog itself already handles browsing/cancel/filename entry, so a custom modal for the happy path is no longer needed. New dependency: `rfd` (app crate only). ### Added **Phase 12 — Non-Linear Timeline, Layers &amp; Version History:** - **12.1 Timeline Layers:** `LayerStack`/`TimelineLayer` — stacked, named layers (`Base`/`Override`/`Additive` blend modes, per-layer opacity, solo/mute/lock) composited by matching track name across layers; `evaluate_track` blends (lerp for Base/Override, sum for Additive) scalar/position/rotation curve values; `merge_down` flattens a layer into the one beneath it, baking a resampled blend for `Base`/`Additive` or keeping an `Override` layer&apos;s curves verbatim, while tracks unique to either side pass through untouched. Additive to the existing single-`tracks` timeline model — projects that never touch layers are unaffected - **12.2 Nested Sequences:** `SubSequence`/`SubSequenceInstance`/`SubSequenceRegistry` — reusable named track bundles placed as time-remapped instances (start offset, speed, optional loop) either on the master timeline or nested inside another sub-sequence; `validate_no_circular_nesting` DFS-detects both direct self-reference and indirect cycles through multiple sequences, and `add_nested_instance` rejects (and rolls back) any addition that would create one - **12.3 Variants (A/B Takes):** `VariantStore`/`TimelineVariant` — full named snapshots of the current tracks kept alongside the master, non-destructively; `diff_tracks` compares two track lists by matching tracks on name and keyframes on id (added/removed/modified), shared by both variant-vs-master and snapshot-vs-snapshot diffing; `merge_keyframes_from_variant` selectively promotes individual keyframes from a variant into master - **12.4 Version History &amp; Annotations:** `SnapshotManager` — filesystem-backed, offline history of full `.stcp` project snapshots with a JSON index; `restore_with_backup` always snapshots the current project first, so restoring can never silently discard unsaved work; `diff_snapshots` reuses the 12.3 diff algorithm; `MarkerTrack`/`TimelineMarker` — free-text annotations pinned to time positions, kept time-sorted, with radius-based lookup - New module `core-engine/src/sequencing/` (`layers.rs`, `subsequence.rs`, `variant.rs`, `version_history.rs`, `marker.rs`), re-exported from the crate root. `TimelineState` gains `layer_stack`, `sub_sequences`, `variants`, `markers` fields (all serde-default, non-breaking) - App: new &quot;Layers / Sequences / Variants / History&quot; panel — layer stack editor (add/reorder/blend-mode/opacity/solo/mute/lock/duplicate/merge-down), sub-sequence manager with drill-in/drill-out navigation (temporarily swaps the Timeline/Curve Editor panels onto the sub-sequence&apos;s own tracks, with an explicit save-or-discard on drill-out) and root/nested instance placement, variant creation/diff/apply/selective-merge, and snapshot creation/diff/restore-with-confirmation plus marker add/remove/jump-to-time - Note: layer composition (`evaluate_track`) and sub-sequence instance time-remapping are engine-level capabilities with app-side editing UI, but are not yet wired into the live camera/GLSL evaluation pipeline that drives the 3D viewport, preview, and export — that integration is a natural follow-up, not part of this change - Tests: 37 new unit tests across the five new modules (blend composition incl. solo/mute, merge-down for all three blend modes, time remap incl. looping, self/indirect cycle rejection with rollback, diff add/remove/modify detection, selective merge, snapshot create/load/persist-across-reload/restore-with-backup/delete, marker sort/lookup) **Phase 13.2 — Asset &amp; Preset Library:** - `AssetLibrary`: local, flat store of `CurvePreset` (single tagged/categorized `Curve`) and `TrajectoryTemplate` (tagged/categorized set of `Track`s) entries, with case-insensitive name/category/tag search, per-entry favoriting, and a deduplicated MRU &quot;recently used&quot; list - Shareable fragment export/import: `save_template_fragment` / `load_template_fragment` write a single `TrajectoryTemplate` to its own `.stcp` fragment file (a `fragment_kind`-discriminated JSON document, distinct from a full `Project` file) for sharing between projects - Thumbnail generation reuses the existing offline CPU frame renderer from the Phase 8 export pipeline (`render_frame_rgba8`) rather than introducing a second render path: a curve preset is wired in as a synthetic camera-position track, a template renders its own tracks directly, both sampled at the mid-duration point and encoded to PNG bytes via the existing `write_png_rgba8` - New module `core-engine/src/library/` (`preset.rs`, `fragment_io.rs`, `thumbnail.rs`), re-exported from the crate root - App: &quot;Asset Library&quot; panel — search, per-entry thumbnail + favorite/apply/delete, save the selected curve as a preset or the selected track as a template, export/import template fragments, favorites &amp; recently-used quick-access strip. Library persists to `asset_library.json` in the app data directory, independent of any single project - Tests: `AssetLibrary` CRUD/search/favorites/MRU/round-trip, fragment save/load and rejection of non-fragment files, thumbnail pixel-buffer sizing and PNG signature **Phase 10.3 — Motion Capture Import:** - Dependency-free recursive-descent BVH (Biovision Hierarchy) parser: `HIERARCHY` skeleton (joint offsets, per-joint channel layout, `End Site` leaves) and `MOTION` per-frame channel data, local-file-only (offline-first) - Forward-kinematics retargeting (`retarget_to_camera_path`): evaluates world-space position/rotation of a chosen joint per frame (root or any end effector) via a single forward pass over the parent-before-child joint order - `MocapSmoothingFilter`: centered moving-average noise reduction with a configurable window radius, applied to both position and (hemisphere-corrected) rotation - Non-destructive preview → user-approved bake flow: `bake_mocap_position_to_curve` / `bake_mocap_rotation_to_curve` insert keyframes only once the retargeted+filtered trajectory is committed - New module `core-engine/src/mocap/` (`bvh.rs`, `retarget.rs`, `filter.rs`), re-exported from the crate root - App: &quot;Physics &amp; Mocap&quot; panel — BVH file load, joint picker, scale/offset/smoothing controls, retarget preview, bake to the selected curve - Tests: BVH parser tests against a fixture file (`core-engine/tests/fixtures/simple_arm.bvh`), FK retargeting tests (root position channel, parent offset composition, scale/offset), smoothing filter tests, bake tests **Phase 10.2 — Rigid-Body &amp; Collision Awareness:** - `RigidBodySolver`: mass/stiffness/damping spring-damper integrator (semi-implicit Euler, unconditionally stable) for camera spring/inertia behavior, with a `critical_damping` helper and optional max-speed clamp - `ObstacleVolume` (sphere/box) + `ObstacleSet`: user-defined keep-out volumes with a configurable clearance margin and signed-distance queries - `preview_collision_avoidance`: non-destructive per-sample suggestion that pushes path positions penetrating an obstacle&apos;s margin-inflated surface back outside it; `bake_collision_avoidance` commits an approved preview into curve keyframes - `min_distance_diagnostic`: samples a curve&apos;s clearance to the nearest obstacle over time, for a diagnostic graph overlay - New module `core-engine/src/camera/physics.rs` and `core-engine/src/camera/collision.rs`, re-exported from `camera` and the crate root - App: &quot;Physics &amp; Mocap&quot; panel — rigid-body spring preview (settle/overshoot readout), obstacle list editor, collision-avoidance preview + bake, min-clearance readout - `TimelineState` gains `obstacles: ObstacleSet` (serde-default, non-breaking) - Tests: unit tests in `physics.rs` (critical/underdamped settle behavior, max-speed clamp, zero-dt no-op) and `collision.rs` (signed distance for both shapes, avoidance preview, bake, diagnostic, margin) **Phase 10.1 — Path Constraint Solver:** - `FollowSplineConstraint`: locks camera position to any position `Curve`, with either a world-space offset or a path-relative (right/up/forward) offset that stays consistent as the path turns - `BankingSolver`: auto-roll angle derived from the path&apos;s local curvature (`|v x a| / |v|^3`, finite-difference), signed by turn direction and clamped to a configurable max roll - `LookAheadSolver`: samples the path ahead of the current time for predictive orientation aiming, holding at the curve&apos;s end instead of extrapolating past it - `ArcLengthTable` + `SpeedNormalizationSolver`: builds an arc-length lookup table (binary-search distance→time) so playback along arbitrarily/unevenly keyframed curves can be reparameterized to constant velocity - New module `core-engine/src/camera/path_constraint.rs`, re-exported from `camera` and the crate root - Tests: unit tests in `path_constraint.rs` (straight line, quarter-circle curvature, arc-length round-trip, constant-speed remap) ### Fixed **Position-curve Catmull-Rom evaluation bug:** `evaluate()` was returning `Value::Scalar(0.0)` for any `Vec3` (position) curve using a Catmull-Rom interpolation mode — the mode `Keyframe::new_position` uses by default — instead of interpolating `position_value` component-wise. This silently froze default-created position curves in place. Fixed by dispatching Catmull-Rom evaluation on `channel_type`, the same way the existing `CubicBezier` branch already does. Regression tests added in `core-engine/tests/catmull_rom_position_regression.rs`. **Phase 9 — Multi-Camera Rig, Switching &amp; Compositing System:** - `CameraRig` data model with parent/child hierarchy, mute/active flags, color tags, auto-bank, shared look-at binding - Non-breaking `TimelineState` fields: `camera_rigs`, `cut_track`, `shared_look_at_targets` (serde defaults) - Cut track with hard cuts and crossfade/blend transitions + easing - `evaluate_active_camera` with hierarchy transform composition and blend during transitions - Director monitor compositor: Single / Split H-V / Quad / Picture-in-Picture layouts with on-screen labels - GLSL `getActiveCamera(float time)` emitter (`glsl-codegen/src/camera`) - Multi-Cam panel: rig list, cut editing, director monitor, shared targets, active-camera readout - Tests: `core-engine/tests/phase9_multicam.rs` ### Added **Phase 8 — Studio-Grade Rendering, Preview &amp; Video Export Pipeline:** - Offline CPU frame renderer driven by timeline camera evaluation (Shadertoy-resolution presets 640x360 → 1920x1080) - Live preview panel with hot-reload on timeline revision, split/wipe compare, pixel inspector, histogram &amp; waveform scopes - Color management: exposure EV, gamma, Reinhard/ACES tone-map, colorblind simulation, `.cube` LUT import with strength blend - Depth-of-field and motion-blur preview approximations + velocity-vector overlay - Frame-accurate export job queue (background thread, progress, cancel) writing PNG sequence, BMP sequence, or uncompressed AVI — zero network / no external ffmpeg required at runtime - Metadata sidecar (`*.meta.txt`) with copyright + version tag - Core modules: `core-engine/src/color/*`, `core-engine/src/export/*`; app panel `Export / Preview` - Tests: `core-engine/tests/phase8_export.rs` ### Added **Phase 7 — Procedural &amp; Expression-Driven Animation Engine:** - Node-based expression graph IR (`ExpressionGraph` / `NodeKind` / typed sockets) with cycle detection and topological evaluation - Math, noise (Perlin / Simplex / Worley / FBM), time-driver, channel-reference, and conditional nodes - Text expression language (recursive-descent parser, AST eval, ternary/compare, safe function set) - Per-keyframe `expression_override` field on `Keyframe` - Physically-based secondary motion drivers: spring-damper, camera-shake, overshoot/settle with non-destructive stacking and bake-to-keyframes - GLSL ES codegen for expression AST + offline noise library (`scpsPerlin2` / `scpsSimplex2` / `scpsWorley2` / `scpsFbm2`) and unsupported-construct diagnostics - Expressions panel in the app (text override editor, node canvas, driver stack controls) - Tests: `core-engine/tests/expression_phase7.rs` plus unit tests in expression modules ### Fixed **Toolchain MSRV mismatch:** bumped `rust-toolchain.toml` from `1.87.0` to `1.89.0` so the workspace builds against current locked dependencies (`wide` 1.5 / `safe_arch` 1.1 require rustc 1.89; `home` 0.5.12, `image` 0.25.10, and `time` 0.3.54 require rustc 1.88+).</description>
  </item>
  <item>
    <title>Sliders-GL v1.0.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=sliders-gl</link>
    <guid isPermaLink="false">sliders-gl-1.0.1</guid>
    <pubDate>Tue, 28 Jul 2026 00:00:00 GMT</pubDate>
    <description>## Sliders GL v1.0.1 ### Fixed Accessibility: fixed several WCAG 2.1 AA color-contrast issues across the gray theme — control borders, status toasts, the active pass/workspace tabs, viewport HUD readouts, and status-bar text now all meet or exceed the required contrast ratios. Accessibility: the Outliner and Inspector panels are now reachable and scrollable by keyboard. See [CHANGELOG.md](https://github.com/Patrickjaillet/Sliders-GL/blob/main/CHANGELOG.md) for the full history.</description>
  </item>
  <item>
    <title>Sliders-GL v1.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=sliders-gl</link>
    <guid isPermaLink="false">sliders-gl-1.0.0</guid>
    <pubDate>Sun, 26 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to Sliders GL are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [1.0.0] — 2026-07-26 ### Added Initial public release of **Sliders GL**, a full rebrand of the Z-GL Shadertoy project into a standalone, Blender-style GLSL shader editor. Blender-identical UI architecture: topbar, tool shelf, tabbed Properties editor, status bar, draggable/collapsible areas, and area-split gizmos. Unique light/medium-gray theme. Ultra-professional, high-precision sliders for every shader uniform, with keyboard fine control and undo/redo. Monaco-based GLSL editor with syntax highlighting, inline diagnostics, autocomplete, and code formatting. ShaderToy-compatible rendering (`mainImage`, `iResolution`, `iTime`, `iMouse`, channel uniforms) and direct ShaderToy import by ID or URL. Export pipeline: current-frame image export, video recording, standalone HTML export, and full project ZIP export. Shader library and preset system. In-app Help Center with a full reference and keyboard shortcut list. &quot;About&quot; tab with copyright, contact, and website information. Offline-first architecture: all runtimes, fonts, and tooling are bundled locally — zero network dependency for core functionality. MIT license. [1.0.0]: https://github.com/Patrickjaillet/Sliders-GL/releases/tag/v1.0.0</description>
  </item>
  <item>
    <title>GLConverter v1.0.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-1.0.1</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [1.0.0] - 2026-07-25 ### Added Official software screenshot (`docs/screenshot.png`) referenced from the `README.md`. ### Changed `README.md` finalized for the `v1.0.0` release: usage instructions, keyboard shortcuts, build/deployment steps, and copyright/contact information reviewed and brought up to date. Project marked stable: first public `v1.0.0` release, matching the `package.json` version. ## [0.9.0] - 2026-07-25 ### Added Full automated test suite (Vitest): unit tests for every golfing technique (dead code elimination, declaration merging, loop golfing, ternary/`&amp;&amp;` conditional golfing, native call shortcuts, variable renaming, operator simplification), unit tests for every de-golfing technique (conditional expansion, loop expansion, operator expansion, block normalization, descriptive renaming), unit tests for the equivalence validator, the parser, the language detector, the session history store, and the JS/WASM engine bridge, and interface integration tests exercising the mounted application (mode and direction toggles, rules panel, keyboard shortcuts, engine badge). GitHub Pages compatibility verification script (`npm run verify:pages`), run automatically at the end of every production build, checking that every emitted asset reference is relative. `npm test` now runs before the build step in the GitHub Actions deployment workflow. ### Changed The Three.js background scene and the engine benchmark module are now loaded on demand via dynamic `import()` instead of being bundled into the application&apos;s initial entry chunk. Production build output is now split into cacheable vendor chunks (Three.js, CodeMirror, the AST/golfing engine) instead of a single monolithic bundle, reducing the initial entry chunk from roughly 1 MB to about 36 kB and removing the Vite large-chunk warning. ## [0.8.0] - 2026-07-25 ### Added Global keyboard shortcuts, active anywhere in the application: `Alt+G` toggles Golf / De-golf, `Alt+M` toggles Minified / Justified, `Alt+C` copies the converted code, `Alt+E` opens Export, `Alt+H` opens History, `Alt+B` opens Benchmark, `Alt+R` opens Rules, `Alt+I` opens Import, and `Escape` closes any open panel. Every corresponding button now shows its shortcut in a tooltip. Keyboard-visible focus outline applied consistently across every interactive element, and `aria-expanded` / `aria-haspopup` states kept in sync on all dropdown-style panels for assistive technology. Reactive Three.js background transition: the point-field background now animates a brief pulse (speed, size, and opacity) on every conversion, and respects the operating system&apos;s reduced-motion preference by disabling the animation entirely when requested. ### Changed Refined responsive breakpoints for tablet and mobile viewports: the header and its controls now wrap cleanly, dropdown panels (History, Benchmark, Export, Rules) constrain themselves to the viewport width instead of overflowing on narrow screens, and the status bar and header actions reflow on small screens. ## [0.7.0] - 2026-07-25 ### Added Local file import: drag and drop a `.js` or `.txt` file onto the original editor panel, or use the new &quot;Import&quot; button to pick a file, to load its content for conversion. Export of the converted code as a downloadable `.js` or `.txt` file from the new &quot;Export&quot; menu in the converted panel. One-click &quot;Copy&quot; button to copy the converted code to the clipboard, with transient confirmation feedback. Session-local conversion history: every distinct edit to the original code is recorded (debounced) with its timestamp, direction, and compression ratio, browsable from the new &quot;History&quot; panel and restorable with a single click. The history is kept in memory only and is cleared on page reload. Undo / Redo controls for the original editor, exposed both as header buttons and through the existing keyboard shortcuts. ## [0.6.0] - 2026-07-25 ### Added Rust engine compiled to WebAssembly, providing a high-performance implementation of the whitespace-compaction pass and lexical token counting used by the minified output and status bar. Transparent engine bridge: the application attempts to load the compiled WASM engine on startup and automatically falls back to the existing JavaScript implementation when the WASM module has not been built or fails to load, with no change in behavior or output for the end user. &quot;Engine&quot; badge in the header indicating whether the active engine is WASM or the JS fallback. Integrated performance benchmark panel comparing the JS and WASM engines on the current source across a fixed number of iterations, with per-task timing bars and a computed speedup factor. `npm run build:wasm` script compiling the Rust engine with `wasm-pack`, wired into the production build and into the GitHub Actions deployment workflow. ## [0.5.0] - 2026-07-25 ### Added Reverse conversion (de-golfing): golfed code can now be converted back into structured, readable code. Restoration of readable variable names: identifiers are renamed according to their role (function names, parameters, loop counters, general values) instead of the original single-letter golfed names. Restoration of indentation and block structure: single-statement bodies are re-wrapped in blocks and the result is reformatted with clean indentation. Heuristic reconstruction of common golfed patterns: ternary and `&amp;&amp;`-shortcut conditionals are expanded back into `if`/`else` statements, countdown `for` loops are expanded back into ascending loops, and double-negation / falsy-literal boolean shortcuts are expanded back into their explicit form. Functional equivalence validation: after de-golfing, the reconstructed code is re-golfed and compared against a canonical signature of the original input to confirm the conversion is behavior-preserving, shown as an &quot;Equivalence&quot; status badge. Direction toggle in the interface to switch between &quot;Golf&quot; (structured to golfed) and &quot;De-golf&quot; (golfed to structured) conversion. ## [0.4.0] - 2026-07-25 ### Added Advanced loop golfing: counting `for` loops are rewritten as reverse countdown loops when the loop variable is unused in the body and the bound is safe to evaluate once, shortening the loop header. Ternary and conditional golfing: `if`/`else` statements are rewritten as conditional (ternary) expressions for matching `return` statements and matching assignments, and single-branch `if` statements using an expression are rewritten with the `&amp;&amp;` short-circuit shortcut. Native call golfing: known JavaScript golfing shortcuts are applied automatically, including `Boolean(x)` to `!!x`, `indexOf` presence checks to `includes`, `Array.prototype` to `[]`, and `.apply(null, x)` calls to spread syntax. Configurable golfing rules panel: each golfing technique (dead code elimination, declaration merging, loop golfing, ternary golfing, native call golfing, variable renaming, operator simplification) can be toggled on or off independently, with the converted output updating immediately. Character count display for both the original and converted code. Compression score, shown as a percentage of characters saved by the conversion. ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>GLConverter v1.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-1.0.0</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [1.0.0] - 2026-07-25 ### Added Official software screenshot (`docs/screenshot.png`) referenced from the `README.md`. ### Changed `README.md` finalized for the `v1.0.0` release: usage instructions, keyboard shortcuts, build/deployment steps, and copyright/contact information reviewed and brought up to date. Project marked stable: first public `v1.0.0` release, matching the `package.json` version. ## [0.9.0] - 2026-07-25 ### Added Full automated test suite (Vitest): unit tests for every golfing technique (dead code elimination, declaration merging, loop golfing, ternary/`&amp;&amp;` conditional golfing, native call shortcuts, variable renaming, operator simplification), unit tests for every de-golfing technique (conditional expansion, loop expansion, operator expansion, block normalization, descriptive renaming), unit tests for the equivalence validator, the parser, the language detector, the session history store, and the JS/WASM engine bridge, and interface integration tests exercising the mounted application (mode and direction toggles, rules panel, keyboard shortcuts, engine badge). GitHub Pages compatibility verification script (`npm run verify:pages`), run automatically at the end of every production build, checking that every emitted asset reference is relative. `npm test` now runs before the build step in the GitHub Actions deployment workflow. ### Changed The Three.js background scene and the engine benchmark module are now loaded on demand via dynamic `import()` instead of being bundled into the application&apos;s initial entry chunk. Production build output is now split into cacheable vendor chunks (Three.js, CodeMirror, the AST/golfing engine) instead of a single monolithic bundle, reducing the initial entry chunk from roughly 1 MB to about 36 kB and removing the Vite large-chunk warning. ## [0.8.0] - 2026-07-25 ### Added Global keyboard shortcuts, active anywhere in the application: `Alt+G` toggles Golf / De-golf, `Alt+M` toggles Minified / Justified, `Alt+C` copies the converted code, `Alt+E` opens Export, `Alt+H` opens History, `Alt+B` opens Benchmark, `Alt+R` opens Rules, `Alt+I` opens Import, and `Escape` closes any open panel. Every corresponding button now shows its shortcut in a tooltip. Keyboard-visible focus outline applied consistently across every interactive element, and `aria-expanded` / `aria-haspopup` states kept in sync on all dropdown-style panels for assistive technology. Reactive Three.js background transition: the point-field background now animates a brief pulse (speed, size, and opacity) on every conversion, and respects the operating system&apos;s reduced-motion preference by disabling the animation entirely when requested. ### Changed Refined responsive breakpoints for tablet and mobile viewports: the header and its controls now wrap cleanly, dropdown panels (History, Benchmark, Export, Rules) constrain themselves to the viewport width instead of overflowing on narrow screens, and the status bar and header actions reflow on small screens. ## [0.7.0] - 2026-07-25 ### Added Local file import: drag and drop a `.js` or `.txt` file onto the original editor panel, or use the new &quot;Import&quot; button to pick a file, to load its content for conversion. Export of the converted code as a downloadable `.js` or `.txt` file from the new &quot;Export&quot; menu in the converted panel. One-click &quot;Copy&quot; button to copy the converted code to the clipboard, with transient confirmation feedback. Session-local conversion history: every distinct edit to the original code is recorded (debounced) with its timestamp, direction, and compression ratio, browsable from the new &quot;History&quot; panel and restorable with a single click. The history is kept in memory only and is cleared on page reload. Undo / Redo controls for the original editor, exposed both as header buttons and through the existing keyboard shortcuts. ## [0.6.0] - 2026-07-25 ### Added Rust engine compiled to WebAssembly, providing a high-performance implementation of the whitespace-compaction pass and lexical token counting used by the minified output and status bar. Transparent engine bridge: the application attempts to load the compiled WASM engine on startup and automatically falls back to the existing JavaScript implementation when the WASM module has not been built or fails to load, with no change in behavior or output for the end user. &quot;Engine&quot; badge in the header indicating whether the active engine is WASM or the JS fallback. Integrated performance benchmark panel comparing the JS and WASM engines on the current source across a fixed number of iterations, with per-task timing bars and a computed speedup factor. `npm run build:wasm` script compiling the Rust engine with `wasm-pack`, wired into the production build and into the GitHub Actions deployment workflow. ## [0.5.0] - 2026-07-25 ### Added Reverse conversion (de-golfing): golfed code can now be converted back into structured, readable code. Restoration of readable variable names: identifiers are renamed according to their role (function names, parameters, loop counters, general values) instead of the original single-letter golfed names. Restoration of indentation and block structure: single-statement bodies are re-wrapped in blocks and the result is reformatted with clean indentation. Heuristic reconstruction of common golfed patterns: ternary and `&amp;&amp;`-shortcut conditionals are expanded back into `if`/`else` statements, countdown `for` loops are expanded back into ascending loops, and double-negation / falsy-literal boolean shortcuts are expanded back into their explicit form. Functional equivalence validation: after de-golfing, the reconstructed code is re-golfed and compared against a canonical signature of the original input to confirm the conversion is behavior-preserving, shown as an &quot;Equivalence&quot; status badge. Direction toggle in the interface to switch between &quot;Golf&quot; (structured to golfed) and &quot;De-golf&quot; (golfed to structured) conversion. ## [0.4.0] - 2026-07-25 ### Added Advanced loop golfing: counting `for` loops are rewritten as reverse countdown loops when the loop variable is unused in the body and the bound is safe to evaluate once, shortening the loop header. Ternary and conditional golfing: `if`/`else` statements are rewritten as conditional (ternary) expressions for matching `return` statements and matching assignments, and single-branch `if` statements using an expression are rewritten with the `&amp;&amp;` short-circuit shortcut. Native call golfing: known JavaScript golfing shortcuts are applied automatically, including `Boolean(x)` to `!!x`, `indexOf` presence checks to `includes`, `Array.prototype` to `[]`, and `.apply(null, x)` calls to spread syntax. Configurable golfing rules panel: each golfing technique (dead code elimination, declaration merging, loop golfing, ternary golfing, native call golfing, variable renaming, operator simplification) can be toggled on or off independently, with the converted output updating immediately. Character count display for both the original and converted code. Compression score, shown as a percentage of characters saved by the conversion. ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>GLConverter v0.9.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-0.9.1</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [0.9.0] - 2026-07-25 ### Added Full automated test suite (Vitest): unit tests for every golfing technique (dead code elimination, declaration merging, loop golfing, ternary/`&amp;&amp;` conditional golfing, native call shortcuts, variable renaming, operator simplification), unit tests for every de-golfing technique (conditional expansion, loop expansion, operator expansion, block normalization, descriptive renaming), unit tests for the equivalence validator, the parser, the language detector, the session history store, and the JS/WASM engine bridge, and interface integration tests exercising the mounted application (mode and direction toggles, rules panel, keyboard shortcuts, engine badge). GitHub Pages compatibility verification script (`npm run verify:pages`), run automatically at the end of every production build, checking that every emitted asset reference is relative. `npm test` now runs before the build step in the GitHub Actions deployment workflow. ### Changed The Three.js background scene and the engine benchmark module are now loaded on demand via dynamic `import()` instead of being bundled into the application&apos;s initial entry chunk. Production build output is now split into cacheable vendor chunks (Three.js, CodeMirror, the AST/golfing engine) instead of a single monolithic bundle, reducing the initial entry chunk from roughly 1 MB to about 36 kB and removing the Vite large-chunk warning. ## [0.8.0] - 2026-07-25 ### Added Global keyboard shortcuts, active anywhere in the application: `Alt+G` toggles Golf / De-golf, `Alt+M` toggles Minified / Justified, `Alt+C` copies the converted code, `Alt+E` opens Export, `Alt+H` opens History, `Alt+B` opens Benchmark, `Alt+R` opens Rules, `Alt+I` opens Import, and `Escape` closes any open panel. Every corresponding button now shows its shortcut in a tooltip. Keyboard-visible focus outline applied consistently across every interactive element, and `aria-expanded` / `aria-haspopup` states kept in sync on all dropdown-style panels for assistive technology. Reactive Three.js background transition: the point-field background now animates a brief pulse (speed, size, and opacity) on every conversion, and respects the operating system&apos;s reduced-motion preference by disabling the animation entirely when requested. ### Changed Refined responsive breakpoints for tablet and mobile viewports: the header and its controls now wrap cleanly, dropdown panels (History, Benchmark, Export, Rules) constrain themselves to the viewport width instead of overflowing on narrow screens, and the status bar and header actions reflow on small screens. ## [0.7.0] - 2026-07-25 ### Added Local file import: drag and drop a `.js` or `.txt` file onto the original editor panel, or use the new &quot;Import&quot; button to pick a file, to load its content for conversion. Export of the converted code as a downloadable `.js` or `.txt` file from the new &quot;Export&quot; menu in the converted panel. One-click &quot;Copy&quot; button to copy the converted code to the clipboard, with transient confirmation feedback. Session-local conversion history: every distinct edit to the original code is recorded (debounced) with its timestamp, direction, and compression ratio, browsable from the new &quot;History&quot; panel and restorable with a single click. The history is kept in memory only and is cleared on page reload. Undo / Redo controls for the original editor, exposed both as header buttons and through the existing keyboard shortcuts. ## [0.6.0] - 2026-07-25 ### Added Rust engine compiled to WebAssembly, providing a high-performance implementation of the whitespace-compaction pass and lexical token counting used by the minified output and status bar. Transparent engine bridge: the application attempts to load the compiled WASM engine on startup and automatically falls back to the existing JavaScript implementation when the WASM module has not been built or fails to load, with no change in behavior or output for the end user. &quot;Engine&quot; badge in the header indicating whether the active engine is WASM or the JS fallback. Integrated performance benchmark panel comparing the JS and WASM engines on the current source across a fixed number of iterations, with per-task timing bars and a computed speedup factor. `npm run build:wasm` script compiling the Rust engine with `wasm-pack`, wired into the production build and into the GitHub Actions deployment workflow. ## [0.5.0] - 2026-07-25 ### Added Reverse conversion (de-golfing): golfed code can now be converted back into structured, readable code. Restoration of readable variable names: identifiers are renamed according to their role (function names, parameters, loop counters, general values) instead of the original single-letter golfed names. Restoration of indentation and block structure: single-statement bodies are re-wrapped in blocks and the result is reformatted with clean indentation. Heuristic reconstruction of common golfed patterns: ternary and `&amp;&amp;`-shortcut conditionals are expanded back into `if`/`else` statements, countdown `for` loops are expanded back into ascending loops, and double-negation / falsy-literal boolean shortcuts are expanded back into their explicit form. Functional equivalence validation: after de-golfing, the reconstructed code is re-golfed and compared against a canonical signature of the original input to confirm the conversion is behavior-preserving, shown as an &quot;Equivalence&quot; status badge. Direction toggle in the interface to switch between &quot;Golf&quot; (structured to golfed) and &quot;De-golf&quot; (golfed to structured) conversion. ## [0.4.0] - 2026-07-25 ### Added Advanced loop golfing: counting `for` loops are rewritten as reverse countdown loops when the loop variable is unused in the body and the bound is safe to evaluate once, shortening the loop header. Ternary and conditional golfing: `if`/`else` statements are rewritten as conditional (ternary) expressions for matching `return` statements and matching assignments, and single-branch `if` statements using an expression are rewritten with the `&amp;&amp;` short-circuit shortcut. Native call golfing: known JavaScript golfing shortcuts are applied automatically, including `Boolean(x)` to `!!x`, `indexOf` presence checks to `includes`, `Array.prototype` to `[]`, and `.apply(null, x)` calls to spread syntax. Configurable golfing rules panel: each golfing technique (dead code elimination, declaration merging, loop golfing, ternary golfing, native call golfing, variable renaming, operator simplification) can be toggled on or off independently, with the converted output updating immediately. Character count display for both the original and converted code. Compression score, shown as a percentage of characters saved by the conversion. ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>GLConverter v0.9.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-0.9.0</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [0.9.0] - 2026-07-25 ### Added Full automated test suite (Vitest): unit tests for every golfing technique (dead code elimination, declaration merging, loop golfing, ternary/`&amp;&amp;` conditional golfing, native call shortcuts, variable renaming, operator simplification), unit tests for every de-golfing technique (conditional expansion, loop expansion, operator expansion, block normalization, descriptive renaming), unit tests for the equivalence validator, the parser, the language detector, the session history store, and the JS/WASM engine bridge, and interface integration tests exercising the mounted application (mode and direction toggles, rules panel, keyboard shortcuts, engine badge). GitHub Pages compatibility verification script (`npm run verify:pages`), run automatically at the end of every production build, checking that every emitted asset reference is relative. `npm test` now runs before the build step in the GitHub Actions deployment workflow. ### Changed The Three.js background scene and the engine benchmark module are now loaded on demand via dynamic `import()` instead of being bundled into the application&apos;s initial entry chunk. Production build output is now split into cacheable vendor chunks (Three.js, CodeMirror, the AST/golfing engine) instead of a single monolithic bundle, reducing the initial entry chunk from roughly 1 MB to about 36 kB and removing the Vite large-chunk warning. ## [0.8.0] - 2026-07-25 ### Added Global keyboard shortcuts, active anywhere in the application: `Alt+G` toggles Golf / De-golf, `Alt+M` toggles Minified / Justified, `Alt+C` copies the converted code, `Alt+E` opens Export, `Alt+H` opens History, `Alt+B` opens Benchmark, `Alt+R` opens Rules, `Alt+I` opens Import, and `Escape` closes any open panel. Every corresponding button now shows its shortcut in a tooltip. Keyboard-visible focus outline applied consistently across every interactive element, and `aria-expanded` / `aria-haspopup` states kept in sync on all dropdown-style panels for assistive technology. Reactive Three.js background transition: the point-field background now animates a brief pulse (speed, size, and opacity) on every conversion, and respects the operating system&apos;s reduced-motion preference by disabling the animation entirely when requested. ### Changed Refined responsive breakpoints for tablet and mobile viewports: the header and its controls now wrap cleanly, dropdown panels (History, Benchmark, Export, Rules) constrain themselves to the viewport width instead of overflowing on narrow screens, and the status bar and header actions reflow on small screens. ## [0.7.0] - 2026-07-25 ### Added Local file import: drag and drop a `.js` or `.txt` file onto the original editor panel, or use the new &quot;Import&quot; button to pick a file, to load its content for conversion. Export of the converted code as a downloadable `.js` or `.txt` file from the new &quot;Export&quot; menu in the converted panel. One-click &quot;Copy&quot; button to copy the converted code to the clipboard, with transient confirmation feedback. Session-local conversion history: every distinct edit to the original code is recorded (debounced) with its timestamp, direction, and compression ratio, browsable from the new &quot;History&quot; panel and restorable with a single click. The history is kept in memory only and is cleared on page reload. Undo / Redo controls for the original editor, exposed both as header buttons and through the existing keyboard shortcuts. ## [0.6.0] - 2026-07-25 ### Added Rust engine compiled to WebAssembly, providing a high-performance implementation of the whitespace-compaction pass and lexical token counting used by the minified output and status bar. Transparent engine bridge: the application attempts to load the compiled WASM engine on startup and automatically falls back to the existing JavaScript implementation when the WASM module has not been built or fails to load, with no change in behavior or output for the end user. &quot;Engine&quot; badge in the header indicating whether the active engine is WASM or the JS fallback. Integrated performance benchmark panel comparing the JS and WASM engines on the current source across a fixed number of iterations, with per-task timing bars and a computed speedup factor. `npm run build:wasm` script compiling the Rust engine with `wasm-pack`, wired into the production build and into the GitHub Actions deployment workflow. ## [0.5.0] - 2026-07-25 ### Added Reverse conversion (de-golfing): golfed code can now be converted back into structured, readable code. Restoration of readable variable names: identifiers are renamed according to their role (function names, parameters, loop counters, general values) instead of the original single-letter golfed names. Restoration of indentation and block structure: single-statement bodies are re-wrapped in blocks and the result is reformatted with clean indentation. Heuristic reconstruction of common golfed patterns: ternary and `&amp;&amp;`-shortcut conditionals are expanded back into `if`/`else` statements, countdown `for` loops are expanded back into ascending loops, and double-negation / falsy-literal boolean shortcuts are expanded back into their explicit form. Functional equivalence validation: after de-golfing, the reconstructed code is re-golfed and compared against a canonical signature of the original input to confirm the conversion is behavior-preserving, shown as an &quot;Equivalence&quot; status badge. Direction toggle in the interface to switch between &quot;Golf&quot; (structured to golfed) and &quot;De-golf&quot; (golfed to structured) conversion. ## [0.4.0] - 2026-07-25 ### Added Advanced loop golfing: counting `for` loops are rewritten as reverse countdown loops when the loop variable is unused in the body and the bound is safe to evaluate once, shortening the loop header. Ternary and conditional golfing: `if`/`else` statements are rewritten as conditional (ternary) expressions for matching `return` statements and matching assignments, and single-branch `if` statements using an expression are rewritten with the `&amp;&amp;` short-circuit shortcut. Native call golfing: known JavaScript golfing shortcuts are applied automatically, including `Boolean(x)` to `!!x`, `indexOf` presence checks to `includes`, `Array.prototype` to `[]`, and `.apply(null, x)` calls to spread syntax. Configurable golfing rules panel: each golfing technique (dead code elimination, declaration merging, loop golfing, ternary golfing, native call golfing, variable renaming, operator simplification) can be toggled on or off independently, with the converted output updating immediately. Character count display for both the original and converted code. Compression score, shown as a percentage of characters saved by the conversion. ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>GLConverter v0.8.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-0.8.0</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [0.8.0] - 2026-07-25 ### Added Global keyboard shortcuts, active anywhere in the application: `Alt+G` toggles Golf / De-golf, `Alt+M` toggles Minified / Justified, `Alt+C` copies the converted code, `Alt+E` opens Export, `Alt+H` opens History, `Alt+B` opens Benchmark, `Alt+R` opens Rules, `Alt+I` opens Import, and `Escape` closes any open panel. Every corresponding button now shows its shortcut in a tooltip. Keyboard-visible focus outline applied consistently across every interactive element, and `aria-expanded` / `aria-haspopup` states kept in sync on all dropdown-style panels for assistive technology. Reactive Three.js background transition: the point-field background now animates a brief pulse (speed, size, and opacity) on every conversion, and respects the operating system&apos;s reduced-motion preference by disabling the animation entirely when requested. ### Changed Refined responsive breakpoints for tablet and mobile viewports: the header and its controls now wrap cleanly, dropdown panels (History, Benchmark, Export, Rules) constrain themselves to the viewport width instead of overflowing on narrow screens, and the status bar and header actions reflow on small screens. ## [0.7.0] - 2026-07-25 ### Added Local file import: drag and drop a `.js` or `.txt` file onto the original editor panel, or use the new &quot;Import&quot; button to pick a file, to load its content for conversion. Export of the converted code as a downloadable `.js` or `.txt` file from the new &quot;Export&quot; menu in the converted panel. One-click &quot;Copy&quot; button to copy the converted code to the clipboard, with transient confirmation feedback. Session-local conversion history: every distinct edit to the original code is recorded (debounced) with its timestamp, direction, and compression ratio, browsable from the new &quot;History&quot; panel and restorable with a single click. The history is kept in memory only and is cleared on page reload. Undo / Redo controls for the original editor, exposed both as header buttons and through the existing keyboard shortcuts. ## [0.6.0] - 2026-07-25 ### Added Rust engine compiled to WebAssembly, providing a high-performance implementation of the whitespace-compaction pass and lexical token counting used by the minified output and status bar. Transparent engine bridge: the application attempts to load the compiled WASM engine on startup and automatically falls back to the existing JavaScript implementation when the WASM module has not been built or fails to load, with no change in behavior or output for the end user. &quot;Engine&quot; badge in the header indicating whether the active engine is WASM or the JS fallback. Integrated performance benchmark panel comparing the JS and WASM engines on the current source across a fixed number of iterations, with per-task timing bars and a computed speedup factor. `npm run build:wasm` script compiling the Rust engine with `wasm-pack`, wired into the production build and into the GitHub Actions deployment workflow. ## [0.5.0] - 2026-07-25 ### Added Reverse conversion (de-golfing): golfed code can now be converted back into structured, readable code. Restoration of readable variable names: identifiers are renamed according to their role (function names, parameters, loop counters, general values) instead of the original single-letter golfed names. Restoration of indentation and block structure: single-statement bodies are re-wrapped in blocks and the result is reformatted with clean indentation. Heuristic reconstruction of common golfed patterns: ternary and `&amp;&amp;`-shortcut conditionals are expanded back into `if`/`else` statements, countdown `for` loops are expanded back into ascending loops, and double-negation / falsy-literal boolean shortcuts are expanded back into their explicit form. Functional equivalence validation: after de-golfing, the reconstructed code is re-golfed and compared against a canonical signature of the original input to confirm the conversion is behavior-preserving, shown as an &quot;Equivalence&quot; status badge. Direction toggle in the interface to switch between &quot;Golf&quot; (structured to golfed) and &quot;De-golf&quot; (golfed to structured) conversion. ## [0.4.0] - 2026-07-25 ### Added Advanced loop golfing: counting `for` loops are rewritten as reverse countdown loops when the loop variable is unused in the body and the bound is safe to evaluate once, shortening the loop header. Ternary and conditional golfing: `if`/`else` statements are rewritten as conditional (ternary) expressions for matching `return` statements and matching assignments, and single-branch `if` statements using an expression are rewritten with the `&amp;&amp;` short-circuit shortcut. Native call golfing: known JavaScript golfing shortcuts are applied automatically, including `Boolean(x)` to `!!x`, `indexOf` presence checks to `includes`, `Array.prototype` to `[]`, and `.apply(null, x)` calls to spread syntax. Configurable golfing rules panel: each golfing technique (dead code elimination, declaration merging, loop golfing, ternary golfing, native call golfing, variable renaming, operator simplification) can be toggled on or off independently, with the converted output updating immediately. Character count display for both the original and converted code. Compression score, shown as a percentage of characters saved by the conversion. ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>GLConverter v0.7.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-0.7.0</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [0.7.0] - 2026-07-25 ### Added Local file import: drag and drop a `.js` or `.txt` file onto the original editor panel, or use the new &quot;Import&quot; button to pick a file, to load its content for conversion. Export of the converted code as a downloadable `.js` or `.txt` file from the new &quot;Export&quot; menu in the converted panel. One-click &quot;Copy&quot; button to copy the converted code to the clipboard, with transient confirmation feedback. Session-local conversion history: every distinct edit to the original code is recorded (debounced) with its timestamp, direction, and compression ratio, browsable from the new &quot;History&quot; panel and restorable with a single click. The history is kept in memory only and is cleared on page reload. Undo / Redo controls for the original editor, exposed both as header buttons and through the existing keyboard shortcuts. ## [0.6.0] - 2026-07-25 ### Added Rust engine compiled to WebAssembly, providing a high-performance implementation of the whitespace-compaction pass and lexical token counting used by the minified output and status bar. Transparent engine bridge: the application attempts to load the compiled WASM engine on startup and automatically falls back to the existing JavaScript implementation when the WASM module has not been built or fails to load, with no change in behavior or output for the end user. &quot;Engine&quot; badge in the header indicating whether the active engine is WASM or the JS fallback. Integrated performance benchmark panel comparing the JS and WASM engines on the current source across a fixed number of iterations, with per-task timing bars and a computed speedup factor. `npm run build:wasm` script compiling the Rust engine with `wasm-pack`, wired into the production build and into the GitHub Actions deployment workflow. ## [0.5.0] - 2026-07-25 ### Added Reverse conversion (de-golfing): golfed code can now be converted back into structured, readable code. Restoration of readable variable names: identifiers are renamed according to their role (function names, parameters, loop counters, general values) instead of the original single-letter golfed names. Restoration of indentation and block structure: single-statement bodies are re-wrapped in blocks and the result is reformatted with clean indentation. Heuristic reconstruction of common golfed patterns: ternary and `&amp;&amp;`-shortcut conditionals are expanded back into `if`/`else` statements, countdown `for` loops are expanded back into ascending loops, and double-negation / falsy-literal boolean shortcuts are expanded back into their explicit form. Functional equivalence validation: after de-golfing, the reconstructed code is re-golfed and compared against a canonical signature of the original input to confirm the conversion is behavior-preserving, shown as an &quot;Equivalence&quot; status badge. Direction toggle in the interface to switch between &quot;Golf&quot; (structured to golfed) and &quot;De-golf&quot; (golfed to structured) conversion. ## [0.4.0] - 2026-07-25 ### Added Advanced loop golfing: counting `for` loops are rewritten as reverse countdown loops when the loop variable is unused in the body and the bound is safe to evaluate once, shortening the loop header. Ternary and conditional golfing: `if`/`else` statements are rewritten as conditional (ternary) expressions for matching `return` statements and matching assignments, and single-branch `if` statements using an expression are rewritten with the `&amp;&amp;` short-circuit shortcut. Native call golfing: known JavaScript golfing shortcuts are applied automatically, including `Boolean(x)` to `!!x`, `indexOf` presence checks to `includes`, `Array.prototype` to `[]`, and `.apply(null, x)` calls to spread syntax. Configurable golfing rules panel: each golfing technique (dead code elimination, declaration merging, loop golfing, ternary golfing, native call golfing, variable renaming, operator simplification) can be toggled on or off independently, with the converted output updating immediately. Character count display for both the original and converted code. Compression score, shown as a percentage of characters saved by the conversion. ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>GLConverter v0.6.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-0.6.0</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [0.6.0] - 2026-07-25 ### Added Rust engine compiled to WebAssembly, providing a high-performance implementation of the whitespace-compaction pass and lexical token counting used by the minified output and status bar. Transparent engine bridge: the application attempts to load the compiled WASM engine on startup and automatically falls back to the existing JavaScript implementation when the WASM module has not been built or fails to load, with no change in behavior or output for the end user. &quot;Engine&quot; badge in the header indicating whether the active engine is WASM or the JS fallback. Integrated performance benchmark panel comparing the JS and WASM engines on the current source across a fixed number of iterations, with per-task timing bars and a computed speedup factor. `npm run build:wasm` script compiling the Rust engine with `wasm-pack`, wired into the production build and into the GitHub Actions deployment workflow. ## [0.5.0] - 2026-07-25 ### Added Reverse conversion (de-golfing): golfed code can now be converted back into structured, readable code. Restoration of readable variable names: identifiers are renamed according to their role (function names, parameters, loop counters, general values) instead of the original single-letter golfed names. Restoration of indentation and block structure: single-statement bodies are re-wrapped in blocks and the result is reformatted with clean indentation. Heuristic reconstruction of common golfed patterns: ternary and `&amp;&amp;`-shortcut conditionals are expanded back into `if`/`else` statements, countdown `for` loops are expanded back into ascending loops, and double-negation / falsy-literal boolean shortcuts are expanded back into their explicit form. Functional equivalence validation: after de-golfing, the reconstructed code is re-golfed and compared against a canonical signature of the original input to confirm the conversion is behavior-preserving, shown as an &quot;Equivalence&quot; status badge. Direction toggle in the interface to switch between &quot;Golf&quot; (structured to golfed) and &quot;De-golf&quot; (golfed to structured) conversion. ## [0.4.0] - 2026-07-25 ### Added Advanced loop golfing: counting `for` loops are rewritten as reverse countdown loops when the loop variable is unused in the body and the bound is safe to evaluate once, shortening the loop header. Ternary and conditional golfing: `if`/`else` statements are rewritten as conditional (ternary) expressions for matching `return` statements and matching assignments, and single-branch `if` statements using an expression are rewritten with the `&amp;&amp;` short-circuit shortcut. Native call golfing: known JavaScript golfing shortcuts are applied automatically, including `Boolean(x)` to `!!x`, `indexOf` presence checks to `includes`, `Array.prototype` to `[]`, and `.apply(null, x)` calls to spread syntax. Configurable golfing rules panel: each golfing technique (dead code elimination, declaration merging, loop golfing, ternary golfing, native call golfing, variable renaming, operator simplification) can be toggled on or off independently, with the converted output updating immediately. Character count display for both the original and converted code. Compression score, shown as a percentage of characters saved by the conversion. ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>GLConverter v0.5.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-0.5.0</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [0.5.0] - 2026-07-25 ### Added Reverse conversion (de-golfing): golfed code can now be converted back into structured, readable code. Restoration of readable variable names: identifiers are renamed according to their role (function names, parameters, loop counters, general values) instead of the original single-letter golfed names. Restoration of indentation and block structure: single-statement bodies are re-wrapped in blocks and the result is reformatted with clean indentation. Heuristic reconstruction of common golfed patterns: ternary and `&amp;&amp;`-shortcut conditionals are expanded back into `if`/`else` statements, countdown `for` loops are expanded back into ascending loops, and double-negation / falsy-literal boolean shortcuts are expanded back into their explicit form. Functional equivalence validation: after de-golfing, the reconstructed code is re-golfed and compared against a canonical signature of the original input to confirm the conversion is behavior-preserving, shown as an &quot;Equivalence&quot; status badge. Direction toggle in the interface to switch between &quot;Golf&quot; (structured to golfed) and &quot;De-golf&quot; (golfed to structured) conversion. ## [0.4.0] - 2026-07-25 ### Added Advanced loop golfing: counting `for` loops are rewritten as reverse countdown loops when the loop variable is unused in the body and the bound is safe to evaluate once, shortening the loop header. Ternary and conditional golfing: `if`/`else` statements are rewritten as conditional (ternary) expressions for matching `return` statements and matching assignments, and single-branch `if` statements using an expression are rewritten with the `&amp;&amp;` short-circuit shortcut. Native call golfing: known JavaScript golfing shortcuts are applied automatically, including `Boolean(x)` to `!!x`, `indexOf` presence checks to `includes`, `Array.prototype` to `[]`, and `.apply(null, x)` calls to spread syntax. Configurable golfing rules panel: each golfing technique (dead code elimination, declaration merging, loop golfing, ternary golfing, native call golfing, variable renaming, operator simplification) can be toggled on or off independently, with the converted output updating immediately. Character count display for both the original and converted code. Compression score, shown as a percentage of characters saved by the conversion. ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>GLConverter v0.4.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-0.4.0</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [0.4.0] - 2026-07-25 ### Added Advanced loop golfing: counting `for` loops are rewritten as reverse countdown loops when the loop variable is unused in the body and the bound is safe to evaluate once, shortening the loop header. Ternary and conditional golfing: `if`/`else` statements are rewritten as conditional (ternary) expressions for matching `return` statements and matching assignments, and single-branch `if` statements using an expression are rewritten with the `&amp;&amp;` short-circuit shortcut. Native call golfing: known JavaScript golfing shortcuts are applied automatically, including `Boolean(x)` to `!!x`, `indexOf` presence checks to `includes`, `Array.prototype` to `[]`, and `.apply(null, x)` calls to spread syntax. Configurable golfing rules panel: each golfing technique (dead code elimination, declaration merging, loop golfing, ternary golfing, native call golfing, variable renaming, operator simplification) can be toggled on or off independently, with the converted output updating immediately. Character count display for both the original and converted code. Compression score, shown as a percentage of characters saved by the conversion. ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>GLConverter v0.3.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glconverter</link>
    <guid isPermaLink="false">glconverter-0.3.0</guid>
    <pubDate>Sat, 25 Jul 2026 00:00:00 GMT</pubDate>
    <description># Changelog All notable changes to GLConverter are documented in this file. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). ## [0.3.0] - 2026-07-25 ### Added Golfing conversion pipeline applied to every conversion: minification, variable renaming, operator simplification, declaration merging, and dead code elimination. Minification engine: strips whitespace, newlines, and redundant semicolons from the converted output. Scope-aware variable renaming engine, assigning short identifiers (`a`, `b`, `c`, ...) while avoiding collisions with global references and reserved words. Operator simplification engine: converts increment/decrement patterns and compound arithmetic assignments to their shorter forms, and boolean literals to their shortest equivalent. Automatic merging of adjacent variable declarations of the same kind. Dead code elimination: removes unreachable statements, redundant pure expression statements, and literal `if` branches. Toggle button to switch the converted panel between &quot;Minified&quot; and &quot;Justified&quot; output modes. &quot;Justified&quot; mode: readable reformatting of the converted code with clean indentation. ## [0.2.0] - 2026-07-25 ### Added Dual-pane code editor built on CodeMirror 6 (Original / Converted). White theme applied to the code editors, matching the interface. Real-time synchronization: edits in the original pane automatically update the converted pane. Automatic source language detection, with JavaScript as the priority language. Source code tokenization and AST parsing powered by Acorn. Status bar showing detected language, token count, and character count. ## [0.1.0] - 2026-07-25 ### Added Initial project foundation with Vite and TypeScript. GitHub Pages compatible build configuration. Three.js background scene integrated into the interface. White theme baseline styling. Rust and WebAssembly engine skeleton for the future conversion engine. Initial `ConversionEngine` core module. Continuous deployment workflow to GitHub Pages. `LICENSE`, `README.md`, and `CHANGELOG.md`.</description>
  </item>
  <item>
    <title>ZipAll v1.1.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=zipall</link>
    <guid isPermaLink="false">zipall-1.1.0</guid>
    <pubDate>Fri, 24 Jul 2026 00:00:00 GMT</pubDate>
    <description>All four post-1.0 backlog items, implemented and verified end to end. ## Added **Drag &amp; drop** — dropping a folder from Windows Explorer onto the Source group sets it as the source directory and prefills the archive name from the folder name. **Password-protected archives** — an optional &quot;Password-protect&quot; checkbox and password field produce AES-256 encrypted `.zip` files via [SharpZipLib](https://github.com/icsharpcode/SharpZipLib) (`EncryptedArchiveWriter`/`EncryptedArchiveVerifier`), alongside the existing unencrypted Stored/Deflate path. **Command-line mode** — `ZipAll.exe --source &lt;dir&gt; --dest &lt;dir&gt; [options]` compresses headlessly, sharing the same core engine as the GUI, with `--help`, `--mode`, `--password`, repeatable `--exclude-file`/`--exclude-dir`, and `--quiet`. Running the exe with no arguments (or a single folder path) still opens the GUI. **Explorer &quot;Send to&quot; integration** — an optional installer task adds a `ZipAll` shortcut to the current user&apos;s Send To folder; launching the app with a single folder argument pre-fills that folder as the source in the GUI, so exclusions/password can still be reviewed before compressing. 6 additional unit tests (45 total) covering password round-trips, wrong-password rejection, exclusions, Stored mode, and confirming an unencrypted reader cannot read the encrypted content. ## Verification Manually verified end-to-end on real Windows hardware: installer rebuilt with the new `sendto` task, install/launch/uninstall cycle (including the Send To shortcut) confirmed clean, Send To launch confirmed via screenshot with the source pre-filled, CLI run from a real console with correct exit codes and output for plain and password-protected archives. ## Known limitations Tested on Windows 10 x64 only; Windows 11 and ARM64 remain unverified.</description>
  </item>
  <item>
    <title>ZipAll v1.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=zipall</link>
    <guid isPermaLink="false">zipall-1.0.0</guid>
    <pubDate>Fri, 24 Jul 2026 00:00:00 GMT</pubDate>
    <description>ZipAll&apos;s first stable release: a native Windows desktop app (C#/.NET, WinForms) that zips a folder, with exclusions, a custom archive name, and a choice of destination. ## Added `CONTRIBUTING.md`: contribution ground rules (C#/.NET/WinForms-only, English-only, no source comments, White theme only, Windows-only, BCL-only archive format, self-contained-only dependencies), PR checklist, and bug report template. `tests/ZipAll.Core.Tests`: xUnit unit test project (39 tests) covering `WildcardMatcher`, `ExclusionEngine`, `DirectoryWalker`, and `ArchiveWriter`/`ArchiveVerifier`, including empty folders, deeply nested trees, Unicode/special-character file names, and large flat trees. `tests/REGRESSION_CHECKLIST.md`: manual regression checklist to run against a built installer before every release. A real application screenshot (`docs/screenshot.png`), now embedded in the README. ## Fixed Inno Setup script: a mis-escaped `AppId` GUID and a `[Files]` wildcard entry that excluded every file the self-contained publish produces, both of which broke the installer build. `ArchiveWriter.CreateArchiveAsync` read `ZipArchiveEntry.Length`/`CompressedLength` after writing each entry, but those properties always throw `InvalidOperationException` in `ZipArchiveMode.Create` — every archive containing at least one file was broken. Found while writing the new unit test suite; fixed by deriving byte counts from the known file length and the underlying zip stream&apos;s position delta instead. ## Changed Version stamp moved to `1.0.0` (`MAJOR` now `1`). Installer build and install/uninstall cycle verified end-to-end (silent install, launch, silent uninstall with no residual files). Self-contained `win-x86` build published and smoke-tested under WOW64. ## Known limitations Tested on Windows 10 x64 only; Windows 11 and ARM64 have not been verified (no such machine was available during development). Zip64 / very large archive support relies on `System.IO.Compression` defaults and has not been explicitly stress-tested with multi-gigabyte archives.</description>
  </item>
  <item>
    <title>MD-Pro-Editor v0.8.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=md-pro-editor</link>
    <guid isPermaLink="false">md-pro-editor-0.8.0</guid>
    <pubDate>Wed, 22 Jul 2026 00:00:00 GMT</pubDate>
    <description>## MD Pro Editor v0.8.0 Free 7-day trial, no functional limitations. Permanent unlock by purchasing a license (€2.99). ### Major fixes Fixed a critical editor rendering bug (the Markdown editor did not display at all) Fixed a startup crash related to code folding Fixed the activation window lockup for already-activated users Fixed a visual overlap in the title bar ### New &quot;About&quot; tab (logo, copyright, contact, version) `.md` / `.markdown` file association now fully functional Complete documentation (README, CHANGELOG, real screenshot) ### Installation Download `MDProEditor-Setup-0.8.0.exe` below and run it. Windows SmartScreen may show a warning because the executable is signed with a test certificate (an official code-signing certificate will be used for the final public release). See [CHANGELOG.md](https://github.com/Patrickjaillet/MD-Pro-Editor/blob/main/CHANGELOG.md) for the full details.</description>
  </item>
  <item>
    <title>MicroShader v3.0.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-3.0.1</guid>
    <pubDate>Wed, 22 Jul 2026 00:00:00 GMT</pubDate>
    <description>UX feedback fix: the Controls panel becomes a persistent right-side inspector instead of a full-screen tab, and two actions that were previously keyboard/command-palette-only get visible buttons. ### Changed The &quot;Controls&quot; tab is removed; `Win32GolfControls` now renders as a fixed-width (300px) inspector docked to the right edge, visible on every tab except Viewport (which keeps the full window width, needed for Compare mode&apos;s split render). All other tab content (editors, Diff, Trace, Stats, Appearance, About) narrows to make room; the Source/Golfed minimap repositions to stay left of the inspector instead of the raw window edge. The pass-checkbox list switched from two columns to one: the two-column layout was sized for a full-width tab and truncated labels like &quot;Common subexpressions&quot; once squeezed into the 300px inspector. Single column uses the inspector&apos;s full available height instead, with no truncation. ### Added A &quot;Golf&quot; button at the top of the inspector runs the same recompile action as `F5` or the command palette&apos;s &quot;Run golf&quot; entry — previously keyboard/palette-only. A &quot;Formatted view&quot; toggle button in the top-right of the Golfed tab&apos;s content area, mirroring the existing `Ctrl+Shift+F` chord — previously also keyboard/palette-only. Both new buttons (`src/ui/win32_tool_button.h/.cpp`, a small reusable owner-drawn button) are registered for UI Automation like every other Phase 26 control, verified live with the same `System.Windows.Automation` client used throughout that phase.</description>
  </item>
  <item>
    <title>MicroShader v3.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-3.0.0</guid>
    <pubDate>Wed, 22 Jul 2026 00:00:00 GMT</pubDate>
    <description>*Breaking change**: µShader&apos;s UI framework is now native Win32 + Direct2D/DirectWrite/GDI+ + WGL-hosted OpenGL — Dear ImGui, SDL3, and the ImGuiColorTextEdit widget are fully removed from the build. `ushader.exe` is the Win32 shell built through Phases 22–26; the old SDL3/ImGui app is gone, not just superseded. This closes the Phase 22–27 migration arc. This release ships a deliberately smaller, curated feature set, not full parity with the retired app — see &quot;Removed / not yet ported&quot; below and `ROADMAP.md` Phase 28 for the tracked follow-up plan. ### Changed `CMakeLists.txt`: removed the `SDL3`, `imgui`, and `imguicolortextedit` `FetchContent` dependencies and library targets; links `user32`, `gdi32`, `gdiplus`, `d2d1`, `dwrite`, and `opengl32` directly (all in-box on every Windows 10/11 edition, including LTSC 2019 — no separate runtime to install). The former `ushader_win32_shell` CMake target is renamed `ushader`, reclaiming the name from the deleted SDL3 app; its subsystem changed from `/SUBSYSTEM:CONSOLE` (a development-time console window) to `/SUBSYSTEM:WINDOWS` — verified live that no console window appears. `theme_tokens.h`&apos;s color constants moved off `ImVec4` (the one remaining header-level ImGui dependency the Win32 shell had picked up) onto a local `tokens::Color4` struct with the same field layout. `installer/ushader.iss`, `README.md`, and `THIRD_PARTY_NOTICES.md` rewritten to describe the actual shipped shell and its real dependencies (previously: FFmpeg, `gif-h`, Inter/Lucide fonts — all removed, see below). ### Removed / not yet ported Six features from the retired ImGui app did not make it into the Win32 shell during Phase 26 and were deferred rather than bundled into this release — tracked in `ROADMAP.md` Phase 28, not silently dropped: Golfing profiles (`.ushaderprofile` save/load, `Maximum`/`Safe`/ `None` built-ins) — the CLI&apos;s `--profile` flag and the published schema are unaffected, this is only the GUI-side save/load UI. Multi-document workspace (multiple open shaders as separate tabs) and the session-restore-on-launch behavior that depended on it. One-click &quot;Copy as Shadertoy / Bonzomatic / bare main()&quot; clipboard exports. Importing a Shader Minifier–style exclude-name list. Self-contained HTML session report export. Viewport capture/recording: PNG screenshots, and GIF/MP4/WebM recording (the latter bundled `ffmpeg.exe` and `gif-h`, both now removed along with the feature). ### Fixed `assets/fonts/` (`Inter.ttf`, `lucide.ttf`) removed — unused once the ImGui shell that loaded them was gone; this phase&apos;s own opening sentence explicitly named the Lucide font as something that must not survive it. Several latent build issues surfaced only once SDL3/ImGui&apos;s incidental include-path and link propagation went away: `exclude_list_import_test` needed a real source swap (`file_dialog_sdl.cpp` → `file_dialog.cpp`, and `import_exclude_list_action` changed from taking `SDL_Window*` to `HWND`), three test targets had a dead `SDL3::SDL3` link, and `ushaderprofile_schema_test` was missing an explicit `rust_core` link it had never needed before. `workspace.h`/`.cpp` and `golf_controls.h` had a transitive dependency on the deleted `theme.h`, decoupled with a local literal default and by dropping dead SDL-specific declarations. `scripts/check_contrast.py`&apos;s token-parsing regex updated for the `ImVec4` → `Color4` rename — re-verified all four text/background pairs still pass WCAG AA (5.87:1 to 13.36:1). ### Known gap The Windows 10 LTSC 2019 hardware/VM acceptance pass from `ROADMAP.md` Phase 27 has not been performed — this development environment has no LTSC 2019 target. Everything verifiable here (clean build, all 8 test executables passing, `wgl_equivalence_test` still 5/5 bit-exact, live UI smoke test) is done.</description>
  </item>
  <item>
    <title>Z-GL-Shadertoy v2.13.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=z-gl-shadertoy</link>
    <guid isPermaLink="false">z-gl-shadertoy-2.13.0</guid>
    <pubDate>Tue, 21 Jul 2026 00:00:00 GMT</pubDate>
    <description>## What&apos;s changed This release removes several features that added maintenance surface without matching real usage: **Share links** — the share-link toolbar button and its versioned multi-pass variant. **Crash reporting** — the opt-in crash-report prompt and its underlying error-log forwarding. **Post-processing pipeline** — bloom, tone mapping, vignette, CRT filter, FXAA, TAA, film grain, and LUT color grading, along with the features built on top of it (FX Stack, Style Layers, Shape Mask, Color Remap, color-blindness simulation). **Performance panel** — the toolbar button and its popup (frame budget, FPS cap, resolution, profiling, GPU capabilities). **Tools menu** — the toolbar&apos;s overflow menu and its pinned-tools system, along with pop-out viewport, the standalone shader-docs panel (F1 still opens the full Help Center), the workspace panel, the Ray Marching Assistant, SDF Visualizer, SDF Composer, presentation mode, and the local &quot;open shader file&quot; / &quot;import ZIP&quot; actions. **Preset library** — the preset-browsing drawer, its save/wizard dialogs, and its ShaderToy-import row. Loading presets via the embed API and importing local shader/preset files continue to work. **Layout presets** — the layout toolbar button, built-in presets (Coder / Performer / Animator / Minimal), custom saved layouts, and the detach-panel feature. Manual panel/inspector resizing and the Uniforms/Channels tab switcher are unaffected. See CHANGELOG.md for full details. ## Installation Download and run the installer for your platform: **`Z-GL Shadertoy_2.13.0_x64-setup.exe`** — NSIS installer (recommended) **`Z-GL Shadertoy_2.13.0_x64_en-US.msi`** — MSI installer</description>
  </item>
  <item>
    <title>Z-GL-Shadertoy v2.12.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=z-gl-shadertoy</link>
    <guid isPermaLink="false">z-gl-shadertoy-2.12.0</guid>
    <pubDate>Tue, 21 Jul 2026 00:00:00 GMT</pubDate>
    <description>Full UI redesign of z-gl in the style of professional motion-graphics compositing software: Fixed 800x450 composition viewport, no fluid resizing. Single fixed dark theme (old multi-theme system fully removed). Sliders redesigned as scrubbable numeric fields. App-wide text/border contrast pass for readability. Compositor-style skin on the sidebar, inspector, toolbar, and panel tabs. See [CHANGELOG.md](https://github.com/Patrickjaillet/Z-GL-Shadertoy/blob/main/CHANGELOG.md) for full details. ## Downloads **Z-GL Shadertoy_2.12.0_x64-setup.exe** — NSIS installer (recommended) **Z-GL Shadertoy_2.12.0_x64_en-US.msi** — MSI installer</description>
  </item>
  <item>
    <title>MicroShader v2.0.1</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-2.0.1</guid>
    <pubDate>Sun, 19 Jul 2026 00:00:00 GMT</pubDate>
    <description>Drag-and-drop .glsl files onto the main window to open each as a new tab. Recent Files list under the File menu, persisted to %APPDATA%\ushader\, with automatic pruning of missing entries.</description>
  </item>
  <item>
    <title>AIPrompt v1.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=aiprompt</link>
    <guid isPermaLink="false">aiprompt-1.0.0</guid>
    <pubDate>Sat, 18 Jul 2026 00:00:00 GMT</pubDate>
    <description>First stable release of AIPrompt: a Windows desktop app to build, organize, and assemble AI prompts from a local library of reusable terms and phrases, and to generate structured `ROADMAP.md` files. ## Installation Download `AIPromptSetup_x64.exe` below, run it (no administrator rights required), and launch AIPrompt from the Start Menu or Desktop shortcut. Windows 10/11 x64, self-contained (.NET 8) — no separate runtime install needed. ## Highlights since the last pre-release ### Added Automatic semantic versioning: a single `VERSION` file drives `Version`/`AssemblyVersion`/`FileVersion`/`InformationalVersion` across all projects, with a local build script (`scripts/build.ps1`) that auto-increments `PATCH` Windows installer built with Inno Setup 6 (per-user install, Start Menu / Desktop shortcuts, clean uninstall) and a one-step build script (`scripts/build-installer.ps1`) Final `README.md` with installation instructions and real application screenshots `CONTRIBUTING.md`, issue templates, and `CODEOWNERS` ### Changed About tab now shows the full informational version (including pre-release suffixes) instead of a truncated 3-part version ### Fixed Removed a stray AI co-author mention found in git history during the final compliance pass (history rewritten, affected tags recreated) See `CHANGELOG.md` for the complete history.</description>
  </item>
  <item>
    <title>GLSL-Hyper-Golfer v1.0.0</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=glsl-hyper-golfer</link>
    <guid isPermaLink="false">glsl-hyper-golfer-1.0.0</guid>
    <pubDate>Sat, 18 Jul 2026 00:00:00 GMT</pubDate>
    <description>No release notes provided.</description>
  </item>
  <item>
    <title>MicroShader v1.2.5</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-1.2.5</guid>
    <pubDate>Sat, 18 Jul 2026 00:00:00 GMT</pubDate>
    <description>## New golf pass: common subexpression elimination When two declarations in a row initialize with a token-identical pure expression, the later ones now reference the first variable instead of recomputing it, e.g. `float a=dot(p,p),b=dot(p,p);` becomes `float a=dot(p,p),b=a;`. Deliberately scoped narrow for safety: only whole declaration initializers, only pure builtins/constructors (never a user function), and the candidate cache is cleared on every block boundary and any non-matching statement. Two real bugs were found and fixed during development, both now covered by regression tests: A variable-name lookup that read the pre-rename identifier instead of the actual rendered name. A cache-invalidation check that missed a brace following `)` or `else` rather than `;`. New &quot;Common subexpressions&quot; toggle in the Passes panel, matching per-pass counter in the stats panel. Unsigned installer - Windows SmartScreen may show an &quot;unknown publisher&quot; warning; click &quot;More info&quot; -&gt; &quot;Run anyway&quot;. Verified on Windows 10 (LTSC 2019, build 17763); not independently verified on Windows 11.</description>
  </item>
  <item>
    <title>MicroShader v1.2.4</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-1.2.4</guid>
    <pubDate>Sat, 18 Jul 2026 00:00:00 GMT</pubDate>
    <description>## New golf pass: algebraic identity simplification First step toward the most powerful GLSL golfing engine possible while never changing shader behavior (tracked going forward under ROADMAP.md Phase 11). Removes x*1, 1*x, x/1, x+0, 0+x, x-0 for a bare identifier x. Rewrites pow(x,2.) to x*x for a bare identifier/number x. Restricted to single-identifier operands so it can never duplicate or drop something with a side effect. New &quot;Algebraic identities&quot; toggle in the Passes panel, new per-pass counter in the stats panel. Covered by a new fixture (fixtures/algebraic_identities.glsl) and 4 new Rust unit tests. Unsigned installer - Windows SmartScreen may show an &quot;unknown publisher&quot; warning; click &quot;More info&quot; -&gt; &quot;Run anyway&quot;. Verified on Windows 10 (LTSC 2019, build 17763); not independently verified on Windows 11.</description>
  </item>
  <item>
    <title>MicroShader v1.2.3</title>
    <link>https://patrickjaillet.github.io/sandefjord-software/software.html?id=microshader</link>
    <guid isPermaLink="false">microshader-1.2.3</guid>
    <pubDate>Sat, 18 Jul 2026 00:00:00 GMT</pubDate>
    <description>## Phase 10 follow-up: two more gaps closed Checkboxes (all 16, across golf-pass toggles, &quot;Aggressive golf&quot;, &quot;Formatted view&quot;, &quot;Compare&quot;) are now a bespoke flat 12px square matching the Phase 10 spec, instead of the default ImGui shape. The &quot;Protected names&quot; field now shows a 1px accent focus ring while being edited. Unsigned installer - Windows SmartScreen may show an &quot;unknown publisher&quot; warning; click &quot;More info&quot; -&gt; &quot;Run anyway&quot;. Verified on Windows 10 (LTSC 2019, build 17763); not independently verified on Windows 11.</description>
  </item>
</channel>
</rss>
